// Half of a WaitlistInvitationAdapter shape. Deliberately incomplete.
export const partsA = {
  capabilities: {} as never,
  inviteToBook: async () => null as never,
  resendInvitation: async () => null as never,
};
