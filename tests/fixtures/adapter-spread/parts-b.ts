// The other half. Neither file alone satisfies the contract.
export const partsB = {
  cancelInvitation: async () => null as never,
  returnToWaitlist: async () => null as never,
  removeFromWaitlist: async () => null as never,
};
