node_modules
.pnp
.pnp.js
.yarn/install-state.gz

.next
out
build
dist

.vercel
.env*.local
.env

*.tsbuildinfo
next-env.d.ts

npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

.DS_Store
*.pem

coverage

# Local harness state (not relevant to repo)
.claude/scheduled_tasks.lock
# Per PR #160 closure: pilot tracker lives outside the repo
Hone_Pilot_Control_Sheet.xlsx

# Playwright (PR #227)
/test-results/
/playwright-report/
.env.local

# Sentry Config File
.env.sentry-build-plugin

# Supabase CLI local workspace state — carries the LINKED project reference
# (project-ref / linked-project.json / pooler-url). MUST stay untracked so a
# checkout never silently re-points the CLI at another project (the staging↔prod
# foot-gun that overwrote the staging link with the production ref).
/supabase/.temp/
