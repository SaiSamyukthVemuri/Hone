# P1 Reconciliation Report — 2026-07-18

**Exact production head:** `0a5741fdb4d92ff8f776ebe4736e03ccfa6626d3` (merge of PR #449)
**Hosted migration parity:** repo max = hosted max = **0133** (verified via linked CLI)
**Hosted flags at reconciliation:** `calendar_sync_control.worker_enabled=false`; 1 calendar
connection (`my-studio-9d37c5` only — **Willow excluded**); 3 studios total; Vercel prod deploy
of head successful; all CI lanes green on head.

## Method

34 source P1 records (18 historical audit `HNE-*` from `Hone_Findings_Register_2026-07-10.csv`,
13 canonical-roadmap `P1-01..13` from roadmap v1.1, 3 analytics `P1-ANALYTICS-*`) were
reconciled against the exact head by 13 independent investigation agents, each reading the
actual migrations (0001–0133), RLS policies, RPCs, server actions, and docs — never PR titles
or changelogs alone. The 3 analytics findings were classified from direct main-session evidence
(live `/ingest` traffic on hone.care; posthog-node source; call-site reads). Classification
rules: `DEPLOYED` requires code+migration on the prod head with hosted parity; `PRODUCTION
VERIFIED` additionally requires a documented controlled production exercise — none was granted
tonight on that standard. Enabled/exercised are tracked as separate axes (deployed-but-flag-off
is stated, not hidden).

## Result summary

| Classification | Count | IDs |
|---|---|---|
| DEPLOYED | 5 | HNE-SEC-001, HNE-SEC-002, HNE-REC-001*, HNE-REC-002, P1-13 |
| FIXED IN CODE | 3 | P1-ANALYTICS-01, P1-ANALYTICS-02, P1-ANALYTICS-03 (PR #450, draft, CI green, unmerged) |
| PARTIALLY FIXED | 16 | HNE-ADM-001, HNE-AUD-001, HNE-CONC-001, HNE-DEP-001, HNE-JOB-001, HNE-PAY-001, HNE-PAY-002, HNE-STO-001, P1-03, P1-04, P1-05, P1-06, P1-07, P1-08, P1-10, P1-11 |
| OPEN | 10 | HNE-CAL-001, HNE-EXP-001, HNE-LOC-001, HNE-PRV-001, HNE-RBAC-001, HNE-SAA-001, P1-01, P1-02, P1-09, P1-12 |

\* HNE-REC-001 is deployed with **both clinical flags OFF** — the finalization/amendment
enforcement protects no production record until enablement is separately authorized.

**Program-level P1s (cannot be patched, remain OPEN by design):** HNE-SAA-001 (self-service
lifecycle), HNE-LOC-001 (multi-location model). Delivered as verified-gap + architecture-input,
not "fixed by design".

**Notable post-audit closures found (audit was right, later work fixed them):**
- HNE-SEC-001 closed by migration **0115** (DELETE policies dropped + grants revoked; audited
  soft-delete only) — the 0115 header cites this audit finding directly.
- HNE-SEC-002 closed by migration **0116** (raw calendar-feed token column DROPPED; hash-only
  auth, rotate-once-display, revocation live).
- HNE-REC-002 closed by migration **0118** (DB-enforced submitted/reviewed intake immutability
  via trigger; amendment = new attributable row).
- P1-13 closed by the observation-chips contract work (structured persist + read-back
  verification + narrative/history/export/finalization inclusion). Reconciled DEPLOYED on
  code+tests; NOT Chloe-validated (a human-validation script remains before PRODUCTION VERIFIED).

**Analytics findings (P1-ANALYTICS-01/-02/-03) — FIXED IN CODE, undeployed (PR #450, head
`4bbe3a2`).** Independent review of the first containment patch found a residual blocking leak:
browser `$pageview`/`$pageleave` are NOT governed by `autocapture.url_allowlist`, so they left
every route including authenticated ones (`/clients/<uuid>`), confirmed in live PostHog Activity.
The patch replaces the autocapture-only guard with a fail-closed `before_send` boundary governing
EVERY browser event by (event, surface): only $pageview/$pageleave/autocapture-family/`marketing:*`
on the 12 exact canonical marketing routes survive; the authenticated app, booking, portal, all
token routes, login/auth and payment send nothing. Correction 2 replaces the `distinctId: string`
API with a discriminated, UUID-validated, fail-closed `AnalyticsActor`. 98 behavioral tests. These
three remain OPEN as production risk until #450 merges + deploys.

## Duplicate / relationship map

| Original | Canonical | Relationship |
|---|---|---|
| HNE-REC-002 ↔ P1-06 | intake immutability vs profile-promotion conflict | related, independent criteria (P1-06 remains PARTIALLY FIXED) |
| HNE-CONC-001 ↔ P1-04/P1-06 | concurrency umbrella vs intake-specific instances | parent/child; tracked separately |
| HNE-EXP-001 ↔ P1-12 ↔ HNE-PRV-001 | export scope vs retention/purge lifecycle | siblings under privacy-lifecycle program |
| HNE-PAY-001 ↔ HNE-PAY-002 | production proof vs per-studio canonical capability | independent; PAY-002 blocks broad PAY-001 |
| P1-ANALYTICS-01/02 | one boundary fix (PR #450) | siblings, single containment |

## First implementation slices (dependency order, after PR #450)

1. **Wave 1 foundations** — SAFE-WILLOW behavioral contract + Studio A/B/C synthetic fixtures
   (blocks every tenant/provider closure claim).
2. **HNE-STO-001** — read-only hosted Storage posture verifier + fail-closed gate (small,
   independent, high leverage).
3. **P1-02** — server-enforced versioned legal acceptance (independent of booking identity).
4. **P1-03/04/05/06 + HNE-CONC-001 intake slice** — token lifecycle + CAS + strict schema.
5. **P1-01** — public booking identity proof (pending product decision on OTP vs portal-rebook).
6. **P1-07/08/09/10/11** — transaction-boundary group (booking, cancel/reschedule, card
   replacement, webhook outcomes, consent dispatch).
7. **HNE-CAL-001** — practitioner-resource reservations (expand/verify/contract).
8. Waves 5–9 per the canonical roadmap.

## Per-finding detail


## DEPLOYED (5)

### HNE-REC-001
- **Enabled:** disabled — clinical_finalization_enabled and clinical_corrections_enabled are OFF for all 3 studios (default false; docs/production/current-state.md Clinical Record status table). Gates enforced both 
- **Rationale:** Acceptance ('every post-finalization change is an explicit attributable amendment preserving prior values') is fully met in deployed code+schema: once a session is finalized, DB triggers block every direct write for every role and the only mutation paths are the attributable append-only amendment and version-N+1 correction RPCs. But both studio flags are OFF everywhere, so no production record can become finalized and the enforcement is inert; the amendment half has zero production exercise and the Phase-2 customer workflow is explicitly PARKED. Additionally, all 59 pre-0119 sessions (includin
- **Key evidence:** supabase/migrations/0119_clinical_record_finalization_phase1.sql:458-611 finalize_session (flag check :508, CAS :533, ap · supabase/migrations/0119_clinical_record_finalization_phase1.sql:285-417 guard_finalized_clinical_write + 5 table trigge · supabase/migrations/0120_clinical_record_corrections_amendments_phase2.sql:94-219 append-only clinical_record_amendments · supabase/migrations/0120_clinical_record_corrections_amendments_phase2.sql:505-800 amend_finalized_session / amend_final
- **Remaining:** Enable clinical_finalization_enabled for willow-electrolysis (needs customer/operator approval; currently OFF for all studios) | Un-park + customer-approve the correction/amendment UX (generic 3-field correction UI deemed unsuitable; full-chart correction workspace def | First documented production amendment/correction exercise (0 so far; historic UI amendment failure never reproduced) | Legacy coverage: classify/handle the 59 record_origin='legacy' sessions — Willow's actual completed history is outside the finalization mode
- **Proposed PR:** No code PR required for the built scope. Activation plan instead: (1) operator+customer-approved enablement of clinical_finalization_enabled for willow-electrolysis, (2) build the parked full-chart correction workspace before enabling clinical_correc

### HNE-REC-002
- **Enabled:** Enabled unconditionally — the trigger has no feature flag and fires on every UPDATE of client_intake_forms; intake itself is live for all studios.
- **Rationale:** All acceptance criteria are met in merged, deployed code+migration: submitted/reviewed answers are DB-enforced immutable against direct authenticated PATCH (the original defect), token-path writes are status-guarded, and the only sanctioned amendment route is a new attributable intake row that preserves every prior version. Migration 0118 <= 0133 is applied to prod and the deploy of 0a5741f succeeded, but there is no explicit production validation record of a rejected forbidden update, so classification stops at DEPLOYED.
- **Key evidence:** supabase/migrations/0118_intake_terminal_immutability.sql:37-104 (trigger: responses/submitted_at immutable, no status r · app/intake/[token]/actions.ts:102-117,186-208 (service-role writes conditional on status='in_progress' — atomic race gua · lib/intake/queries.ts:176-203 (createIntakeRequestForClient: reissue ALWAYS inserts a new row, prior rows untouched) + s · app/(app)/clients/[id]/intake/actions.ts:18-91 (review sets reviewed_by = caller's own practitioner; notes-only edit pat
- **Remaining:** None for the acceptance itself; to upgrade to PRODUCTION VERIFIED: a documented controlled production exercise showing a direct authenticate
- **Proposed PR:** Optional hardening follow-up (not required to close): extend the 0118 trigger to also block deleted_at changes on submitted/reviewed rows (concealment), and add a documented production rejection exercise to the operator checklist to reach PRODUCTION 

### HNE-SEC-001
- **Enabled:** enabled — not flag-gated; the hardening is an unconditional DB policy/grant removal, active in the hosted database since 0115 was applied. The soft-delete-only app path (PR #391/#393) is live for all 
- **Rationale:** The finding was real and was independently confirmed, then closed by dedicated hardening migration 0115 + PR #393 (the 0115 header explicitly cites this audit finding). Acceptance ('no interactive tenant role can physically delete a treatment-pass row') is met in merged, prod-applied code: RLS DELETE policies dropped AND table-level DELETE/TRUNCATE grants revoked from anon+authenticated, with no later re-grant, no hard-delete RPC, and the app converted to an audited soft-delete with server-side actor derivation. Classified DEPLOYED rather than PRODUCTION VERIFIED because no explicit validation
- **Key evidence:** supabase/migrations/0115_entry_hard_delete_hardening.sql:34-35 — drops 'electrolysis_entries: members delete' and 'laser · supabase/migrations/0115_entry_hard_delete_hardening.sql:40-41 — revoke truncate, delete on both tables from anon, authe · supabase/migrations/0087_clinical_rls_delete_hardening.sql:238-240,261-263 — the original explicit authenticated DELETE  · supabase/migrations/0114_entry_soft_delete.sql:33-51 — audited triad columns + active partial indexes on both entry tabl
- **Remaining:** Optional (to reach PRODUCTION VERIFIED): record a controlled production negative test — e.g., in Sam's test studio (my-studio-9d37c5), attem
- **Proposed PR:** None — fix already merged and deployed (PR #393 + migration 0115). Only an optional ops task remains: a documented controlled negative-test in the test studio to upgrade the record to PRODUCTION VERIFIED (no code change).

### HNE-SEC-002
- **Enabled:** enabled (always-on). The hash-only credential model is not flag-gated; it is the only code path for the live calendar-feed feature. Rotation and revocation are live in Settings > Profile for all studi
- **Rationale:** Acceptance ('no tenant query returns a reusable calendar-feed secret') is met in production: migration 0116 (applied to hosted prod 2026-07-10; hosted parity max 0133 confirms) dropped the raw bearer column, so no query — member-scoped or otherwise — can return the reusable credential. The only member-readable remnant is calendar_feed_token_hash (returned by e.g. lib/supabase/queries.ts:206 getPractitionersForStudio select('*')), which is a one-way SHA-256 of a 256-bit CSPRNG token: it cannot be replayed against the route (the route hashes the URL's raw token) and preimage recovery is infeasib
- **Key evidence:** supabase/migrations/0116_drop_calendar_feed_raw_token.sql:29-32 — drops index practitioners_calendar_feed_token_uniq and · docs/production/migration-ledger.md:79 (and :24-25) — 0116 marked applied to hosted 2026-07-10, code-first after PR #395 · docs/production/release-changelog.md:47 — PR #395 / 0116 status Live, merge SHA b31fd3993047c36d8e11d63e737fbaf7b6fd0914 · app/calendar-feed/[token]/route.ts:81-87 — lookup is .eq('calendar_feed_token_hash', hashCalendarFeedToken(token)) via a
- **Remaining:** For PRODUCTION VERIFIED: record an explicit controlled production validation (e.g., operator-checklist entry: fetch a real feed URL on hone. | Optional defense-in-depth (not required by acceptance): column-restrict practitioner projections (e.g., getPractitionersForStudio select('*' | Docs hygiene: update stale docs/03_SECURITY_AND_PRIVACY.md:174 ('Partially resolved… Phase 2 is not started') to reflect the completed 0116 
- **Proposed PR:** None required to close the finding. Optional low-priority follow-up PR (docs + hardening hygiene): (1) fix the stale docs/03_SECURITY_AND_PRIVACY.md §174 'Partially resolved' entry to record 0116/PR #395 completion; (2) narrow practitioner select('*'

### P1-13
- **Enabled:** enabled — live for all studios, no feature flag anywhere on the chip path.
- **Rationale:** Every acceptance clause is satisfied in deployed code: selection is explicit structured state (never re-derived from free text), persists with strict read-back verification (a failed write surfaces an error instead of silently dropping chips), reloads via the shared resolveDisplayChips contract, renders in the visible narrative/history pills, is included in the CSV export and in the 0119 finalization snapshot. The original defect (chips written by block-setup-form only; SimplifiedEntryForm/addElectrolysisEntryAction never migrated) is closed by merged PR #411, hardened by #414, both on the pro
- **Key evidence:** git: d5ff51d (PR #411), c437b21 (PR #414), 6bdb7ec (PR #357) all merge-base ancestors of 0a5741f · lib/observation-chips.ts:58-143 normalizeChips/resolveDisplayChips/verifyStoredChips (strict raw read-back: noncanonical · app/(app)/clients/[id]/sessions/[sessionId]/actions.ts:227-408 — chips parsed FIRST, persisted, read back with .select(" · components/entry-row.tsx:86,252,326 — resolveDisplayChips → ObservationChips pills in all read/history surfaces (session
- **Remaining:** Document a controlled production verification pass (chips saved via SimplifiedEntryForm on Willow render after reload/in history) — code is  | Minor: no dedicated printable chart artifact exists (chips reach print only via browser-printing the session view; the clinical-notes print 
- **Proposed PR:** None required for the contract. Optional follow-ups: a docs-only PR recording the production verification, and a dedicated printable/exportable chart view if a formal chart-print artifact becomes a requirement.


## FIXED IN CODE (0 + 3 analytics)


## PARTIALLY FIXED (16)

### HNE-ADM-001
- **Enabled:** enabled (partial): the /admin surface and the 0113 audit trail are live in production with no feature flag (platform admin = ADMIN_EMAILS env allowlist per established facts). The missing controls (AA
- **Rationale:** Acceptance requires that an allowlisted email alone never unlocks service-role operations, backed by DB roles, AAL2/MFA, elevation, and audit. The audit criterion is met in deployed code (0113 + lib/audit/admin-actions.ts, append-only, redacted, blocked-attempt logging), and the allowlist itself was hardened fail-closed — so some criteria are met on prod head. But the material criteria remain: a single-factor magic-link session for an allowlisted mailbox immediately unlocks RLS-bypassing cross-tenant service-role reads and writes with no MFA/AAL2 assertion, no DB-level role separation, and no 
- **Key evidence:** /Users/chloebaca/Hone/lib/admin.ts:42-72 — isAdmin = ADMIN_EMAILS allowlist, fail-closed in prod, dev fallback list · /Users/chloebaca/Hone/app/admin/studios/new/actions.ts:37-79 — isAdmin check then createAdminClient() service-role cross · /Users/chloebaca/Hone/lib/supabase/admin-server.ts:9-27 — service-role client, RLS bypass, server-only import guard · /Users/chloebaca/Hone/app/(auth)/login/actions.ts:59 (signInWithOtp) + login/page.tsx:38 (signInWithOAuth) — single-fact
- **Remaining:** Require AAL2/MFA (e.g. supabase.auth.mfa getAuthenticatorAssuranceLevel === 'aal2') in the isAdmin gate before any createAdminClient() use | Introduce a DB-side platform-admin identity (dedicated table/claim, not env-only) so service-role scope is attributable and revocable per op | Add an elevation ceremony (short-lived re-auth/step-up) for mutating admin actions, distinct from ordinary session auth | Log read-only admin surface access (cross-studio data views) to admin_action_events, and decide whether the highest-risk writes should be au | Replace broad service-role table access with narrowly-scoped service-role RPCs for admin mutations where feasible
- **Proposed PR:** hardening/admin-step-up: (1) add an assertAdminElevated() helper that verifies isAdmin AND AAL2 (Supabase MFA) AND a recent re-auth timestamp, adopted by all /admin actions and layouts; (2) enroll TOTP for ADMIN_EMAILS accounts; (3) log admin read vi

### HNE-AUD-001
- **Enabled:** Always-on for the deployed portions — RLS policies, triggers, and RPC audit are not feature-flag gated and apply to all 3 studios including willow-electrolysis. Exception: the strongest clinical audit
- **Rationale:** The acceptance criteria ('every critical state transition has one durable non-user-forgeable event; audit failure rolls back mandatory mutation') are met only for the newer audit generations (payments 0032, record keeping 0086, clinical 0119/0120, admin table posture 0113, most appointment lifecycle RPCs incl. 0133), all merged and deployed. They remain unmet for the three legacy audit tables, which are user-insertable under live RLS policies (within-tenant forgery), and for several critical transitions whose audit is app-level insert-after-mutate that is explicitly fail-open (or entirely unch
- **Key evidence:** supabase/migrations/0001_init.sql:294 'audit_logs: members insert' policy (is_studio_member only; actor_id unconstrained · supabase/migrations/0010_booking_v1.sql:291 appointment_audit_member_insert (appointment-visibility only; actor_type/act · app/(app)/calendar/actions.ts:261 in-app appointment creation: `await supabase.from("appointment_audit").insert({...})`  · app/(app)/clients/[id]/actions.ts:309-326 archive (and :361 restore): audit_logs insert AFTER the mutation, failure only
- **Remaining:** Remove/replace member INSERT on audit_logs (0001) — any studio member can forge tenant audit rows with arbitrary action and actor_id | Remove/replace member INSERT on appointment_audit (0010) — actor_type/actor_id/action unconstrained; lifecycle evidence forgeable (incl. row | Harden session_audit content authorship (0008/0117) — within-tenant field/old/new forgery still possible; finalize_session evidence shares t | Make appointment-creation audit transactional on both paths (calendar/actions.ts:261 fire-and-forget; book/[slug]/actions.ts:874 post-commit | Decide + implement fail-closed posture for client archive/restore, session started_at edit, and SMS opt-out audit writes (currently log-and-
- **Proposed PR:** Audit-evidence hardening PR (migration 0134+, code-first ordering because deployed writers depend on the member INSERT policies): (1) add SECURITY DEFINER writer RPCs (or authorship triggers) for audit_logs / appointment_audit / session_audit that bi

### HNE-CONC-001
- **Enabled:** Mixed: the block-area CAS (0129) and intake terminal immutability (0118) are live and unconditional in production. The finalize/correction CAS is deployed but unreachable — both clinical flags are OFF
- **Rationale:** Acceptance ('a stale client cannot silently overwrite a newer clinical/intake version') is met only for finalized records (flag-gated, currently unreachable), the block area-edit path, and intake terminal states. Material silent-overwrite windows remain in merged code: most draft session field edits, the plain block/first-entry update paths, and the intake responses merge are all last-write-wins with no version token — exactly the failure mode the finding describes. Hence PARTIALLY FIXED: some criteria met in merged code, material criteria remain.
- **Key evidence:** supabase/migrations/0119_...sql:533-536 finalize CAS ('Record version conflict'); app/(app)/clients/[id]/sessions/[sessi · supabase/migrations/0120_...sql correct_finalized_session: 'if p_expected_record_version is null or <> v_version then ra · supabase/migrations/0129_atomic_session_block_area_writes.sql:99,128-130 — p_expected_updated_at CAS with FOR UPDATE, bu · block-setup-form.tsx:610 expectedUpdatedAt: block.updated_at; block-actions.ts:1280-1290 maps stale_block_version to a r
- **Remaining:** Version tokens (updated_at or record_version CAS) on the remaining draft clinical writes: next_session_note, started_at, performed_by, price | Make the 0129 p_expected_updated_at mandatory (reject null) so the CAS cannot be silently skipped by an omitting caller (new migration) | Optimistic concurrency on intake in-progress responses (per-field or whole-form version/updated_at CAS on the merge write) + practitioner in | A stale-conflict test/exercise record proving the reject-and-reload behavior end to end
- **Proposed PR:** Single focused PR: (a) thread expectedUpdatedAt through the remaining session/block/entry write actions and refuse-with-reload on mismatch (reuse the 0129 pattern and its distinct error mapping); (b) migration making update_session_block_with_areas' 

### HNE-DEP-001
- **Enabled:** Partially enabled: check-production-env-gates.mjs runs on every production build; verify-production.mjs and ledger reconciliation are manual-cadence operator actions (last run/reconciled 2026-07-17 pe
- **Rationale:** Acceptance requires EVERY production state traceable to an immutable source revision + an approved hosted-control manifest. Source-revision traceability largely holds for the app (Vercel deploys the production branch HEAD; merge SHAs + deploy IDs are recorded per PR through #434), and DB hosted controls are script-attested. But the manifest criterion fails on three material points: (1) the canonical ledgers contradict each other and reality on the same commit (hosted-max 0132 vs 0133; last runtime-bearing HEAD wrong by 15 merges), proving the manual process drifts within a single merge wave; (
- **Key evidence:** /Users/chloebaca/Hone/docs/production/migration-ledger.md:7 — 'Hosted (production) migration max = 0132' at prod head 0a · /Users/chloebaca/Hone/docs/production/current-state.md:15 — 'Last runtime-bearing production HEAD: 89e77f0 … PR #434', b · /Users/chloebaca/Hone/docs/production/release-changelog.md — last PR row is #434; no entries for #435–#449 despite all b · /Users/chloebaca/Hone/docs/production/current-state.md:22 — current HEAD deliberately 'not pinned inside this self-modif
- **Remaining:** Reconcile the three docs/production/* ledgers to head 0a5741f: migration-ledger hosted max 0132→0133; current-state last runtime-bearing HEA | Add an automated freshness tripwire (CI test failing when migration-ledger's stated hosted max ≠ repo max, mirroring the existing derived-ma | Produce an approved hosted-control manifest covering non-DB controls: external scheduler contract, required env inventory (values attested b | Bind deployments to attestation automatically (e.g., build-time stamp of SHA + vercel.json crons + env-gate results into a versioned artifac
- **Proposed PR:** Docs-only ledger reconciliation PR (migration-ledger hosted-max 0133, current-state runtime-bearing HEAD + #435–#449 rows, changelog entries) + a CI tripwire test asserting the ledger's stated hosted max equals the repo-derived max; follow-up (separa

### HNE-JOB-001
- **Enabled:** Mixed: the 3 vercel.json crons are registered on the prod deploy of 0a5741f. materialize-recurring-breaks is fully active. calendar-reconcile + calendar-sync are registered but DORMANT by hosted flags
- **Rationale:** Acceptance = every relied-upon job has declared schedule + owner + observable health + tested failure path. Met in merged code: declared schedules for 4 of 5 routes (3 in vercel.json pinned by tests; no-show declared-disabled), strong observable health for all live jobs, and real unit-tested failure paths including fail-closed auth and heartbeat/alert sabotage cases. Materially unmet: (1) the schedule of the single most business-critical job exists only on an external unversioned cron-job.org dashboard — declared in docs but not declarative/attestable, with dead-scheduler detection up to ~24h 
- **Key evidence:** /Users/chloebaca/Hone/vercel.json — exactly 3 crons: /api/cron/materialize-recurring-breaks '0 8 * * *', /api/cron/calen · /Users/chloebaca/Hone/app/api/cron/ — full handler inventory: appointment-reminders, calendar-reconcile, calendar-sync,  · /Users/chloebaca/Hone/lib/cron/reminder-schedule.ts:27 — APPOINTMENT_REMINDER_CRON_SCHEDULE = '*/15 * * * *' 'documents  · /Users/chloebaca/Hone/lib/cron/reminder-heartbeat.ts — heartbeat (reminder_cron:last_success, stale at 45 min) + deduped
- **Remaining:** Make the appointment-reminder schedule declarative/attestable: either move to vercel.json '*/15 * * * *' on a plan upgrade (the documented p | Reduce dead-scheduler detection latency below ~24h (e.g., run recordReminderSchedulerHealthAlert from all three daily crons, or an external  | Declare an owner per job (a jobs registry in docs/production or a machine-readable manifest consumed by a test): schedule, mechanism, owner, | Record the first calendar-reconcile/calendar-sync scheduled-run observation to close the C2 no-work validation (operator evidence task, sepa | Optional: document a production failure drill for the external scheduler (disable → observe stale/missing alert → resolve) so the failure pa
- **Proposed PR:** Small code PR: invoke the reminder-scheduler health check from the two calendar daily crons as well (3 daily detection points), plus a docs/production jobs manifest (route, schedule source, owner, health signal, failure remediation) pinned by a test 

### HNE-PAY-001
- **Enabled:** Live charging is enabled deployment-wide (STRIPE_ALLOW_LIVE_MODE=true + sk_live in Vercel Production per lib/stripe/server.ts header + docs/16 'Current production state (post-live-proof)'). Effective 
- **Rationale:** Acceptance = every approved studio has attributable provider/config + controlled-exercise evidence. Provider/config attribution is met structurally (0103 mode-scoped bindings, applied). Controlled-exercise evidence is met attributably for Sam's controlled studio but for Willow exists only as unattributable summary assertions, and no canonical registry marks studios approved. The finding's headline remains true by the docs' own admission: broad live safety is NOT production-proven ('Broad self-serve live payments are not ready').
- **Key evidence:** docs/16_LIVE_PAYMENTS_READINESS.md:1337-1345 — attributable controlled proof: 2 live charges + receipts + full refund on · docs/production/current-state.md:116-122,185-192 — canonical: live proven for 'approved studios (Willow + Sam's controll · docs/16_LIVE_PAYMENTS_READINESS.md:1493-1565 — §17.15 Willow first-live-charge checklist retained with ALL boxes uncheck · docs/production/release-changelog.md — no entries for PRs #413-#423 (Willow live/pilot + quick-checkout #419 era); no Wi
- **Remaining:** An attributable, dated controlled-exercise validation record for Willow (first supervised live charge / refund, PI-level references) checked | A canonical per-studio approval registry (DB state or at minimum a docs register) enumerating approved-live studios with who/when/evidence l | Changelog/current-state coverage of the #413-#423 live-pilot wave so the live-payment evidence chain is reconstructible | Explicit statement per studio of provider config (live Connect account id binding) in the evidence record
- **Proposed PR:** Docs-first PR: add docs/production/live-payment-approvals.md — per-studio approval + evidence register (Willow, Sam) with dates, operator, Connect-account binding, and links/ids for the controlled charges; backfill changelog entries #413-#423. Pairs 

### HNE-PAY-002
- **Enabled:** Live mode enabled deployment-wide in production (env flag + sk_live). The absent approval/suspension mechanisms cannot be 'disabled' — they are unbuilt.
- **Rationale:** Acceptance has five criteria. Expected-livemode and matching-Connect-account are robustly proven per live mutation in merged, deployed, applied code. Per-studio canonical approval, webhook non-elevation, and immediate per-studio suspension are structurally absent — Stripe live permission remains deployment-wide, exactly as the finding states.
- **Key evidence:** lib/stripe/server.ts:44-66 — live gate is the deployment-wide STRIPE_ALLOW_LIVE_MODE env + key prefix; header confirms p · grep lib/ app/ supabase/migrations for live_approved/approved_live/suspend: zero hits — no per-studio approval or suspen · lib/billing/session-payment-eligibility.ts:249-289 — prepare gates on mode-scoped settings row with stripe_account_statu · app/api/stripe/webhook/route.ts:302-372 — account.updated/capability.updated → sync_studio_account_status writes charges
- **Remaining:** Canonical per-studio live approval state (e.g. live_charges_approved_at/by on the live-mode studio_payment_settings row, or a studio_live_ap | Webhook non-elevation: account-status sync must not be sufficient for live chargeability — approval must be operator-set, never webhook-writ | Immediate per-studio suspension flag honored at execute time (fail-closed, no redeploy), plus an admin surface to flip it | Execute-time re-check of stripe_account_status='enabled' (currently only account id + mode are re-verified at charge time)
- **Proposed PR:** Migration-first PR: add live_charges_approved_at/approved_by + suspended_at to the live-mode studio_payment_settings row (service-role write only; webhook RPC must not touch them); gate prepare + execute on approved-and-not-suspended in live mode (te

### HNE-STO-001
- **Enabled:** disabled (as a gate) — no posture-mismatch blocking mechanism exists or is enabled at runtime, CI, or deploy. Meanwhile the photo feature itself is LIVE for all studios with no flag ('Treatment Photos
- **Rationale:** The finding's mechanism is confirmed and unremediated at the migration layer — 0093 (and 0092) can silently no-op their storage-schema portions on insufficient_privilege, and nothing since re-asserts posture. The acceptance criterion ('photo enablement blocked when hosted storage posture differs from approved manifest') is NOT met: there is no runtime, CI, deploy, or flag-based gate tying photo availability to storage posture — photos are live unconditionally and would keep working (and keep signing URLs) even if the bucket flipped public or an authenticated storage.objects policy appeared. Ho
- **Key evidence:** supabase/migrations/0093_harden_treatment_image_storage.sql:37-57 — bucket-privacy DO block and storage.objects policy-d · Grep of supabase/migrations 0094-0133: no migration touches storage.buckets/storage.objects again — only incidental 'sto · scripts/verify-production.mjs:184-199 fail-closed '0093 bucket' check (exists + private via supabase db query --linked); · app/(app)/clients/[id]/images/actions.ts — service-role upload (line ~173-176) and createSignedUrl (~284-287) against th
- **Remaining:** No blocking gate: photo upload/signing (or deploy/CI) does not refuse when hosted storage posture differs from the approved manifest — add a | storage.objects policy posture on the HOSTED project is verifiable only by a manual dashboard eyeball with no completion record found — eith | The 0093/0092 insufficient_privilege skips remain notice-only at apply time; any future re-run or new storage migration inherits the same si | verify-production bucket check is operator-run on demand only; posture drift between runs (bucket flipped public, authenticated policy added
- **Proposed PR:** Small hardening PR, no migration: (1) add a fail-closed runtime posture assertion to the treatment-image server actions or a cached periodic check (service-role storage.getBucket('treatment-images') must return public=false; on mismatch, refuse uploa

### P1-03
- **Enabled:** Deployed parts live for all studios, no flag. Revocation/supersession: does not exist, so nothing to enable.
- **Rationale:** Material acceptance criteria unmet: 'supersession' and 'revocation' are absent (stateless HMAC with no server-side version check means minting a fresh link does NOT kill older ones), and 'no sensitive-answer exposure through dead links' only holds for submitted/reviewed intakes — a superseded-but-unexpired link to an in-progress intake still reads and writes partial answers. The fixed parts (dedicated secret, 14-day TTL down from 60, status-based post-submission lockout, reissue-as-new-row) are real, merged, and deployed, hence PARTIALLY FIXED rather than OPEN.
- **Key evidence:** lib/intake/tokens.ts:64-117 (stateless HMAC verify: signature + embedded expiry only, no DB/version check) · lib/intake/queries.ts:15-26 ('A future enhancement may add client_intake_forms.token_version … so a forwarded link can b · lib/intake/queries.ts:230-237 + app/(app)/clients/[id]/intake/actions.ts:252-299,305-387 (Copy link / Resend mint fresh  · supabase/migrations/0097_intake_link_metadata.sql ('NO raw token / token column stored (verification stays stateless)')
- **Remaining:** Supersession: minting a fresh link must invalidate previously issued tokens for the same intake (e.g. token_version in HMAC payload + column | Revocation: practitioner affordance to kill an in-progress intake link immediately (leak response) without waiting 14 days | No partial-answer exposure through superseded links: page/save must reject tokens older than the current version | Correct the docs/02_DOMAIN_MODEL.md claim of a column-based token or implement it
- **Proposed PR:** Add client_intake_forms.token_version smallint NOT NULL DEFAULT 0 (new migration), include it in the HMAC payload, verify equality on page load + save + submit, increment on every mint (booking, copy-link, resend, cron reminder — automatic supersessi

### P1-04
- **Enabled:** Deployed mitigations live for all studios (no flag). Missing CAS: not built.
- **Rationale:** Acceptance ('two writers / stale offline saves cannot silently lose answers') is not met: last-write-wins per key with full-map payloads means a stale writer silently reverts newer answers. The merged merge-semantics and atomic status guard eliminate the omitted-key clobber and the post-submit race — genuine partial credit — but the core lost-update criterion materially remains.
- **Key evidence:** app/intake/[token]/actions.ts:89-94 (JS spread merge over freshly SELECTed responses — read-modify-write, no version pre · app/intake/[token]/actions.ts:102-117 (UPDATE conditional only on status='in_progress' + deleted_at IS NULL — never on a · app/intake/[token]/IntakeWizard.tsx:113-117,134-138,150 (full `responses` state posted on every goBack/goNext/submit — s · supabase/migrations/0015_client_intake_forms.sql:27-30 (schema: status + responses jsonb; no version column in any later
- **Remaining:** Optimistic concurrency on responses: a version (or updated_at) compare-and-swap in saveIntakeStepAction and submitIntakeAction that fails cl | Wizard conflict handling: on CAS failure, refetch server responses, re-merge locally, surface to the user instead of silently retrying | Stale offline replay cannot revert newer answers (covered by the CAS + refetch pair)
- **Proposed PR:** New migration: add client_intake_forms.responses_version integer NOT NULL DEFAULT 0. Actions: SELECT responses_version with responses; UPDATE … SET responses_version = v+1 WHERE id=… AND status='in_progress' AND responses_version=v; zero rows with in

### P1-05
- **Enabled:** Deployed partial checks live for all studios (no flag). Strict value/schema validation: not built.
- **Rationale:** Acceptance requires exact schema validation, strict enums, lengths, array/cardinality, and total-body limits. Only unknown-key rejection, step bounds, and required-presence are met; every value-level criterion (types, enums, lengths, cardinality, total size) is unmet server-side. Genuine merged partial coverage → PARTIALLY FIXED, with the bulk of the acceptance still OPEN.
- **Key evidence:** app/intake/[token]/actions.ts:38-46 (key whitelist only — values passed through untouched: `out[key] = value`) · lib/intake/questions.ts:763-799 (isAnswerProvided/findMissingRequiredAnswers: presence checks only, no enum/length/type  · next.config.ts:56-57 (serverActions bodySizeLimit: '16mb' — global, so intake actions accept up to 16MB payloads) · lib/rate-limit/public.ts:277-278 (intake_save 30/5m, intake_submit 5/5m — bounds frequency, not payload size/shape)
- **Remaining:** Server-side per-question value validation: type per QuestionType, enum membership against Option values for select/multi_select, checkbox st | String length limits (answers and _notes) and date sanity (mirror client rules server-side) | Array cardinality caps for multi_select (bounded by the question's option count) | Total responses size cap enforced in both actions before write (and optionally a DB CHECK e.g. pg_column_size(responses) limit as defense-in
- **Proposed PR:** Code-only PR (no migration): add validateResponses(responses) to lib/intake/questions.ts — schema-driven type/enum/length/cardinality enforcement + a total-serialized-size cap (e.g. 64KB) — called from both saveIntakeStepAction and submitIntakeAction

### P1-06
- **Enabled:** Live for all studios (no flag) — runs on every intake submission's winner branch.
- **Rationale:** Acceptance is 'profile-promotion conflicts detected, not silent-last-write'. The merged field rules (fill-only-if-null, append-only allergies) eliminate the common case — an intake submission can no longer clobber values Chloe entered before the submission — which is substantial partial credit. But within the SELECT→UPDATE window a concurrent practitioner edit still loses silently, and there is zero conflict detection, so the named acceptance criterion remains unmet → PARTIALLY FIXED.
- **Key evidence:** app/intake/[token]/actions.ts:294-300 (client row SELECT — stale basis for the patch) · app/intake/[token]/actions.ts:311-341 (fill-only-if-null per field; allergies concat computed in JS from the stale read) · app/intake/[token]/actions.ts:344-347 (UPDATE … eq('id', clientId) only — no IS NULL guards, no CAS, silent last-write i · app/intake/[token]/actions.ts:210-215,348-350 (sync failures logged and swallowed; no conflict signal to anyone)
- **Remaining:** Conditional per-field promotion (e.g. UPDATE … WHERE emergency_contact_name IS NULL per field, or a single SQL statement with COALESCE seman | Allergies append performed atomically in SQL (set allergies = allergies || …) rather than JS-concat of a stale read | Detection + surfacing: when a promotion write is skipped/conflicted, record it (e.g. ops_alerts or practitioner notification) instead of sil
- **Proposed PR:** Code-only PR (no migration): rewrite syncIntakeToClient to issue per-field conditional UPDATEs (WHERE field IS NULL) and an atomic SQL allergies append (single UPDATE with allergies = CASE WHEN … concat), compare affected-row results to detect confli

### P1-07
- **Enabled:** enabled — no feature flag; ack requirement + post-commit evidence write are live for any studio with policy text on file (hasAnyPolicy gate).
- **Rationale:** One of two acceptance criteria (immutable policy snapshots) is met in merged+deployed code; the material criterion (acknowledgement evidence commits atomically with the state transition) is unmet — the ack insert is post-commit, non-transactional, non-retryable by design in both flows.
- **Key evidence:** app/cancel/[token]/actions.ts:348-385 — ack insert after RPC commit; documented non-rollback + non-retryable; failure on · app/reschedule/[token]/actions.ts:755-791 — same pattern after reschedule_appointment; public_reschedule_policy_ack_inse · supabase/migrations/0056_appointment_policy_acknowledgements.sql:30-107 — NOT NULL snapshot columns default '', hash CHE · lib/booking/policy-acknowledgement.ts:61-75 — canonical SHA-256 snapshot hash shared by both actions
- **Remaining:** Ack evidence row must be inserted inside the same transaction as the status transition (extend both RPCs via a new migration to accept snaps | Cancel path: snapshot should be built from the same studio row used for the requiresAck gate (currently re-read post-RPC via the apptRow joi
- **Proposed PR:** New migration: add p_ack_* params (snapshot texts + hash, or a requires-ack boolean) to public_cancel_appointment_with_token and reschedule_appointment; insert the appointment_policy_acknowledgements row inside the RPC transaction and fail the transi

### P1-08
- **Enabled:** Enabled — the webhook route is live on prod head; production runs LIVE Stripe mode, and shouldIgnoreLiveModeEvent is a mode-MATCHING guard (PR #323, lib/billing/payment-webhook-reconciliation.ts lines
- **Rationale:** Acceptance requires the existing active card not be retired until the replacement local state is safely persisted, plus duplicate/out-of-order/concurrent safety. Only the duplicate-webhook criterion is met in merged, deployed code; retire-before-persist ordering, out-of-order fencing, and concurrent-replacement silent-drop all remain, now applying to LIVE cards.
- **Key evidence:** app/api/stripe/webhook/route.ts:647-659 — pre-flip UPDATE to status='removed' runs before the INSERT (661-680); two stat · app/api/stripe/webhook/route.ts:686-693 — any 23505 on INSERT (including the 0104 one-active unique from a concurrent di · app/api/stripe/webhook/route.ts:575-598 — SELECT-first idempotency covers same-SetupIntent re-delivery only; no created- · supabase/migrations/0059_client_payment_methods_setup_intent_unique.sql — duplicate-delivery DB backstop (applied)
- **Remaining:** Atomic replacement: retire the old active row and persist the new one in one transaction (RPC), or insert-pending-then-promote so failure ne | Out-of-order fence: compare SetupIntent/event created timestamps before allowing an older intent to displace a newer active card | Distinguish 23505 causes: setup-intent-unique = idempotent success; one-active-unique from a concurrent distinct event = retry/alert, never  | Ops alert when a pre-flip succeeded but the insert failed (zero-active-card window)
- **Proposed PR:** Add a service-role transactional RPC replace_client_payment_method (retire same-mode active + insert new active atomically, with SetupIntent created-time fencing); webhook calls it instead of the UPDATE+INSERT pair; split the 23505 handling by constr

### P1-10
- **Enabled:** enabled — public booking is live, unflagged, for all 3 studios.
- **Rationale:** Material criteria met in merged code (exclusion-constraint dedup for the same slot; non-throwing email/SMS providers with truthful attempt recording), but the core criteria — one authoritative booking command, idempotent retries, and a guarantee that post-commit failures never surface as a booking failure — remain unmet: concrete throwing calls sit between the appointment commit and the ok return.
- **Key evidence:** app/book/[slug]/actions.ts:770-825 — bare appointments INSERT (own autocommit), 23P01 → slot_taken; no wrapping transact · app/book/[slug]/actions.ts:669-673, 625-631 — client row INSERT / sms_consent_at stamp commit before the appointment ins · lib/treatment-time/queries.ts:284-285 — getTreatmentTimeContextForEmail throws on DB error; called uncaught post-commit  · lib/app-origin.ts:53-56 — getRequiredAppOrigin throws in misconfigured prod; called post-commit at actions.ts:883
- **Remaining:** Single authoritative booking command (RPC/transaction covering client upsert + appointment insert + audit + consent evidence) | Idempotency key so a retried command returns the existing booking instead of slot_taken | Post-commit section wrapped so NO throw (treatment-time read, app-origin, PostHog) can convert a committed booking into a reported failure; 
- **Proposed PR:** Migration adding public_book_appointment RPC (SECURITY DEFINER, service-role-only) doing client-resolve/insert + appointment insert + appointment_audit + booking_tracking_consents in one transaction with a unique (studio_id, idempotency_key); action 

### P1-11
- **Enabled:** Consent-evidence capture: enabled (unflagged, every public booking). Provider dispatch: disabled in effect — gates require a studio_tracking_providers row with enabled=true plus a decryptable per-stud
- **Rationale:** The deployed 0106 dedup claim satisfies the idempotency half of one criterion, but the two material criteria — atomic consent evidence + durable intent, and a DB consent recheck at dispatch time — are absent: consent persistence is best-effort post-commit and the dispatcher trusts an in-memory flag with no durable queue or replay.
- **Key evidence:** app/book/[slug]/actions.ts:851-872 — consent insert in void async IIFE post-commit; failure logged only (public_booking_ · app/book/[slug]/actions.ts:890-901 — inline void dispatchBookingConversion with consentGranted: marketingConsent (form v · lib/conversion/dispatch.ts:61-62 — Gate 1 = params.consentGranted; no read of booking_tracking_consents anywhere in the  · lib/conversion/dispatch.ts:109-115 — claim_conversion_delivery atomic dedup (0106) with deterministic event id
- **Remaining:** Write consent evidence + a durable provider-intent row in the same transaction as (or atomically after) the booking commit | Dispatcher must re-read the persisted consent row before any send (deny when absent), not trust the in-memory flag | Durable replay: drain intents via worker/cron with bounded retries; keep the 0106 claim as the dedup layer
- **Proposed PR:** Migration adding a conversion_intents (outbox) table + RPC that inserts booking_tracking_consents and the intent atomically; move dispatch to a cron/worker drain that re-reads the consent row per intent, keeps claim_conversion_delivery dedup, and ret


## OPEN (10)

### HNE-CAL-001
- **Enabled:** enabled — the studio-wide exclusion constraints are unconditional DB constraints, always enforced (no flag).
- **Rationale:** The headline acceptance criterion — parallel legitimate appointments succeed — is architecturally unmet with zero remediation progress; the studio remains one exclusive resource. This is, however, a documented deliberate v1 product constraint, not an oversight: docs/integrations/google-calendar-sync.md §1 'Product constraint: Hone scheduling is studio-wide today' records it and hard-gates Phase C (Google busy) on either whole-studio-block or practitioner-resource-awareness. Race-safety of the sole existing resource is intact, so this is an accepted product-model limitation rather than a correc
- **Key evidence:** supabase/migrations/0029_double_booking_constraint.sql:236-240 — exclusion keyed (studio_id with =, tstzrange &&), statu · supabase/migrations/0030_calendar_reservations.sql:556-562 — cross-type shadow exclusion keyed on studio_id only; table  · supabase/migrations/0133_practitioner_move_appointment.sql:12-16, 48-58 — reuses the per-studio constraints; practitione · docs/integrations/google-calendar-sync.md:64-89 — studio-wide scheduling documented as a deliberate product constraint w
- **Remaining:** Introduce a resource dimension (practitioner_id or resource_id) on appointments + studio_calendar_reservations and re-key both GiST exclusio | Availability computation (lib/booking/slots.ts) and all lifecycle RPCs must become resource-aware before a multi-practitioner studio onboard
- **Proposed PR:** Not a tonight-fix. Either (a) formally accept: record HNE-CAL-001 as an accepted product constraint referencing google-calendar-sync.md §1 in the risk register, or (b) design PR: add resource_id to appointments + reservations shadow (backfilled to on

### HNE-EXP-001
- **Enabled:** enabled — export button live for every studio owner, no flag; the overbroad claims ('Export your full studio history any time' app/page.tsx:56, 'Full data export, any time' app/pricing/page.tsx:49, RE
- **Rationale:** The finding is confirmed and unremediated on prod head 0a5741f. This is not a broken feature — it is a working partial export sold as a full one, with an in-memory transport that cannot scale and no pagination, so it will also become silently incomplete (row-cap truncation) before it becomes visibly broken. Tonight's untracked CAPABILITY_MANIFEST.json itself records it as open ('scope narrower than full-export copy; synchronous/in-memory'). Classified OPEN rather than PARTIALLY FIXED because both acceptance criteria (copy-accurate manifest; size/SLO limits) fail; the export's existence predate
- **Key evidence:** app/(app)/settings/data/actions.ts:64-197 — full-table Promise.all selects, zero .range() pagination; :774-775 in-memory · app/(app)/settings/data/actions.ts:161,658 — only mentions of payment/photo tables are comments confirming deliberate ex · supabase/migrations/0126_client_clinical_notes.sql — clinical notes table live for all studios (no flag, per Willow PR A · app/page.tsx:56 'Export your full studio history any time'; app/pricing/page.tsx:49 'Full data export, any time'; app/(a
- **Remaining:** Export scope covers, or copy accurately discloses exclusion of: clinical notes, consent signatures, intake forms, payment records, photos, p | Marketing ('full studio history', 'Full data export'), settings-page, README, and terms §16 copy reconciled to actual scope | Pagination or async job removing the PostgREST row-cap silent-truncation risk; defined size/SLO limit with a tested failure mode | Audit manifest (files + row_counts) generated from the actual ZIP contents, not a hardcoded stale list | Async export path (roadmap DATA-02) or documented synchronous ceiling with enforcement
- **Proposed PR:** Single containment PR, no migration: (1) add clinical notes + consent signatures + intake forms to the export (owner-only, same RLS client) or explicitly list every exclusion in README + settings 'Not included' + soften app/page.tsx and pricing copy;

### HNE-LOC-001
- **Enabled:** Not applicable — no flag; the domain model does not exist.
- **Rationale:** Acceptance criterion — every location-sensitive entity/action has an explicit location owner + authorization rule — is unmet everywhere: there is no location entity to own anything, no location dimension on appointments/availability/inventory/records, and authorization is exclusively studio_id-scoped RLS. Confirmed program-level OPEN with zero partial landing; the product currently equates studio == single location by construction.
- **Key evidence:** supabase/migrations 0001-0133 full create-table list — no locations/organizations/rooms/resources table · supabase/migrations/0010_booking_v1.sql:24 — studios.address is a single free-text column (only physical-location datum  · supabase/migrations/0010_booking_v1.sql:174-190 — appointments columns: studio_id/practitioner_id/client_id/service_id o · Availability tables all studio-scoped: studio_availability_default, studio_availability_overrides, studio_blockouts, stu
- **Remaining:** Location entity (table + RLS) with ownership under studio (or organization) and explicit owner | location_id on location-sensitive entities: appointments, availability/blockouts/breaks, services, inventory (probe_lots, record_keeping_*), | Location-aware authorization rules (who can act at which location) layered on the existing studio_id RLS | Location-aware scheduling (per-location availability instead of studio-wide) — interacts with the Google Calendar Phase-C studio-wide-schedu | Organization layer decision (multi-studio vs multi-location-within-studio) before any schema work
- **Proposed PR:** Not a single PR — requires a domain-model design decision first (organization -> studio -> location hierarchy vs locations within studio), then a migration series adding a locations table + location_id backfill (defaulting existing rows to a per-stud

### HNE-PRV-001
- **Enabled:** disabled/absent — no workflow to enable; the one deployed adjacent capability (clinical corrections) is behind clinical_corrections_enabled = OFF for all studios (Clinical Phase 2 parked). Live privac
- **Rationale:** Confirmed OPEN on 0a5741f. The acceptance bar — every lifecycle request has attributable state, scope, deadline, completion evidence, and an exception path — is met for zero request types because there is no request object at all; the strongest partial (corrections backend 0119/0120) is deployed-dormant, unapproved, and tracks amendments, not requests. Independent of P1-12: that finding is the missing data-layer machinery (states/hold/purge/manifest); this one is the missing operational process layer that would consume it — fixing either alone does not close the other. Classified OPEN rather t
- **Key evidence:** Repo-wide grep (lib/, app/, supabase/): zero matches for dsar|privacy_request|data_subject|subject_access|deletion_reque · app/privacy/page.tsx:331-334 — 'We will respond within 30 days' + practitioner-first routing; :352-362 — 30-day account- · app/terms/page.tsx:198-212,481-497 — §7.6/7.7 DPA assist-with-requests + 30-day deletion, §16 termination export via pri · app/(app)/clients/[id]/actions.ts:269-332 — deletion = soft archive with audit row; 'Hard delete is intentionally NOT su
- **Remaining:** A request object (access / correction / deletion / export / offboarding) with attributable state machine: received -> verified -> scoped ->  | Scope binding: which data classes and subjects each request covers | Deadline tracking honoring the published 30-day commitment, with alerting | Completion evidence: what was done, by whom, when — durable and auditable | Exception path: legal-obligation refusals (e.g. 7-year billing retention) recorded with reason
- **Proposed PR:** PR-1 (small, no migration): docs/runbooks/privacy-request-runbook.md defining the manual interim process (intake via privacy@hone.care, identity verification, scope, 30-day clock, evidence stored in audit_logs via an admin action) + tighten privacy/t

### HNE-RBAC-001
- **Enabled:** enabled (the coarse model is what is live): studio-scoped RLS + the two-tier owner/practitioner checks run unconditionally in production for all 3 studios; no feature flag. The required explicit-capab
- **Rationale:** Acceptance requires every operation be denied unless the caller's active studio role has an explicit capability for that operation. In code, authorization for the bulk of operations (all client/clinical/session/appointment CRUD) is bare active studio membership via 202 is_studio_member policy usages including 18 FOR ALL 'members all' policies — any active practitioner can read, modify, and (where policies allow) delete any clinical/client record in the studio with no per-operation capability distinction. The owner/practitioner split that covers financial/config surfaces predates the finding an
- **Key evidence:** /Users/chloebaca/Hone/supabase/migrations/0001_init.sql:24 — role text check (role in ('owner','practitioner')): two-tie · /Users/chloebaca/Hone/supabase/migrations/0001_init.sql:153-184 — is_studio_member/is_studio_owner definitions (both req · grep counts across supabase/migrations/*.sql: is_studio_member 202 hits in 40 files; is_studio_owner 45 hits in 9 files; · No capability framework in app code: grep 'capabilit' in lib/+app/ matches only Stripe Connect capability strings (lib/p
- **Remaining:** Define an explicit role→capability map (e.g. clients.read, clients.write, sessions.delete, records.export, payments.charge) and a single aut | Split the 18 FOR ALL 'members all' RLS policies into per-operation policies so destructive operations (DELETE/hard-update) require an explic | Decide and add any needed intermediate roles (the constraint currently forbids anything beyond owner|practitioner) via additive migration | Deny-by-default: new operations must fail closed until a capability is granted, verified by tests that enumerate action↔capability coverage | Document the capability matrix in docs/09_DATABASE_AND_RLS.md and wire it to the capability manifest
- **Proposed PR:** rbac/capability-model phase 1 (no behavior change): introduce lib/authz/capabilities.ts (role→capability map where owner ⊇ practitioner exactly reproduces today's checks) + authorize() helper, and mechanically convert existing role!=='owner' checks a

### HNE-SAA-001
- **Enabled:** Not applicable — no feature flag; the capability does not exist to enable. The live posture is intentionally the opposite (invite-only gate active in production).
- **Rationale:** Acceptance criterion — stranger becomes an active paying studio and leaves without DB/provider/env/code intervention — is not met on any leg. Signup, tenant billing, and offboarding are all absent by design at head 0a5741f; the admin studio wizard is operator-only provisioning (double isAdmin gate + service-role, no RLS INSERT path), so nothing partial toward SELF-service has landed. Expected-OPEN confirmed.
- **Key evidence:** app/(auth)/login/actions.ts:7-78 — invite-only magic link; shouldCreateUser gated on pending_invitations match, fails cl · app/admin/studios/new/actions.ts:27-149 — createStudioWithOwnerInvite operator gate (isAdmin re-check), service-role ins · lib/admin.ts:4-30,69 — isAdmin = ADMIN_EMAILS env allowlist, fail-closed in production · supabase/migrations (0001-0133 full create-table list) — no subscriptions/plans/invoices/tenant-billing tables; studios 
- **Remaining:** Public self-serve signup (account + studio creation without operator wizard) | SaaS subscription billing of studios by Hone (plans, checkout, invoicing, dunning) — no schema or provider integration exists | Self-serve cancellation/offboarding and data disposition (no studio status/deactivate/delete path exists) | Plan enforcement/entitlements (pricing tiers are marketing-only; nothing in code reads a plan)
- **Proposed PR:** Not a single PR — program-level epic requiring product authorization: (1) self-serve signup + studio creation reusing the invitation/handle_new_user machinery, (2) Stripe Billing (subscriptions) schema + webhooks distinct from the existing client-pay

### P1-01
- **Enabled:** enabled — public booking (/book/[slug]) is an always-on unauthenticated surface with no feature flag; the readiness soft-gate only checks active services + open availability, which the live pilot stud
- **Rationale:** Acceptance criteria require that typed email alone never binds a new public booking to an existing clinical identity, plus an OTP/magic-link or reviewed proof model and duplicate review/merge tooling. All three are unmet in the code at prod head: the binding-by-typed-email occurs on both the existing-client path and (via lookup + race fallback) the new-client path; no verification flow or merge tooling exists anywhere in app/ or lib/. The roadmap (SEC-01) lists this as still-to-build work. Secondary observation: EXISTING_CLIENT_NO_MATCH_ERROR vs success on the existing-client path is an active
- **Key evidence:** app/book/[slug]/actions.ts:534-540 — active-client lookup by studio_id + normalized_email, no ownership proof · app/book/[slug]/actions.ts:577,599 — if (existingClient) binds clientId to the matched clinical identity even when clien · app/book/[slug]/actions.ts:592-598 — comment: verified-ownership/claim-or-merge flow 'has NOT been made in this branch' · app/book/[slug]/actions.ts:694-728 — 23505 race fallback binds to the existing active winner row by email
- **Remaining:** Typed email must never bind a public booking to an existing clinical identity (pending-identity model until proven) | OTP/magic-link or reviewed proof of email ownership before binding (or authenticated portal rebooking path) | Reviewed duplicate/merge tooling for practitioner-adjudicated identity claims | Close the existing-client email-existence oracle (no-match error vs success divergence)
- **Proposed PR:** PR: 'Public booking identity proof (SEC-01 phase 1)' — (1) introduce a pending_booking_identities model: existing-client submissions create a pending bind + send a short-lived OTP/magic-link to the matched client email; the appointment binds to the c

### P1-02
- **Enabled:** enabled — the login consent checkbox and the stamping trigger are always-on production behavior; no feature flag exists for any part of this flow.
- **Rationale:** Acceptance criteria require server-enforced versioned acceptance with content hash, actor, timestamp, no fabricating trigger, and re-acceptance on material change. At prod head: enforcement is client-side only and bypassable (OAuth path especially), the fabricating trigger is live in the hosted DB, there is no content hash or acceptance-event record beyond four columns on practitioners, and no re-acceptance mechanism exists (0027's own comment defers it and it was never built). The versioned columns + timestamp that do exist are populated exclusively by the fabrication path, so they cannot cou
- **Key evidence:** app/(auth)/login/page.tsx:26-58,75 — 'agreed' checkbox enforced only in client-side React state (both OAuth and magic-li · app/(auth)/login/actions.ts:35-65 — server action signature takes only email; no consent evidence reaches the server · supabase/migrations/0081_invite_only_handle_new_user.sql:27-47 — current handle_new_user() stamps terms/privacy acceptan · supabase/migrations/0027_terms_acceptance.sql:8-14,31-32 — original fabricating trigger + comment deferring the re-accep
- **Remaining:** Server-enforced acceptance (post-auth acceptance screen or server-validated consent evidence on both magic-link and OAuth paths) | Versioned acceptance records with policy content hash, actor, event ID, timestamp, source, privacy-safe request metadata (acceptance-events  | Remove fabricated acceptance stamping from handle_new_user() (corrective migration) | Re-acceptance gate when terms/privacy change materially (version bump forces re-accept; access gate or banner)
- **Proposed PR:** PR: 'Server-enforced legal acceptance (SEC-02)' — (1) new migration: terms_acceptance_events table (practitioner_id/user_id, doc type, version, content SHA-256, accepted_at, source, event id, privacy-safe request metadata) + rewrite handle_new_user()

### P1-09
- **Enabled:** Enabled — route live on prod head; production processes LIVE setup_intent.succeeded events (mode-matching guard), so a live-card lineage rejection would be silently marked processed.
- **Rationale:** Neither acceptance criterion is met: a terminal lineage rejection creates no actionable reconciliation case (event marked processed, no alert, no surface), and portal success is derived from Stripe confirmSetup, not canonical local state — the client is told the card saved while Hone may hold no row.
- **Key evidence:** app/api/stripe/webhook/route.ts:475-481,492-498,527-533,550-556,604-610,625-631 — all lineage rejections return summarie · app/api/stripe/webhook/route.ts:230-243 — returned summaries are marked processed via mark_stripe_event_processed · app/api/stripe/webhook/route.ts:255-271 — card_on_file_setup_failed critical alert exists ONLY in the catch (thrown) pat · app/portal/PortalPaymentMethodForm.tsx:386-401 + line 60 — success UI ('Card saved…') driven solely by Stripe confirmSet
- **Remaining:** Record a critical/warning ops alert (or dedicated reconciliation case row) for every terminal lineage rejection of setup_intent.succeeded, w | Surface rejected card-save events on an operator surface (e.g. /admin/ops-alerts or a payments manual-review queue entry) | Portal success should confirm (or poll for) the canonical client_payment_methods row before asserting 'Card saved', or clearly present pendi
- **Proposed PR:** Code-only PR: recordOpsAlert (severity critical, event card_on_file_lineage_rejected) in every rejection arm before returning; keep marking processed (idempotency preserved); add the rejected-summary count to the admin payments surface; portal follow

### P1-12
- **Enabled:** disabled/absent — nothing to enable. Meanwhile live legal copy promises the missing machinery: privacy policy §9 (30-day soft-delete then hard-delete, backups purged within 90 days, 7-year billing ret
- **Rationale:** Confirmed fully OPEN on 0a5741f. This is the canonical parent of the data-lifecycle cluster: HNE-EXP-001 is its export child slice (partially built, separately tracked) and HNE-PRV-001 is the sibling process-layer finding. Zero of the listed acceptance criteria (data classes, retention states, hold, verified purge, immutable deletion manifest) are met, so this is OPEN, not PARTIALLY FIXED — the existing soft-delete/archive posture is deliberate indefinite retention, not retention management. The gap is compounded by live legal commitments (30/90-day deletion and purge windows) that no code, cr
- **Key evidence:** Table inventory from supabase/migrations/0001-0133 (create-table scan): no retention, legal_hold, purge, erasure, or del · app/(app)/clients/[id]/actions.ts:269-332 — archiveClientAction: soft-archive only; comment 'Hard delete is intentionall · app/(app)/settings/data/page.tsx:133-138 — 'Delete all studio data' is a disabled 'Coming this week' button · vercel.json — exactly 3 crons (materialize-recurring-breaks, calendar-reconcile, calendar-sync); no purge/retention job
- **Remaining:** Data-class catalog: every table/object/provider data class classified for owner, export, retention, hold, deletion, audit (DATA-01) | Retention states with due dates per data class and jurisdiction, legally reviewed | Legal-hold construct that blocks purge and is auditable | Purge pipeline with attempts, object (storage) deletion, verification, and backup-exception handling | Immutable deletion manifest recording what was destroyed, when, by whom, under what authority
- **Proposed PR:** Not a single-PR fix. Sequence: PR-1 (docs, immediate) — data-class catalog (DATA-01) + align privacy/terms copy with actual behavior or add an interim manual-purge runbook; PR-2 (migration, post-legal-review) — retention_policies + legal_holds + dele
