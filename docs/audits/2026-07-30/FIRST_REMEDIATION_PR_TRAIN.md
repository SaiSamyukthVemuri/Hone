# First remediation PR train — derived from canonical data at `395532489a07defd16d5c3a04ce26d2aedf46096`

**Nothing here is implemented.** Each PR needs its own authorization; every migration needs separate
migration-only authorization.

**How to read this table**

- **Migration** is *derived*: it is true when any finding this PR carries requires one at this PR
  (`migration_required`, or `migration_required_by_pr` for a phased finding). **Rollback** is derived
  the same way — a migration PR is rolled back by a forward fix, never by editing an applied migration.
- **Depends on** is *derived* from canonical `depends_on`, resolved to the PR that actually delivers
  each depended-on capability, plus the two hard ordering edges stated below. `—` means genuinely
  nothing blocks it.
- **Gate effect** says *Closes* only when the PR covers **every** open finding at that gate; otherwise
  *Contributes to*.
- **Production verification** lists **every** carried finding's `acceptance_evidence`, one bullet per
  finding, untruncated — what would prove the fix, not an inventory of tests that already exist.
- **Willow risk** is the carried findings' own `Willow_risk` field (the most severe one), not a
  restatement of the gate. A PR whose finding records a HIGH risk is never shown as low.
- **Scheduling rule enforced here:** a code-only P1 is never packaged with migration work, so no
  code-only P1 can be delayed behind migration-only authorization it does not need.

**Hard ordering constraints**

1. **PR-11 must not be authorized until PR-10 is DEPLOYED** — not merely merged. L18 cannot be solved
   revoke-first: 26 call sites write the clinical tables directly, so revoking first breaks Willow's
   charting at apply time. This edge is in the `Depends on` column and in `pr_dependencies` in the
   machine-readable register, not only in this sentence.
2. **PR-01 precedes only PR-10 and PR-11**, which is where the clinical-DML posture changes. PR-08,
   PR-09 and PR-12 add constraints, triggers or a separate role revoke, not the default-privilege
   sweep, and do **not** depend on it — as the derived `Depends on` column shows.

| PR | Title | Canonical findings | Train | Depends on | Migration | Willow risk | Production verification | Rollback | Gate effect |
|---|---|---|---|---|---|---|---|---|---|
| **PR-01** | Default table-privilege sweep | `L19a` | T1-privilege | — | **yes** | None — nothing reachable uses these. | `L19a` — Fresh-DB privilege matrix proving anon and authenticated hold no TRUNCATE/REFERENCES/TRIGGER on any of the 64 tables, plus a drift test that creates a NEW table inside the test and asserts it does not re-acquire them through ALTER DEFAULT PRIVILEGES. | forward-fix via a new migration | Contributes to BEFORE_THREE_STUDIOS |
| **PR-02** | Truthful public and in-app copy | `F-DATA-001`, `F-RET-001`, `F-DOC-001`, `F-COMP-001`, `N-DOC-001` | T0-copy | — | no | Live at Willow today as a misrepresentation: the export UI invites the owner to 'keep it as a backup' while the export is partial. A Willow owner rel… | `F-DATA-001` — Phase 1: a copy truth-guard asserting the export UI states exactly what the export contains. Phase 2: a test proving a full asynchronous export completes at scale and its manifest matches the delivered contents.<br>`F-RET-001` — Copy component: a truth-guard asserting the privacy and terms pages state only retention behaviour that exists. Implementation component: a test proving a soft-deleted row past its stated window is actually purged, that a legal hold blocks the purge, and that the storage object is removed and not merely stamped.<br>`F-DOC-001` — Source-stated acceptance criteria (Hone_Independent_Audit_2026-07-27.md): Release job fails when migration manifest/deploy config differs from generated docs.<br>`F-COMP-001` — A docs/copy truth-guard asserting that the settings surface and the privacy policy state the SAME hosting region, failing when they diverge.<br>`N-DOC-001` — A docs/copy truth-guard test asserting that app/terms/page.tsx and app/pricing/page.tsx contain no claim of automated subscription billing, Stripe-processed subscription payments, subscription-period refunds or payment-failure suspension while those capabilities do not exist; the guard must fail if the claim is reintroduced. | revert the PR | Contributes to BEFORE_STUDIO_2; Contributes to WILLOW_NOW |
| **PR-03** | Server-authoritative charge amount | `F-PAY-001` | T2-payment | — | no | Live money, today, and NOT resolved by PR #488. A mistyped or off-book amount up to CAD 2,000 still charges a real Willow client's saved card with no… | `F-PAY-001` — Fake-Stripe browser test proving a submitted amount that does not match the booked service's authoritative price is refused server-side; an owner override is recorded with amount AND reason; and one controlled real-charge reconciliation. | revert the PR | Contributes to WILLOW_NOW |
| **PR-04** | Custom-area keystroke commit fix | — | — | — | no | none recorded | — | revert the PR | — |
| **PR-05** | Sentry bearer-path containment | `F-PRIV-001` | T4-privacy | — | no | Live at Willow today. Replayable intake, portal and calendar-feed bearer tokens are transmitted to Sentry on every captured event, so a third-party s… | `F-PRIV-001` — Source-stated acceptance criteria (Hone_Independent_Audit_2026-07-27.md): Sentry scrub tests with realistic opaque tokens in request URL, transaction, span description, breadcrumb and exception context; assert the token never appears in serialized event JSON. | revert the PR | Contributes to WILLOW_NOW |
| **PR-06** | Intake completeness and review-state integrity | `F-CLIN-003`, `F-CLIN-004` | T5-intake | — | no | Live at Willow today, one click, no confirmation. app/(app)/clients/[id]/intake/actions.ts:39-51 updates on .eq("id", intakeId).eq("studio_id", studi… | `F-CLIN-003` — Source-stated acceptance criteria (Hone_Independent_Audit_2026-07-27.md): Two-connection DB test: update client after intake read but before sync; command must preserve the newer value or return a conflict requiring review.<br>`F-CLIN-004` — Fresh-DB test proving 'Mark reviewed' is refused for an intake whose status is in_progress and for an intake belonging to a different client, that the update asserts a row count of exactly 1, and that a genuine submitted intake still reviews successfully. | revert the PR | Contributes to BEFORE_TEN_STUDIOS; Contributes to WILLOW_NOW |
| **PR-07** | Public-booking audit payload minimization | `F-PRIV-002` | T4-privacy | — | no | none recorded | `F-PRIV-002` — Source-stated acceptance criteria (Hone_Independent_Audit_2026-07-27.md): Tests assert raw email, phone and arbitrary notes never appear in audit/log/ops payloads. | revert the PR | Contributes to BEFORE_PUBLIC_SELF_SERVICE |
| **PR-08** | Practitioner attribution and session identity | `F-SEC-001`, `N-SEC-001` | T6-identity | — | **yes** | Willow has real clinical records; false attribution corrupts who-treated-whom history. Willow: 2 practitioners total — 1 ACTIVE owner, 1 INACTIVE non… | `F-SEC-001` — DB tests reject direct client/studio/practitioner re-parenting of a session by an authenticated studio member, including re-pointing any of the five practitioner FKs at another studio's practitioner (N-SEC-001), and prove the approved correction path — soft-delete plus re-chart, per the 0160 product decision — leaves an attributed trail. RESTATED for this baseline: the July-27 criterion additionally required proof 'in draft and finalized states' and an 'approved reassignment command'; finalized states are permanently retired (0159) and 0160:26-32 records that the reassignment command was deliberately not built. The source text is preserved verbatim in the source_test_limitations column of this finding's July-27 row in MASTER_FINDINGS_REGISTER.csv.<br>`N-SEC-001` — Fresh-DB test proving an authenticated member of studio A cannot set sessions.practitioner_id or performed_by_practitioner_id to a practitioner belonging to studio B — by composite same-studio FK or by trigger — and that the same-studio case still succeeds. | forward-fix via a new migration | Contributes to BEFORE_TEN_STUDIOS; Contributes to BEFORE_STUDIO_2 |
| **PR-09** | Session link same-client validation | `L19b` | T6-identity | — | **yes** | Low. | `L19b` — DB test proving a same-studio PATCH that re-points sessions.appointment_id or sessions.treatment_plan_id at another CLIENT's appointment or plan is rejected by the validating trigger, that legitimate same-client re-linking still succeeds, and that the trigger fires for every role reachable through PostgREST. | forward-fix via a new migration | Contributes to BEFORE_THREE_STUDIOS |
| **PR-10** | Clinical writes onto narrow commands (application half) | `L18` | T7-clinical-dml | PR-01 | **yes** | Live at Willow today. authenticated holds DELETE on sessions and session_blocks, so any studio member can permanently destroy a clinical record throu… | `L18` — Fresh-DB proof that authenticated holds no INSERT/UPDATE/DELETE on the 5 clinical tables, AND a deployed-build charting E2E proving every one of the 26 former direct writers still succeeds through its narrow command. The deploy proof must precede the revoke. | forward-fix via a new migration | Contributes to BEFORE_STUDIO_2 |
| **PR-11** | Revoke obsolete clinical DML (revoke half) | `L18` | T7-clinical-dml | PR-01, PR-10 deployed | **yes** | Live at Willow today. authenticated holds DELETE on sessions and session_blocks, so any studio member can permanently destroy a clinical record throu… | `L18` — Fresh-DB proof that authenticated holds no INSERT/UPDATE/DELETE on the 5 clinical tables, AND a deployed-build charting E2E proving every one of the 26 former direct writers still succeeds through its narrow command. The deploy proof must precede the revoke. | forward-fix via a new migration | Contributes to BEFORE_STUDIO_2 |
| **PR-12** | service_role trigger and replication-role hardening | `L20` | T1-privilege | — | **yes** | None. | `L20` — Fresh-DB proof that service_role holds no TRIGGER privilege on the guarded clinical tables and cannot set session_replication_role, so the 0160 lineage triggers cannot be disabled by any role reachable through PostgREST. | forward-fix via a new migration | Contributes to BEFORE_THREE_STUDIOS |
| **PR-13** | Checkout default and note regression reproduction | — | — | — | no | none recorded | — | revert the PR | — |
| **PR-14** | Atomic service reordering | `CHLOE-003` | T3-charting | — | no | Reported by Willow's owner against the live build. | `CHLOE-003` — Reorder N services to an arbitrary target order and assert the persisted order matches exactly; concurrent reorder does not interleave. | revert the PR | Contributes to BEFORE_STUDIO_2 |
| **PR-15** | Dashboard truncation and service-card legibility | `CHLOE-004`, `CHLOE-005` | T3-charting | — | no | Reported by Willow's owner against the live build. | `CHLOE-004` — Assert the full remember-note and latest-settings content is reachable (expand/wrap/tooltip) at the mobile breakpoint the studio actually uses.<br>`CHLOE-005` — Spacing and palette reviewed at the real device breakpoint; colour pairs meet a stated contrast/differentiation bar. | revert the PR | Contributes to WILLOW_NOW; Contributes to BEFORE_STUDIO_2 |
| **PR-16** | Appointment and schedule command boundaries | `F-SEC-002`, `F-SCHED-001` | T8-schedule | — | **yes** | Live at Willow today. Any authenticated studio member holds direct appointment DML through PostgREST, so the owner gates, duration authority, booking… | `F-SEC-002` — Source-stated acceptance criteria (Hone_Independent_Audit_2026-07-27.md): Direct DML negative tests for every restricted field/status transition; command tests assert locks, authority, audit and one-row effects.<br>`F-SCHED-001` — Source-stated acceptance criteria (Hone_Independent_Audit_2026-07-27.md): DB tests assert members cannot modify another practitioner/studio-wide settings; approved commands serialize with public/internal booking. | forward-fix via a new migration | Contributes to BEFORE_STUDIO_2; Contributes to BEFORE_THREE_STUDIOS |
| **PR-17** | Public booking atomicity and kill switches | `F-SCHED-002`, `F-SCHED-003`, `F-PUBLIC-001`, `F-PUBLIC-002` | T9-public | PR-16 | **yes** | none recorded | `F-SCHED-002` — Source-stated acceptance criteria (Hone_Independent_Audit_2026-07-27.md): DB/browser test toggles the kill switch between slot display and submit; submission must fail with a safe unavailable message and create no client/appointment side effects.<br>`F-SCHED-003` — Source-stated acceptance criteria (Hone_Independent_Audit_2026-07-27.md): Two-connection tests for public booking versus override, blockout, service archive, practitioner deactivation, internal booking and kill-switch toggle.<br>`F-PUBLIC-001` — Source-stated acceptance criteria (Hone_Independent_Audit_2026-07-27.md): Fault-injection and race tests assert zero durable side effects on every rejected submit.<br>`F-PUBLIC-002` — Source-stated acceptance criteria (Hone_Independent_Audit_2026-07-27.md): Browser tests for known-email impersonation, repeated nuisance submissions, rate-limiter outage and confirmation expiry. | forward-fix via a new migration | Contributes to BEFORE_THREE_STUDIOS; Contributes to BEFORE_TEN_STUDIOS; Contributes to BEFORE_PUBLIC_SELF_SERVICE |
| **PR-18** | Timezone and slot correctness | `F-SCHED-004`, `F-SCHED-006`, `F-SCALE-002` | T9-public | — | no | none recorded | `F-SCHED-004` — Source-stated acceptance criteria (Hone_Independent_Audit_2026-07-27.md): Unit and browser tests for Toronto/New York spring-forward and fall-back, midnight boundaries, long services and Google/notification rendering.<br>`F-SCHED-006` — Source-stated acceptance criteria (Hone_Independent_Audit_2026-07-27.md): Unit tests inject each query error; public submit must revalidate and create no booking; UI distinguishes “no availability” from system failure.<br>`F-SCALE-002` — Source-stated acceptance criteria (Hone_Independent_Audit_2026-07-27.md): Benchmark sparse/dense calendars at 10/50/100 studios and assert maximum query/time budget. | revert the PR | Contributes to BEFORE_TEN_STUDIOS |
| **PR-19** | Public create-command eligibility and allocation | `F-SCHED-005` | T9-public | PR-17 | **yes** | none recorded | `F-SCHED-005` — Source-stated acceptance criteria (Hone_Independent_Audit_2026-07-27.md): Browser/DB tests for chosen practitioner, no preference, parallel practitioners, deactivation, reassignment and cross-studio practitioner substitution. | forward-fix via a new migration | Contributes to BEFORE_THREE_STUDIOS |
| **PR-20** | Import atomicity | `F-IMPORT-001` | T10-data | — | **yes** | Not exercised at Willow yet (no import has been run), but the path is live: a partial import leaves the studio with half-loaded client records and no… | `F-IMPORT-001` — Source-stated acceptance criteria (Hone_Independent_Audit_2026-07-27.md): Fault-injection DB tests at every phase; retry same batch must converge to one correct result; cross-tenant IDs and spreadsheet formula payloads must fail safely. | forward-fix via a new migration | Contributes to BEFORE_STUDIO_2 |
| **PR-21** | Retention, purge and storage lifecycle | `F-RET-001`, `F-STORAGE-001` | T0-copy, T10-data | — | **yes** | Live at Willow today. app/privacy/page.tsx:353-361 and app/terms/page.tsx:484-495 commit to a 30-day deletion and 90-day backup purge; no purge job, … | `F-RET-001` — Copy component: a truth-guard asserting the privacy and terms pages state only retention behaviour that exists. Implementation component: a test proving a soft-deleted row past its stated window is actually purged, that a legal hold blocks the purge, and that the storage object is removed and not merely stamped.<br>`F-STORAGE-001` — Source-stated acceptance criteria (Hone_Independent_Audit_2026-07-27.md): Fault-injection tests for every failure point and reconciler convergence without cross-tenant deletion. | forward-fix via a new migration | Contributes to WILLOW_NOW; Contributes to BEFORE_TEN_STUDIOS |
| **PR-22** | Complete asynchronous export | `F-DATA-001`, `F-DATA-002`, `F-SCALE-001` | T0-copy, T10-data | PR-21 | **yes** | Live at Willow today as a misrepresentation: the export UI invites the owner to 'keep it as a backup' while the export is partial. A Willow owner rel… | `F-DATA-001` — Phase 1: a copy truth-guard asserting the export UI states exactly what the export contains. Phase 2: a test proving a full asynchronous export completes at scale and its manifest matches the delivered contents.<br>`F-DATA-002` — Source-stated acceptance criteria (Hone_Independent_Audit_2026-07-27.md): Test manifest, archive entries and audit payload are exactly equal.<br>`F-SCALE-001` — Source-stated acceptance criteria (Hone_Independent_Audit_2026-07-27.md): Load test at projected 100k clients/1m records and large image corpus; enforce quotas and cancellation. | forward-fix via a new migration | Contributes to BEFORE_STUDIO_2; Contributes to BEFORE_TEN_STUDIOS |
| **PR-23** | Offboarding and studio-deletion workflow | `F-OFF-001` | T10-data | PR-21, PR-22 | **yes** | none recorded | `F-OFF-001` — Source-stated acceptance criteria (Hone_Independent_Audit_2026-07-27.md): End-to-end sandbox offboarding test including retry/rollback and post-delete access denial. | forward-fix via a new migration | Contributes to BEFORE_TEN_STUDIOS |
| **PR-24** | Public rate-limiter fail-open disposition | `F-OPS-001` | T11-ops | — | no | none recorded | `F-OPS-001` — Source-stated acceptance criteria (Hone_Independent_Audit_2026-07-27.md): Fault-injection load tests with limiter unavailable; ensure mutation throughput remains bounded and legitimate read traffic has a controlled degraded path. | revert the PR | Contributes to BEFORE_PUBLIC_SELF_SERVICE |
| **PR-25** | Ops alerting and test assurance | `F-OPS-003`, `F-TEST-003` | T11-ops | — | no | none recorded | `F-OPS-003` — Source-stated acceptance criteria (Hone_Independent_Audit_2026-07-27.md): Staging job test plus production no-op heartbeat verification; simulate missed schedule and recovery without duplicates.<br>`F-TEST-003` — Acceptance checklist covering copy, charting and payment on the deployed build. RESTATED for this baseline: the July-27 checklist also named 'finalization/correction', which is permanently retired (0159) and must not be reinstated as a test target. The source text is preserved verbatim in the source_test_limitations column of this finding's July-27 row in MASTER_FINDINGS_REGISTER.csv. | revert the PR | Contributes to BEFORE_STUDIO_2; Contributes to WILLOW_NOW |
| **PR-26** | Backup/restore proof and staging parity | `F-OPS-004`, `F-STAGE-001` | T11-ops | — | no | none recorded | `F-OPS-004` — Source-stated acceptance criteria (Hone_Independent_Audit_2026-07-27.md): Restore a sanitized environment from backup, verify tenant/data/object counts and run core workflows; record signed results.<br>`F-STAGE-001` — Rehearse a schema migration and a restore on staging against a production-sized dataset and record the result. RESTATED for this baseline: the July-27 criterion named a 'P0 snapshot migration'; the snapshot v2 direction is retired and no such migration will exist. The source text is preserved verbatim in the source_test_limitations column of this finding's July-27 row in MASTER_FINDINGS_REGISTER.csv. | revert the PR | Contributes to WILLOW_NOW; Contributes to BEFORE_TEN_STUDIOS |
| **PR-27** | Whole-session-copy provenance durability | `F-COPY-001` | T10-data | PR-21 | **yes** | none recorded | `F-COPY-001` — Source-stated acceptance criteria (Hone_Independent_Audit_2026-07-27.md): DB test deletes/archives source/target and verifies provenance remains interpretable. | forward-fix via a new migration | Contributes to BEFORE_TEN_STUDIOS |

## PR scope detail

- **PR-01** — Revoke TRUNCATE/REFERENCES/TRIGGER from anon and authenticated on the 64 exposed tables, and change ALTER DEFAULT PRIVILEGES so new tables never re-acquire them. Add an ACL drift test.
- **PR-02** — One code-only PR correcting every live false published claim together: the terms and pricing pages' subscription/payment/refund lifecycle, the settings-vs-privacy hosting-region contradiction, the export UI's contents claim, and the published retention/deletion commitment wording.
- **PR-03** — Compare the submitted amount server-side against the authoritative booked-service or custom price; owner-only override recorded with amount and reason.
- **PR-04** — Commit a custom treatment area once on confirm/blur instead of once per keystroke.
- **PR-05** — Stop transmitting replayable intake, portal and calendar-feed bearer tokens to Sentry.
- **PR-06** — Refuse 'Mark reviewed' for an unsubmitted intake or an intake belonging to another client; assert an exact affected-row count; fix intake merge state.
- **PR-07** — Stop duplicating the raw client email and free-text booking notes into appointment_audit.details; store minimized metadata instead.
- **PR-08** — Composite same-studio FK and/or guard on the five practitioner FKs of sessions; closes the residual half of F-SEC-001.
- **PR-09** — Additive validating trigger so sessions.appointment_id and sessions.treatment_plan_id must belong to the SAME client, not merely the same studio.
- **PR-10** — Move the 26 direct writers onto reviewed commands. NO grant change in this PR.
- **PR-11** — Revoke INSERT/UPDATE/DELETE from authenticated on the 5 clinical tables. MUST NOT be authorized until PR-10 is DEPLOYED.
- **PR-12** — Revoke TRIGGER from service_role on the guarded clinical tables and remove the ability to set session_replication_role, so the 0160 lineage triggers cannot be disabled.
- **PR-13** — Reproduce on the deployed build first, then fix and pin.
- **PR-14** — Single atomic reorder over the full ordered set.
- **PR-15** — Expand or wrap the memory note and settings summary; spacing and palette separation.
- **PR-16** — Route appointment and schedule-config mutations through audited commands with ownership enforcement.
- **PR-17** — Booking atomicity, kill switches and public-surface hardening.
- **PR-18** — DST-correct slot generation and bounded next-available scanning.
- **PR-19** — Eligibility and allocation under lock for the public create command.
- **PR-20** — All-or-nothing import with safe retry via a transactional RPC.
- **PR-21** — Implement the published retention commitments: purge job, hard-delete path, legal hold, storage-object removal and a bucket reconciler.
- **PR-22** — Full asynchronous export at scale with a manifest-derived audit list.
- **PR-23** — Lifecycle state, offboarding-run ledger, hold flags and a rehearsed teardown.
- **PR-24** — Explicit decision and fix for the fail-open path during a live Upstash outage.
- **PR-25** — Close the WILLOW_NOW alerting and test-quality gaps.
- **PR-26** — Prove restore works; staging parity.
- **PR-27** — Change the provenance ledger's two session FKs off ON DELETE CASCADE so the copy record survives deletion of either session.

## Recommended first authorization

**PR-02 — truthful public and in-app copy.**

The derived facts, not a superlative: **5 open P1s** at WILLOW_NOW or BEFORE_STUDIO_2
need no migration — `F-CLIN-004`, `F-PAY-001`, `F-PRIV-001`, `F-COMP-001`, `N-DOC-001` — and the PRs whose whole
scope is code-only are **PR-02**, **PR-03**, **PR-05**, **PR-06**, **PR-07**, **PR-14**, **PR-15**, **PR-18**, **PR-24**, **PR-25**, **PR-26**. PR-02 is not the only code-only
option; it is the recommended **first** one for three reasons a reader can check against the register:

1. It is the only PR that corrects statements Hone publishes to the public and to a Canadian health
   practitioner — a terms and pricing page describing a subscription, payment-processing and refund
   lifecycle that does not exist, a settings page contradicting the privacy policy about where health
   data is hosted, an export UI overstating what it exports, and a retention commitment with no
   implementing code. Every one of those is false **today**, on a live page, with a real audience.
2. It carries **no migration, no flag and no data operation**, so it needs no migration-only
   authorization and is reverted by reverting the PR.
3. Its `Depends on` is genuinely `—`.

**PR-03** (server-authoritative charge amount) is the strongest companion: it is the only P1 touching
live money, and `F-PAY-001`'s amount half is live at Willow **today** regardless of practitioner count.
**PR-04**, **PR-05** and **PR-06** each carry a WILLOW_NOW P1, need no migration, and have no
dependencies — any of them may run alongside PR-02. **PR-01** also has no dependency, but it is the
prerequisite for the privilege trains (PR-10 → PR-11, PR-12) and is a migration PR, so it needs its own
migration-only authorization.
