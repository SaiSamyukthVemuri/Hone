# Current P0 and P1 findings — exact production `395532489a07defd16d5c3a04ce26d2aedf46096`, migration max 0160

Corrected pass. Verified against the exact source and read-only hosted evidence on 2026-07-30.

**Authoritative Willow fact used throughout:** Willow: 2 practitioners total — 1 ACTIVE owner, 1 INACTIVE non-owner. Active non-owner practitioners: 0. Read-only aggregate (roles/active/counts only), 2026-07-30.

## Current P0 findings

**ZERO current P0 confirmed.** `F-CLIN-000` (the only source P0) is P3: its reachable half was closed
by migration 0159 §5c, and its remaining half rests on the permanently retired signed-record premise.

## Current P1 findings (12)

| ID | Title | Exposure | Gate | Train | PR | Dependency |
|---|---|---|---|---|---|---|
| `F-CLIN-004` | "Mark reviewed" accepts an unsubmitted (in_progress) intake and any intake in the studio — one UI click creates a false clinical-review signal and pe… | REACHABLE_IN_PRODUCTION | WILLOW_NOW | T5-intake | PR-06 | Self-contained; nothing blocks it. Minimal correct fix, entirely inside app/(app)/clients/[id]/intake/actions.ts:41-52: add `.eq('status','… |
| `F-SEC-002` | Any authenticated studio member can create, retime, re-status or delete appointments by direct PostgREST DML, bypassing the owner gates, duration aut… | REACHABLE_IN_PRODUCTION | BEFORE_STUDIO_2 | T8-schedule | PR-16 | Independent of L18 and of the clinical work. Two coupled steps: (1) revoke insert, update, delete on public.appointments from anon, authent… |
| `F-PAY-001` | Session-payment amount is browser-supplied and unbounded up to CAD 2,000; any active practitioner (not just the owner) can prepare and execute the ch… | REACHABLE_IN_PRODUCTION | WILLOW_NOW | T2-payment | PR-03 | Independent of L18 and of the privilege sweep. Code-only. |
| `F-PRIV-001` | Sentry receives raw bearer credentials embedded in URL path segments (intake, portal, appointment-manage and calendar-feed tokens) | REACHABLE_IN_PRODUCTION | WILLOW_NOW | T4-privacy | PR-05 | None — fully self-contained and code-only, confined to lib/observability/sentry-scrub.ts. Three changes: (1) in scrubRequest, canonicalize … |
| `F-DATA-001` | Owner ZIP export omits most tenant-owned data (intake, consent, clinical notes, images, payments, portal, numbing/inventory fields) while the UI pres… | REACHABLE_IN_PRODUCTION | BEFORE_STUDIO_2 | T0-copy | PR-02, PR-22 | Two-phase. Phase 1 (no migration, days): correct app/(app)/settings/data/page.tsx to say "partial export", replace the "Not included" parag… |
| `F-IMPORT-001` | Quick Import commits clients and treatment memories in separate statements; a memory-insert failure strands clients permanently with no idempotent re… | REACHABLE_IN_PRODUCTION | BEFORE_STUDIO_2 | T10-data | PR-20 | Needs a transactional import RPC (SECURITY DEFINER, owner-verified, taking the planned clients + memories as one JSON payload and inserting… |
| `F-RET-001` | Published 30-day / 90-day retention and deletion commitments have no implementing code: no purge job, no hard-delete path, no legal hold, no cross-sy… | REACHABLE_IN_PRODUCTION | WILLOW_NOW | T0-copy | PR-02, PR-21 | Sequenced after F-DATA-001 (a complete export must exist before anything is deleted) and jointly with F-OFF-001 (offboarding is the process… |
| `F-COMP-001` | The in-app Data settings page tells studio owners their clinical data is hosted in Canada while the privacy policy states it is in AWS US-East-1 | REACHABLE_IN_PRODUCTION | WILLOW_NOW | T0-copy | PR-02 | Independent of all engineering findings. Shares a root cause with F-DOC-001 — no gated claims manifest — but must not wait for that control… |
| `N-SEC-001` | Session practitioner attribution can be re-pointed, including to another studio's practitioner | REACHABLE_IN_PRODUCTION | BEFORE_STUDIO_2 | T6-identity | PR-08 | Additive composite same-studio FK or validating trigger on the five practitioner FKs of sessions. One call site DOES write one of these col… |
| `N-DOC-001` | Public terms and pricing pages claim a subscription, payment-processing and refund lifecycle the product does not have | REACHABLE_IN_PRODUCTION | WILLOW_NOW | T0-copy | PR-02 | None. Copy-only. |
| `L18` | authenticated holds direct row DML on five clinical tables | REACHABLE_IN_PRODUCTION | BEFORE_STUDIO_2 | T7-clinical-dml | PR-10, PR-11 | MUST be application-first: move the 26 callers onto narrow commands and DEPLOY before revoking. Revoke-first breaks live charting at apply … |
| `CHLOE-001` | Typing a custom treatment area commits one partial area row per keystroke | NOT_REACHABLE_IN_PRODUCTION | NONE_SHIPPED | NONE | — | None. Shipped. |

### Evidence per P1

#### `F-CLIN-004` — "Mark reviewed" accepts an unsubmitted (in_progress) intake and any intake in the studio — one UI click creates a false clinical-review signal and permanently locks the client out of finishing their intake

- **Status** OPEN · **Gate** WILLOW_NOW · **Reachable** true · **Train** T5-intake · **PR** PR-06
- **Source evidence:** app/(app)/clients/[id]/intake/actions.ts:41-52 — `supabase.from('client_intake_forms').update({ status:'reviewed', reviewed_at, reviewed_by, practitioner_notes }).eq('id', intakeId).eq('studio_id', studio.id).is('deleted_at', null)`: no `.eq('status','submitted')` predicate, no `.eq('client_id', clientId)` predicate (clientId is accepted at :26-28 and used only for revalidatePath at :54-55), no `.select()` and therefore no affected-row check — a zero-row update returns `{ ok: true }`. app/(app)/clients/[id]/intake/page.tsx:336-341 renders `<IntakeReviewForm ... alreadyReviewed={intake.status === 'reviewed'} />` UNCONDITIONALLY, including when the branch at :281-286 has just printed "The client has not submitted their intake yet". app/(app)/clients/[id]/intake/IntakeReviewForm.tsx:76-85 shows the "Mark reviewed" button whenever `!alreadyR…
- **Hosted evidence:** The supplied hosted facts are silent on client_intake_forms: they cover the 0159/0160 clinical retirement objects, the 86-table TRUNCATE/DML ACL survey of clinical tables, NOLOGIN role reality and the SECURITY DEFINER inventory. No count of in_progress-marked-reviewed rows in production was supplied, and I did not query production. The relevant hosted fact is negative: nothing in the retirement or the lineage triggers touches this table or this transition, so 0159 and 0160 neither fix nor affect it. The general fact that `authenticated` retains broad row DML is consistent with the 0087 studio-membership UPDATE policy still being what authori…
- **Why P1:** This one is real, unmitigated, and reachable through the ordinary deployed UI — not merely through a crafted request. The review page renders the answers-so-far of an unsubmitted intake along with an amber "the client has not submitted yet" notice, and directly beneath it renders a live "Mark reviewed" button; one click writes status='reviewed', reviewed_at and reviewed_by. Migration 0118 does not catch it because its whole body is conditioned on the OLD status already being terminal, so the in_progress -> reviewed edge is the one transition nobody guards. The consequences are worse than the audit stated. First, a false clinical signal: lib/intake/queries.ts selects `in ('submitted','reviewed')` as the latest intake of record, so partial a…
- **Willow risk:** Live at Willow today, one click, no confirmation. app/(app)/clients/[id]/intake/actions.ts:39-51 updates on .eq("id", intakeId).eq("studio_id", studio.id).is("deleted_at", null) with no submitted-status predicate, no client_id predicate and no row-count check, so every path reports "Marked reviewed."
- **Missing evidence:** I did not query production, so I cannot say whether any live intake row is currently in status='reviewed' with submitted_at IS NULL — that single read-only query (`select count(*) from public.client_intake_forms where status='reviewed' and submitted_at is null and deleted_at is null`, grouped by studio) is the decisive evidence for whether this ha…

#### `F-SEC-002` — Any authenticated studio member can create, retime, re-status or delete appointments by direct PostgREST DML, bypassing the owner gates, duration authority, booking kill switch, buffer rule and every audit row of create_internal_appointment_v2 / move_or_reassign_appointment

- **Status** OPEN · **Gate** BEFORE_STUDIO_2 · **Reachable** true · **Train** T8-schedule · **PR** PR-16
- **Source evidence:** supabase/migrations/0010_booking_v1.sql:241 (alter table public.appointments enable row level security), :271-276 (policy "appointments_member_all" FOR ALL using/with check is_studio_member(studio_id) — the only appointments policy in the whole tree; grep of every migration for 'create policy'/'drop policy' on public.appointments returns exactly these two lines), :217-225 (appointment_audit.appointment_id references public.appointments(id) ON DELETE CASCADE). No table-level revoke exists for appointments anywhere in supabase/migrations (a grep for table-level revoke returns 0092/0111/0113/0115/0119/0122/0126 etc., never appointments or any schedule table), so the Supabase default privileges leave authenticated with INSERT/UPDATE/DELETE. What the command path enforces that direct DML skips: supabase/migrations/0152_actual_overlap_hard_buf…
- **Hosted evidence:** Authoritative hosted facts supplied: 'of 86 public tables, 64 grant TRUNCATE to BOTH anon and authenticated (Supabase ALTER DEFAULT PRIVILEGES). The 9 clinical tables 0159 covered are exceptions.' appointments is not one of the 9 covered tables and has no revoke in source, so it is inside the 64 — i.e. the default ALL grant (which carries INSERT/UPDATE/DELETE alongside TRUNCATE) is intact for authenticated. The same facts confirm the reachability boundary that matters here: PostgREST exposes SELECT/INSERT/UPDATE/DELETE and RPC (it only withholds TRUNCATE), and anon/authenticated/service_role are NOLOGIN so only authenticator can connect — wh…
- **Why P1:** This is a live authorization-boundary defect, not a missing feature, and it is reachable today by a real browser user. A logged-in studio member holds the anon key and their own JWT, and appointments carries a single FOR ALL membership policy with the Supabase default ALL grant to authenticated. A raw PostgREST POST/PATCH/DELETE therefore reaches the table directly and skips, in one request, every control the 0142→0152 command series was built to impose: the practitioner_capacity_booking_enabled kill switch, the owner-only gate on retargeting another practitioner, the owner-only duration override, the authoritative duration derived from a locked service row, the future-time check, validate_appointment_availability, and the mandatory appoin…
- **Willow risk:** Live at Willow today. Any authenticated studio member holds direct appointment DML through PostgREST, so the owner gates, duration authority, booking kill switch and buffer rule can all be bypassed and no audit row is written. Bounded in ACTOR terms only — Willow has 0 active non-owner practitioners — not in effect: the owner can bypass her own invariants and the audit cascade is missing either w…
- **Missing evidence:** Willow has 2 practitioner rows: 1 ACTIVE owner and 1 INACTIVE non-owner. Active non-owner practitioners: 0 (hosted-verified read-only aggregate, 2026-07-30). The member-versus-owner half of this bypass therefore has no distinct actor at Willow today; the owner-versus-her-own-invariants half and the audit-cascade half are unaffected and live. What …

#### `F-PAY-001` — Session-payment amount is browser-supplied and unbounded up to CAD 2,000; any active practitioner (not just the owner) can prepare and execute the charge

- **Status** OPEN · **Gate** WILLOW_NOW · **Reachable** true · **Train** T2-payment · **PR** PR-03
- **Source evidence:** UNCHANGED at 395532489a07defd16d5c3a04ce26d2aedf46096: app/(app)/clients/[id]/sessions/[sessionId]/payment-actions.ts is byte-identical between the audit baseline and the current production head — PR #485 and PR #488 do not touch it. amount_dollars is still read straight from the submitted form (:147), validated only by parseAmountCents (:112-125) as non-empty, finite, greater than zero and at most SESSION_PAYMENT_AMOUNT_CEILING_CENTS, and written as amount_cents (:201). There is no comparison anywhere in the file against the booked service's price, a custom client price, or any other authoritative figure. Neither prepareSessionPaymentChargeAction (:127) nor executeSessionPaymentChargeAction (:292) checks practitioner.role; that check exists only in the refund path (:546).
- **Hosted evidence:** Re-checked against 395532489a07defd16d5c3a04ce26d2aedf46096 for this reconciliation. PR #488 repaired how the amount field is POPULATED — a broken PostgREST embed alias meant the booked-service price never loaded, so the field arrived blank. It did not add, and did not attempt to add, any server-side check that the amount actually submitted matches that price. EDITABLE AUTO-POPULATION IS NOT SERVER-AUTHORITATIVE AMOUNT ENFORCEMENT: the field is a default, the practitioner can overwrite it with any value up to the CAD 2,000 ceiling, and the server accepts whatever arrives. The defect F-PAY-001 describes is untouched.
- **Why P1:** Gate is WILLOW_NOW. amount_dollars is taken from the submitted form (payment-actions.ts:147) and validated only as >0 and <=200000 cents (parseAmountCents :112-125) before being written as amount_cents (:201). No server-side comparison against the booked service's authoritative price exists. STATUS CORRECTION at the 395532489a07defd16d5c3a04ce26d2aedf46096 baseline: previously PARTIALLY_FIXED, which rested on display-side work only. PR #488 repaired how the field is POPULATED (a 0151 embed-alias regression that made the default fail silently) and added a message for the no-price case. Both are display and default resolution. EDITABLE AUTO-POPULATION IS NOT SERVER-AUTHORITATIVE AMOUNT ENFORCEMENT — the practitioner can still overwrite the d…
- **Willow risk:** Live money, today, and NOT resolved by PR #488. A mistyped or off-book amount up to CAD 2,000 still charges a real Willow client's saved card with no server-side comparison against the booked service. #488 makes the correct figure appear in the box; it does not make the server require it.
- **Missing evidence:** Willow has 2 practitioner rows: 1 ACTIVE owner and 1 INACTIVE non-owner; active non-owner practitioners: 0 (hosted-verified, 2026-07-30). That bounds the AUTHORIZATION half only. What remains genuinely unproven: the 6 live charge amounts were not compared against the service-menu prices — that requires a production query and was not authorized. PR…

#### `F-PRIV-001` — Sentry receives raw bearer credentials embedded in URL path segments (intake, portal, appointment-manage and calendar-feed tokens)

- **Status** OPEN · **Gate** WILLOW_NOW · **Reachable** true · **Train** T4-privacy · **PR** PR-05
- **Source evidence:** lib/observability/sentry-scrub.ts:183-199 scrubRequest — deletes cookies, headers and query_string, but line 189 does request.url = redactString(request.url.split("?")[0]), i.e. the PATH is deliberately preserved and only value-pattern scrubbed. lib/observability/sentry-scrub.ts:91-98 the value patterns: JWT_RE = /\beyJ[a-zA-Z0-9_-]+\.[a-zA-Z0-9_-]+\.[a-zA-Z0-9_-]+/g requires THREE dot-separated segments; BEARER_RE requires a literal 'Bearer ' prefix; SUPABASE_TOKEN_RE matches only sb-*-auth-token / sbp_ / service_role. lib/observability/sentry-scrub.ts:235-291 scrubEventCommon never touches event.transaction (the transaction NAME is not scrubbed at all). Token routes that put a credential in the path: app/intake/[token], app/manage/[token], app/cancel/[token], app/reschedule/[token], app/portal/verify/[token], app/calendar-feed/[token].…
- **Hosted evidence:** Sentry is live in production — it was merged and deployed at ed23a7a with no env kill switch in the config (the DSN is a hardcoded literal, not an env-conditional), so every production deployment initializes it. Migrations 0159 and 0160 are clinical-lineage work and touch nothing in the observability path; git log shows no commit touching lib/observability/sentry-scrub.ts after the original hardening, so the exact-head file is the deployed file. The NOLOGIN/PostgREST reachability facts do not mitigate this at all — the leak path is HTTPS egress from the Next.js process to a third-party SaaS, not a database grant.
- **Why P1:** CONFIRMED precisely, and the audit's mechanism holds up under direct inspection with one detail it did not have: I can show exactly WHY the regexes miss. Hone's intake and appointment tokens are two-segment HMAC tokens whose payload base64 begins 'eyJ', which looks like a JWT but has only one dot — JWT_RE demands two dots, so the token is emitted verbatim. The calendar-feed token is a dot-free 32-byte base64url random string matching none of the five value patterns. scrubRequest explicitly keeps the path (line 189 splits on '?' and keeps the left side), and event.transaction is never scrubbed anywhere in the file. So an exception, a fetch/xhr breadcrumb, or one of the 10% sampled production transactions on any of six route families ships a…
- **Willow risk:** Live at Willow today. Replayable intake, portal and calendar-feed bearer tokens are transmitted to Sentry on every captured event, so a third-party service holds credentials that grant access to a real client's clinical surfaces. Code-only to fix.
- **Missing evidence:** I cannot prove from source whether any such event has ALREADY been transmitted to Sentry — that requires querying the Sentry project's stored events (search by route pattern, never exporting raw URLs). I also cannot confirm from the repository whether Sentry's data region is US or EU, or the project's retention window and member list, all of which…

#### `F-DATA-001` — Owner ZIP export omits most tenant-owned data (intake, consent, clinical notes, images, payments, portal, numbing/inventory fields) while the UI presents it as a complete backup

- **Status** OPEN · **Gate** BEFORE_STUDIO_2 · **Reachable** true · **Train** T0-copy · **PR** PR-02, PR-22
- **Source evidence:** app/(app)/settings/data/actions.ts:30-41 (owner gate); :47-198 (the ONLY 16 reads: clients, sessions, electrolysis_entries, laser_entries, practitioners, client_pricing, session_blocks[13 cols], appointments, treatment_plans, treatment_plan_stages, services[id,name], practitioners[id,name], 4x record_keeping_*); :334-339 (session_block_areas read, used ONLY to build a merged label string, never its own file); :162 comment "No image binaries / storage paths / payment tables here."; :267-777 (exactly 13 CSVs + README.txt written to the zip); :747-775 README "This export contains all client records, sessions, entries, appointments, and treatment plans for your studio" and "If you ever leave Hone, your records leave with you". app/(app)/settings/data/page.tsx:58-63 "You can export everything at any time"; :66-69 "Download a ZIP of your studi…
- **Hosted evidence:** Hosted facts supplied: migration max 0160; live payments ARE on at Willow (6 live charges) — so real payment records exist in production that this export cannot produce. Willow has ZERO non-draft sessions and there is exactly ONE legacy finalized session + ONE snapshot in a NON-Willow test studio, so the omitted clinical_record_snapshots content is currently 1 row outside Willow. Copy ledger (session_copy_operations) 0 rows; GCal tables 1 row. 0 clinical_audit_events, 0 amendments. Willow's actual intake/consent/image/portal row counts were NOT re-queried today.
- **Why P1:** Confirmed exactly as written, and worse than described. The action reads 16 datasets and writes 13 CSVs; roughly 60 tenant-owned tables plus the entire private treatment-image bucket are absent. Two later chart fields that Chloe uses daily are silently absent even from the rows that ARE exported: session_blocks.numbing_notes/numbing_status (0156) and probe_inventory_item_id (0155) are not in the select list at actions.ts:109-114. The severity driver is not the missing capability — it is the live in-product representation: the page says "You can export everything at any time" and "Exportable any time", explicitly recommends the ZIP as the pilot backup, and its "Not included" section names ONLY private warnings/personal notes, which reads as…
- **Willow risk:** Live at Willow today as a misrepresentation: the export UI invites the owner to 'keep it as a backup' while the export is partial. A Willow owner relying on it as a backup would discover the gap only at restore time.
- **Missing evidence:** Willow's live row counts for client_intake_forms, client_consent_signatures, client_clinical_notes, treatment_images and client_portal_messages were not queried, so the concrete volume of currently-unexportable Willow data is unquantified. No test or CI check exists that compares export coverage to the schema, so drift since 0128 is unmeasured.

#### `F-IMPORT-001` — Quick Import commits clients and treatment memories in separate statements; a memory-insert failure strands clients permanently with no idempotent repair, and the final batch-completion update is unchecked

- **Status** OPEN · **Gate** BEFORE_STUDIO_2 · **Reachable** true · **Train** T10-data · **PR** PR-20
- **Source evidence:** app/(app)/settings/import/actions.ts:173-339 confirmImportAction. Three independent statements, no transaction and no RPC: :212-226 inserts import_batches; :249-264 bulk-inserts clients (on failure softVoidBatch + honest "nothing was imported"); :289-314 bulk-inserts imported_treatment_memories with exactly ONE retry (:293-300) then, on persistent failure, :306-312 soft-voids the batch and returns "N client(s) were created, but their imported treatment history could not be saved ... Re-importing will skip these clients, so their history must be added separately". :316-320 `await supabase.from("import_batches").update({completed_at}).eq("id", batchId);` — the returned error is never destructured or checked. Idempotency gap: :191-196 buildImportPlan drops groups whose action === "skip_duplicate", and existing clients are loaded at :100-113…
- **Hosted evidence:** Hosted facts do not cover import_batches / imported_treatment_memories row counts, so it is unknown whether Willow has run Quick Import in production or whether any voided batch exists. Migration max 0160 confirms 0089 (imported_treatment_memory) is applied, so the tables and their owner-INSERT policies are live.
- **Why P1:** Confirmed exactly as described, on all four points: non-transactional, clients survive a memory failure, the batch is soft-voided, and the completed_at update result is unchecked. Two mitigations the audit did not credit are real but do not close it — each insert is a single bulk statement (so it is all-or-none within itself, not a per-row partial), and the memory insert is retried once. Two aggravations the audit did not name: (a) the unchecked update at :316-320 means a fully successful import can permanently render as `completed: false` via lib/imported-treatment-memory.ts:178, with no alert; (b) there is no repair path at all — Quick Import cannot attach history to an existing client (the code comment at :307-308 says attach-to-existin…
- **Willow risk:** Not exercised at Willow yet (no import has been run), but the path is live: a partial import leaves the studio with half-loaded client records and no all-or-nothing retry.
- **Missing evidence:** Whether Quick Import has ever been executed in production (import_batches row count, and whether any row has voided_at set or completed_at NULL) was not queried. No fault-injection test exists, so the actual behaviour of the failure branch under a real DB error is unproven.

#### `F-RET-001` — Published 30-day / 90-day retention and deletion commitments have no implementing code: no purge job, no hard-delete path, no legal hold, no cross-system lifecycle

- **Status** OPEN · **Gate** WILLOW_NOW · **Reachable** true · **Train** T0-copy · **PR** PR-02, PR-21
- **Source evidence:** app/privacy/page.tsx:342-362 commits publicly to: retain while account active; "When a practitioner closes their account, we retain data for 30 days to allow account recovery, then delete it"; "When a client is deleted by their practitioner in Hone, their record is soft-deleted ... for 30 days, then hard-deleted from active systems. Backups are purged within 90 days." app/terms/page.tsx:208-213 (DPA 7.7) commits to "delete Client Data within 30 days" of termination. Against that: vercel.json registers exactly 3 crons (materialize-recurring-breaks 0 8 * * *, calendar-reconcile 0 9 * * *, calendar-sync 30 9 * * *); app/api/cron/ contains only appointment-reminders, calendar-reconcile, calendar-sync, materialize-recurring-breaks, no-show-check — none retention-related. A grep for legal hold / legal_hold / retention_policy / purge / offboard…
- **Hosted evidence:** Hosted facts confirm only 3 registered crons run, which independently corroborates that no retention job is scheduled. Migration max 0160; no retention/hold table appears in the migration create-table inventory. Supabase backup retention/purge configuration is provider-side and was not inspected.
- **Why P1:** Published 30-day deletion and 90-day backup-purge commitments are live on app/privacy/page.tsx:353-361 and app/terms/page.tsx:484-495, with no implementing code: no purge job, no hard-delete path, no legal hold, no cross-system lifecycle. app/api/cron/ contains only appointment-reminders, calendar-reconcile, calendar-sync, materialize-recurring-breaks and no-show-check, and archiveTreatmentImageAction stamps deleted_at without removing the storage object. P1 on parity: this is the same class as N-DOC-001 and F-COMP-001 — a live published claim the product does not honour — and both are P1. A published deletion commitment made to a health practitioner already holding real client data is not a smaller claim than a billing-lifecycle one. The …
- **Willow risk:** Live at Willow today. app/privacy/page.tsx:353-361 and app/terms/page.tsx:484-495 commit to a 30-day deletion and 90-day backup purge; no purge job, hard-delete path or legal hold exists. Willow already holds real client health data, so the audience for the commitment exists now.
- **Missing evidence:** Supabase backup retention configuration and whether a 90-day backup purge actually occurs could not be established from source; that is provider-side. No count of currently soft-deleted rows past 30 days at Willow. No legal review of whether the published wording matches operational reality.

#### `F-COMP-001` — The in-app Data settings page tells studio owners their clinical data is hosted in Canada while the privacy policy states it is in AWS US-East-1

- **Status** OPEN · **Gate** WILLOW_NOW · **Reachable** true · **Train** T0-copy · **PR** PR-02
- **Source evidence:** app/(app)/settings/data/page.tsx:58-62 — "All client records, session notes, and pricing belong to you. Hone stores this data in Canada on your behalf."; app/(app)/settings/data/page.tsx:141 — "Data hosted in Canada. Encrypted at rest. Exportable any time." Directly contradicted by app/privacy/page.tsx:182-187 — "Personal information is stored on infrastructure provided by **Supabase**, hosted in **AWS US-East-1 (Northern Virginia, United States)**"; app/privacy/page.tsx:188-192 — "This means data may be transferred to and stored outside of Canada. The United States has different privacy laws than Canada, and US authorities may have legal access to data stored in the US under US law."; app/privacy/page.tsx:195-197 — "We are evaluating Canadian data residency options for future deployments."; app/privacy/page.tsx:396-399 — "Hone is operat…
- **Hosted evidence:** Unchanged at c64366c9. Both statements are live on hone.care today: /privacy is a public marketing route and /settings/data is reachable by every studio owner, which includes Chloe at Willow. The two claims are mutually exclusive — data cannot simultaneously be "hosted in Canada" and "hosted in AWS US-East-1 (Northern Virginia, United States)" — so at least one in-app representation about the location of clinical records is false, and that is provable from source alone without any knowledge of the actual Supabase region. The privacy page's specificity, and its explicit statement that Canadian residency is only being *evaluated*, make the set…
- **Why P1:** Raised P2 -> P1. The original auditor rated this P2 largely because the ZIP could not establish the real cloud region; that caveat no longer limits the finding, because the defect is the contradiction itself and the contradiction is fully provable from the deployed source. This is an active, browser-reachable, written misrepresentation to a regulated Canadian health practitioner about where her clients' treatment records physically live — precisely the fact a practitioner relies on when discharging her own privacy obligations under PIPEDA and provincial health-information law, and precisely the fact she would repeat to her clients. It is not a missing future capability, it is not speculative, and it is not mitigated by single-studio scale:…
- **Willow risk:** Live at Willow today. The settings surface tells a Canadian health practitioner her clients' data is hosted in Canada while the privacy policy states AWS US-East-1. Whichever is true, one of the two statements she relies on is false.
- **Missing evidence:** I could not verify from source the actual AWS region of the production Supabase project, the plan tier, whether backups are encrypted, or whether any subprocessor or data-processing agreement exists. Resolving which of the two statements is true requires the Supabase dashboard and the provider contract; the contradiction, however, is established w…

#### `N-SEC-001` — Session practitioner attribution can be re-pointed, including to another studio's practitioner

- **Status** OPEN · **Gate** BEFORE_STUDIO_2 · **Reachable** true · **Train** T6-identity · **PR** PR-08
- **Source evidence:** supabase/migrations/0001_init.sql:82 sessions.practitioner_id references practitioners(id) on delete restrict; 0003_allergies_and_session_fields.sql:12 performed_by_practitioner_id references practitioners(id) on delete set null. 0094_tenant_consistency_constraints.sql adds composite same-studio FKs for client_id and appointment_id ONLY. 0160 names neither practitioner column.
- **Hosted evidence:** Read-only 2026-07-30: all five practitioner FKs on public.sessions are PLAIN single-column FKs to practitioners(id) — zero composite same-studio constraints. Triggers guarding practitioner columns: 0. has_table_privilege('authenticated','public.sessions','UPDATE')=true. sessions has 1 UPDATE-capable policy predicated on is_studio_member(studio_id), which still holds because studio_id does not change. 6 practitioners across 4 studios.
- **Why P1:** Surfaced by challenging the F-SEC-001 closure; F-SEC-001's own acceptance criteria named practitioner re-parenting, which 0160 does not cover. Because the FK targets practitioners(id) with no studio component, the target need not belong to the caller's studio. REACHABILITY QUALIFIER (pass 4): the cross-studio half is NOT reachable through the shipped UI — its only writer validates same-studio. It requires a direct PostgREST PATCH under the authenticated UPDATE grant on sessions, which is L18. So PR-11 (the L18 revoke) closes this finding's browser-reachability outright, and PR-08's constraint is the durable fix that survives any future grant. The finding remains P1 because the constraint is absent at the database level and the grant is liv…
- **Willow risk:** Willow has real clinical records; false attribution corrupts who-treated-whom history. Willow: 2 practitioners total — 1 ACTIVE owner, 1 INACTIVE non-owner practitioner. Active non-owner practitioners: 0. (Read-only aggregate, roles/active/counts only, 2026-07-30.)
- **Missing evidence:** Not reproduced by an actual PATCH (writes not authorized in this audit). CI-parity reproduction is the required next evidence.

#### `N-DOC-001` — Public terms and pricing pages claim a subscription, payment-processing and refund lifecycle the product does not have

- **Status** OPEN · **Gate** WILLOW_NOW · **Reachable** true · **Train** T0-copy · **PR** PR-02
- **Source evidence:** app/terms/page.tsx:256-272 — live public legal copy stating pricing is published at hone.care/pricing, that subscriptions are billed in advance and processed by Stripe, and a refund policy for subscription periods. No subscription billing exists (see F-BILL-001). Split out of F-BILL-001 by the frozen-head review, which found this live claim buried inside a row closed NOT_A_LAUNCH_REQUIREMENT. app/pricing/page.tsx carries the same recurring-subscription framing ("while you stay continuously subscribed"; "Pricing may change with 30 days' notice to existing subscribers") and only partly self-corrects at :171-173 ("Setup and payment activation happen during a guided onboarding, there is no self-service checkout"). The remediation must correct both public surfaces together, otherwise a corrected terms page points at an uncorrected pricing pag…
- **Hosted evidence:** Served publicly from the deployed build at c64366c9; no subscription, plan, or refund-lifecycle tables or Stripe subscription objects exist for it to describe.
- **Why P1:** A published legal page describing a billing lifecycle that does not exist is a live truthfulness/consumer-claim exposure, independent of whether the platform is ever built. It is copy-only and can be corrected immediately — it must not wait on billing architecture.
- **Willow risk:** Reputational/consumer-claim, not clinical. Live today.
- **Missing evidence:** No legal review performed; this audit states the mismatch, not its legal weight.

#### `L18` — authenticated holds direct row DML on five clinical tables

- **Status** OPEN · **Gate** BEFORE_STUDIO_2 · **Reachable** true · **Train** T7-clinical-dml · **PR** PR-10, PR-11
- **Source evidence:** 26 application call sites write sessions, session_blocks, electrolysis_entries, laser_entries and treatment_images directly with the browser client.
- **Hosted evidence:** authenticated: sessions SIUD, session_blocks SIUD, electrolysis_entries SIU, laser_entries SIU, treatment_images SIU. anon SELECT-only on all. session_block_areas SELECT-only (0159).
- **Why P1:** authenticated holds direct row DML on five clinical tables (sessions SIUD, session_blocks SIUD, electrolysis_entries SIU, laser_entries SIU, treatment_images SIU), reachable through PostgREST by any studio member. SEVERITY BASIS (corrected in pass 4): the pass-3 rationale rested this on parity with F-SEC-002 — 'bypasses the reviewed command path, its authorization checks and its audit trail'. All three of those specifics are FALSE at this baseline and have been withdrawn: (a) no reviewed command path exists for these five tables — the direct authenticated write IS the shipped path, which is why the remediation must BUILD the commands first; (b) the clinical audit trail is trigger-based (0086: sessions_aftercare_audit, session_blocks_probe_…
- **Willow risk:** Live at Willow today. authenticated holds DELETE on sessions and session_blocks, so any studio member can permanently destroy a clinical record through direct PostgREST DML; the application only soft-deletes, so there is no recovery path and no UI that would produce this state. HIGH RISK IF THE REMEDIATION IS ORDERED WRONG — revoking before the 26 callers are deployed onto narrow commands breaks …
- **Missing evidence:** none stated

#### `CHLOE-001` — Typing a custom treatment area commits one partial area row per keystroke

- **Status** DEPLOYED_NOT_VERIFIED · **Gate** NONE_SHIPPED · **Reachable** false · **Train** NONE · **PR** —
- **Source evidence:** FIXED at 395532489a07defd16d5c3a04ce26d2aedf46096 by PR #485 (merge c510575; fix commit f85ade2, review commit 62b5b63). The per-keystroke commit is gone: components/multi-area-editor.tsx no longer calls onChange([...value, { area }]) from the picker's change handler. Custom text is now local draft state inside AreaPicker and reaches the parent only through an explicit onCommitCustom callback, which routes through commitAreaToSet in the new lib/sessions/area-input.ts. At the audit baseline this file did not exist.
- **Hosted evidence:** Deployed to production in the delta between the audit baseline and 395532489a07defd16d5c3a04ce26d2aedf46096, with no migration. Production is serving the fix: hone.care, /login and /dashboard all return 200 at the reconciliation. NOT verified by a live production probe of the charting surface — that would require authenticated writes, which this audit is not authorized to perform.
- **Why P1:** Typing an N-character custom treatment area committed N partial area rows, corrupting the treated-area record for the session. FIXED and DEPLOYED by PR #485; the commit now happens once, on confirm/blur, through commitAreaToSet. Status is DEPLOYED_NOT_VERIFIED rather than PRODUCTION_VERIFIED because the studio owner who reported it has not yet accepted it on device, and no hosted read was run against live charting data. Severity is retained at P1 as the record of what was fixed; it carries no remaining remediation.
- **Willow risk:** Resolved on the live build. Historical rows created before 395532489a07defd16d5c3a04ce26d2aedf46096 may still hold duplicate partial areas from the old behaviour; no backfill or cleanup was performed or authorized, and none is scheduled.
- **Missing evidence:** STUDIO-OWNER ACCEPTANCE IS PENDING. Chloe reported this defect from live use and has not confirmed the fix on her own device. This finding is DEPLOYED_NOT_VERIFIED, not PRODUCTION_VERIFIED, for exactly that reason: CI proves the committed behaviour, it does not prove the reported symptom is gone for the person who reported it. No production row-le…

## Demoted from P1 — exact basis (8)

No demotion rests on "only one studio uses Hone today", and none rests on reversibility alone —
reversibility does not separate a P2 from a P1 here, since `CHLOE-001`, `F-COMP-001` and `F-CLIN-004`
are all practitioner-initiated and all reversible, and all three are P1.

| ID | Original | Current | Exact downgrade fact | Source evidence | Hosted evidence |
|---|---|---|---|---|---|
| `F-CLIN-001` | P1 | P3 | The race requires a finalizer transaction to run concurrently with a child write. There is no longer any way to start a finalizer transaction: the RPC's EXECUTE is revoked from every runtime role, no application code calls it, the enabling flags are pinned false by validated CHECKs so the studios-o… | supabase/migrations/0159_retire_signed_clinical_records.sql:175-183 (`revoke all on function public.finalize_session(uuid, integer) from public, anon, authenticated, service_role` plus the three correction/amendment RPCs); :190-191 (build_… | Hosted facts (authoritative, supplied): EXECUTE is revoked from public/anon/authenticated/service_role on all 10 retired functions including finalize_session and build_session_snapshot; both studio flags are FALSE on al… |
| `F-CLIN-002` | P1 | P3 | The finding's harm is that a verifier could call a hash valid while the snapshot fails to represent structured areas or the later 0155 probe-inventory / 0156 numbing fields. That harm requires snapshots to be a live product surface. They are not: build_session_snapshot cannot be executed by any run… | supabase/migrations/0159_retire_signed_clinical_records.sql:190-191 (`revoke all on function public.build_session_snapshot(uuid) from public, anon, authenticated, service_role` — the owner alone keeps it, solely for read-only re-derivation… | Hosted facts (authoritative, supplied): EXECUTE revoked from public/anon/authenticated/service_role on all 10 retired functions including build_session_snapshot; INSERT blocked on all three signed ledgers; exactly ONE l… |
| `F-CLIN-003` | P1 | P3 | I read the actual sync and the audit's central premise does not hold. There IS a field-level merge contract, written down and enforced at app/intake/[token]/actions.ts:254-341: five scalar fields are fill-only-if-null and allergies is append-only. A practitioner value that exists when the sync read… | app/intake/[token]/actions.ts:186-208 (the submit CAS: `.update({... status:'submitted' ...}).eq('id', v.intake_id).eq('status','in_progress')` with a zero-rows race-loser branch — this part is sound); :215 (`await syncIntakeToClient(merge… | The supplied hosted facts cover the clinical-retirement objects (0159), the lineage triggers (0160), the 86-table ACL survey, NOLOGIN role reality and the 139 SECURITY DEFINER functions. They say nothing about public.cl… |
| `F-SEC-001` | P1 | P2 | The finding as written — 'a same-studio member can re-parent a draft clinical session to the wrong client through direct DML' — is now FALSE in production. 0160 attaches BEFORE UPDATE OF row triggers that reject any change to sessions.client_id/studio_id and session_blocks.session_id/studio_id with… | supabase/migrations/0160_immutable_clinical_lineage.sql:98-123 (guard_immutable_clinical_lineage, SECURITY INVOKER, set search_path=''), :133-158 (guard_clearable_clinical_lineage), :172-201 (five BEFORE UPDATE OF triggers: sessions(client… | Authoritative hosted facts supplied: migration max 0160, 0160 present exactly once, nothing above it. 'Clinical lineage protection is APPLIED (migration 0160, PR #483 merged+deployed): 2 SECURITY INVOKER guard functions… |
| `F-SCHED-001` | P1 | P2 | The finding's premise is now only half true, and the half that survives is materially narrower than 'availability, overrides, blockouts and service scheduling settings remain directly writable by any studio member'. Availability defaults and date overrides — the tables the title leads with — were t… | ALREADY FIXED, contradicting the finding as written: supabase/migrations/0135_practitioner_availability.sql:101-124 drops the 0010 *_member_all policies on studio_availability_default and studio_availability_overrides and replaces them wit… | Authoritative hosted facts supplied: 'of 86 public tables, 64 grant TRUNCATE to BOTH anon and authenticated (Supabase ALTER DEFAULT PRIVILEGES). The 9 clinical tables 0159 covered are exceptions.' services, studio_block… |
| `F-SCHED-002` | P1 | P2 | The finding's mechanism is real and reproduced in the deployed source: internal creation (create_internal_appointment_v2) and internal move (move_or_reassign_appointment) both take the studios row lock plus the capacity advisory lock and return 'booking_paused' when v_cap AND NOT v_book, while publ… | app/book/[slug]/actions.ts:336-1029 (publicBookAppointmentAction — no read of practitioner_capacity_booking_enabled anywhere); app/book/[slug]/actions.ts:456 createAdminClient(); app/book/[slug]/actions.ts:463-466 loadPublicReadiness gate … | Not re-queried in this reconciliation. Authoritative facts supplied for this audit cover migration max 0160, clinical retirement flags and ACLs; they do NOT include studios.practitioner_capacity_enabled / practitioner_c… |
| `F-SCHED-003` | P1 | P2 | Downgraded from P1 to P2 after tracing what the DB actually enforces on the public path. The audit's core worry — an unserialized check-then-insert — is only half true. Every appointment INSERT, including this direct public one, fires appointments_sync_calendar_reservation_trg, which mirrors into s… | app/book/[slug]/actions.ts:488-510 (app-code slot recheck via getAvailableSlots + `free` membership test); app/book/[slug]/actions.ts:656-757 (clients INSERT happens FIRST, its own statement); app/book/[slug]/actions.ts:770-792 (separate a… | Not re-queried. Migrations 0029/0030/0031/0134/0137/0152 are all below the verified hosted max of 0160, so the exclusion constraints, the mirror triggers and the HB001 buffer trigger cited above are applied in productio… |
| `F-SCHED-004` | P1 | P3 | Technically confirmed exactly as written, including the audit's test-limitation claim: tests/lib/booking/tz-dst-two-pass.test.ts:127-139 deliberately PINS the wrong-by-an-hour mapping as expected behaviour, so the suite provides false confidence rather than rejecting the gap. No caller performs the… | lib/booking/tz.ts:42-68 (utcInstantFromLocal two-pass; comments :54-59 state the conventions explicitly — ambiguous fall-back 01:xx resolves to the FIRST occurrence, nonexistent spring-forward 02:xx maps to the instant one hour BEFORE the … | Not applicable — this is pure application code with no DB or flag dependency. It is in the deployed bundle at production SHA c64366c9. |

## Raised or re-classified in this pass (6)

| ID | Was | Now | Basis |
|---|---|---|---|
| `F-CLIN-004` | P2/OPEN | P1/OPEN | This one is real, unmitigated, and reachable through the ordinary deployed UI — not merely through a crafted request. The review page renders the answers-so-far of an unsubmitted intake along with an amber "the client has not submitted yet" notice, and directly beneath it renders a live "Mark revie… |
| `F-SEC-001` | see challenge | P2/PARTIALLY_FIXED | The finding as written — 'a same-studio member can re-parent a draft clinical session to the wrong client through direct DML' — is now FALSE in production. 0160 attaches BEFORE UPDATE OF row triggers that reject any change to sessions.client_id/studio_id and session_blocks.session_id/studio_id with… |
| `F-PAY-001` | P1/PARTIALLY_FIXED | P1/OPEN | Gate is WILLOW_NOW. amount_dollars is taken from the submitted form (payment-actions.ts:147) and validated only as >0 and <=200000 cents (parseAmountCents :112-125) before being written as amount_cents (:201). No server-side comparison against the booked service's authoritative price exists. STATUS… |
| `F-RET-001` | P2/OPEN | P1/OPEN | Published 30-day deletion and 90-day backup-purge commitments are live on app/privacy/page.tsx:353-361 and app/terms/page.tsx:484-495, with no implementing code: no purge job, no hard-delete path, no legal hold, no cross-system lifecycle. app/api/cron/ contains only appointment-reminders, calendar-… |
| `F-PROV-001` | see challenge | P3/NOT_A_LAUNCH_REQUIREMENT | This finding is about the ORIGINAL audit's evidence base, not about the product, and it is now resolved by construction. This reconciliation is not reading a ZIP: it reads a git worktree whose HEAD is the production SHA, whose tree hash equals that commit's tree, and which has no dirty tracked file… |
| `L18` | P2/OPEN | P1/OPEN | authenticated holds direct row DML on five clinical tables (sessions SIUD, session_blocks SIUD, electrolysis_entries SIU, laser_entries SIU, treatment_images SIU), reachable through PostgREST by any studio member. SEVERITY BASIS (corrected in pass 4): the pass-3 rationale rested this on parity with… |
