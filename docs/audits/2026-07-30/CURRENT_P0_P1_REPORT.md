# Current P0 and P1 findings — exact production `c64366c9ba4130283932bbe21e32bf2ed62c4975`, migration max 0160

Every classification below was verified against the exact production source at `c64366c9ba4130283932bbe21e32bf2ed62c4975` and read-only
hosted evidence on 2026-07-30. Nothing here is inherited from a historical register.

## Current P0 findings

**ZERO current P0 confirmed.**

The only P0 in the source audit was **F-CLIN-000**. It is no longer P0, on two independent grounds,
both verified:

1. **Its reachable half is closed.** Migration 0159 §5c revoked all DML on `session_block_areas` from
   `public`/`anon`/`authenticated` and replaced the 0128 `FOR ALL` membership policy with a
   SELECT-only policy. Hosted ACL confirms `session_block_areas` is SELECT-only for `authenticated`
   and `anon` holds nothing. All five remaining references in shipped code are `.select()` reads.
2. **Its other half is a retired capability, not a deferred fix.** "Outside correction lineage" and
   "add structured areas to snapshot v2" rest on the signed-record premise that was permanently
   retired. Finalization cannot be enabled (two validated CHECK constraints), cannot be invoked
   (EXECUTE revoked from every runtime role on all 10 functions), cannot be entered by status
   transition, and no new snapshot/amendment/audit row can be inserted.

Residual: **P3, PRODUCTION_VERIFIED.** No containment action is outstanding.

## Current P1 findings (6)

| ID | Title | Exposure | Exact evidence | Studio #2 blocker | Dependency | Required proof |
|---|---|---|---|---|---|---|
| `HN-007` / `F-SEC-002` | Any authenticated studio member can create, retime, re-status or delete appointments by direct PostgREST DML, bypassing the owner gates, duration authority, booking kill switch, buffer rule and every audit row of create_internal_appointment_v2 / move_or_reassign_appointment | REACHABLE_IN_PRODUCTION | supabase/migrations/0010_booking_v1.sql:241 (alter table public.appointments enable row level security), :271-276 (policy "appointments_member_all" FOR ALL using/with check is_studio_member(studio_id) — the only appointments policy in the whole tree; grep of every migration for 'create policy'/'dro… | **YES** | Independent of L18 and of the clinical work. Two coupled steps: (1) revoke insert, update, delete on public.appointments from anon, authenticated (SELECT retained — the user-scoped server client reads appointments in lib/booking/queries.ts, app/(app)/calendar/[id]/page.tsx:96, app/(app)/dashboard/p… | None for this boundary. tests/db contains strong behavioural coverage of the COMMAND path (duration-and-availability-validator.db.test.ts, double-booking-constraint.db.test.ts, move-target-integrity.db.test.ts, practitioner-move-appointment.db.test.ts, appointments-tenant-consistency.db.test.ts, sc… |
| `HN-016` / `F-PRIV-001` | Sentry receives raw bearer credentials embedded in URL path segments (intake, portal, appointment-manage and calendar-feed tokens) | REACHABLE_IN_PRODUCTION | lib/observability/sentry-scrub.ts:183-199 scrubRequest — deletes cookies, headers and query_string, but line 189 does request.url = redactString(request.url.split("?")[0]), i.e. the PATH is deliberately preserved and only value-pattern scrubbed. lib/observability/sentry-scrub.ts:91-98 the value pat… | no — gate WILLOW_NOW | None — fully self-contained and code-only, confined to lib/observability/sentry-scrub.ts. Three changes: (1) in scrubRequest, canonicalize the path against the known bearer-route prefixes (/intake/, /manage/, /cancel/, /reschedule/, /portal/verify/, /calendar-feed/) to a template such as /intake/[t… | None, and the gap is verifiable: tests/lib/observability/sentry-scrub.test.ts contains no occurrence of 'intake', '/manage/', 'path' or 'transaction'. Every existing assertion covers key-based redaction and the five value patterns. This is exactly the audit's stated test limitation — the suite pass… |
| `HN-019` / `F-DATA-001` | Owner ZIP export omits most tenant-owned data (intake, consent, clinical notes, images, payments, portal, numbing/inventory fields) while the UI presents it as a complete backup | REACHABLE_IN_PRODUCTION | app/(app)/settings/data/actions.ts:30-41 (owner gate); :47-198 (the ONLY 16 reads: clients, sessions, electrolysis_entries, laser_entries, practitioners, client_pricing, session_blocks[13 cols], appointments, treatment_plans, treatment_plan_stages, services[id,name], practitioners[id,name], 4x reco… | **YES** | Two-phase. Phase 1 (no migration, days): correct app/(app)/settings/data/page.tsx to say "partial export", replace the "Not included" paragraph with the real exclusion list (intake, consent, clinical notes, treatment photos, payments, portal messages, availability, audit), add the four record_keepi… | None. tests/app/settings/data/export-owner-gate.test.ts is a source-string test (readFileSync + regex over actions.ts). There is no DB or browser test that seeds a row in each table and asserts it appears in the archive. |
| `HN-021` / `F-IMPORT-001` | Quick Import commits clients and treatment memories in separate statements; a memory-insert failure strands clients permanently with no idempotent repair, and the final batch-completion update is unchecked | REACHABLE_IN_PRODUCTION | app/(app)/settings/import/actions.ts:173-339 confirmImportAction. Three independent statements, no transaction and no RPC: :212-226 inserts import_batches; :249-264 bulk-inserts clients (on failure softVoidBatch + honest "nothing was imported"); :289-314 bulk-inserts imported_treatment_memories wit… | **YES** | Needs a transactional import RPC (SECURITY DEFINER, owner-verified, taking the planned clients + memories as one JSON payload and inserting both inside one statement), or staged tables with validate→preview→commit. Requires giving each source row a stable identity key so a retry upserts idempotentl… | tests/db/quick-import.db.test.ts and tests/app/settings/import/quick-import-action.test.ts exist but no fault-injection test forces the memory insert to fail after clients commit, and none asserts retry convergence. tests/lib/import/quick-import.test.ts covers the pure planner only. |
| `HN-038` / `F-COMP-001` | The in-app Data settings page tells studio owners their clinical data is hosted in Canada while the privacy policy states it is in AWS US-East-1 | REACHABLE_IN_PRODUCTION | app/(app)/settings/data/page.tsx:58-62 — "All client records, session notes, and pricing belong to you. Hone stores this data in Canada on your behalf."; app/(app)/settings/data/page.tsx:141 — "Data hosted in Canada. Encrypted at rest. Exportable any time." Directly contradicted by app/privacy/page… | no — gate WILLOW_NOW | Independent of all engineering findings. Shares a root cause with F-DOC-001 — no gated claims manifest — but must not wait for that control to be built. The verified-wording change should precede any further sales conversation. | None. No test asserts consistency between in-app claims and the privacy policy, and per F-TEST-001 the marketing-copy tests that do exist assert wording rather than truth — so this contradiction survives a fully green suite at exact head. |
| `HN-049` / `N-SEC-001` | Session practitioner attribution can be re-pointed, including to another studio's practitioner | REACHABLE_IN_PRODUCTION | supabase/migrations/0001_init.sql:82 sessions.practitioner_id references practitioners(id) on delete restrict; 0003_allergies_and_session_fields.sql:12 performed_by_practitioner_id references practitioners(id) on delete set null. 0094_tenant_consistency_constraints.sql adds composite same-studio FK… | **YES** | Independent of L18. Fix is a same-studio composite FK plus/or a 0160-style column guard; both are additive and need no application change (no call site writes these columns from a payload — verify before revoking). | None exists. tests/db/immutable-clinical-lineage.db.test.ts covers only the 0160 columns. |

### Why each P1 is a P1

#### `F-SEC-002` — Any authenticated studio member can create, retime, re-status or delete appointments by direct PostgREST DML, bypassing the owner gates, duration authority, booking kill switch, buffer rule and every audit row of create_internal_appointment_v2 / move_or_reassign_appointment

- **Status:** OPEN · **Gate:** BEFORE_STUDIO_2 · **Reachable:** true
- **Source:** supabase/migrations/0010_booking_v1.sql:241 (alter table public.appointments enable row level security), :271-276 (policy "appointments_member_all" FOR ALL using/with check is_studio_member(studio_id) — the only appointments policy in the whole tree; grep of every migration for 'create policy'/'drop policy' on public.appointments returns exactly these two lines), :217-225 (appointment_audit.appointment_id references public.appointments(id) ON DELETE CASCADE). No table-level revoke exists for appointments anywhere in supabase/migrations (a grep for table-level revoke returns 0092/0111/0113/0115/0119/0122/0126 etc., never appointments or any schedule table), so the Supabase default privileges leave authenticated with INSERT/UPDATE/DELETE. What the command path enforces that direct DML skips: supabase/migrations/0152_actual_overlap_hard_buffer_soft.sql:407-423 (studios row FOR UPDATE + acq…
- **Hosted:** Authoritative hosted facts supplied: 'of 86 public tables, 64 grant TRUNCATE to BOTH anon and authenticated (Supabase ALTER DEFAULT PRIVILEGES). The 9 clinical tables 0159 covered are exceptions.' appointments is not one of the 9 covered tables and has no revoke in source, so it is inside the 64 — i.e. the default ALL grant (which carries INSERT/UPDATE/DELETE alongside TRUNCATE) is intact for authenticated. The same facts confirm the reachability boundary that matters here: PostgREST exposes SELECT/INSERT/UPDATE/DELETE and RPC (it only withholds TRUNCATE), and anon/authenticated/service_role are NOLOGIN so only authenticator can connect — which is exactly the path a logged-in studio member'…
- **Willow risk:** Real but currently self-limited. Willow is live with real appointments and live payments (6 charges), so the exposed row set is production clinical/scheduling data. Today the only authenticated principals are Willow's own practitioners — most likely Chloe as owner — so the practical vector is a compromised or XSS'd session, a buggy client, or a future non-owner practitioner, rather than a distinct malicious insider. The revoke itself carries essentially zero Willow risk because no browser or us…
- **Missing evidence:** I could not determine from source how many active non-owner practitioners exist at Willow today; if the answer is zero, the member-versus-owner half of this bypass has no distinct actor at Willow right now (the owner-versus-her-own-invariants half, and the audit-cascade half, remain). A direct read of practitioners (studio, role, active) would settle it. I also did not execute anything — no live …

#### `F-PRIV-001` — Sentry receives raw bearer credentials embedded in URL path segments (intake, portal, appointment-manage and calendar-feed tokens)

- **Status:** OPEN · **Gate:** WILLOW_NOW · **Reachable:** true
- **Source:** lib/observability/sentry-scrub.ts:183-199 scrubRequest — deletes cookies, headers and query_string, but line 189 does request.url = redactString(request.url.split("?")[0]), i.e. the PATH is deliberately preserved and only value-pattern scrubbed. lib/observability/sentry-scrub.ts:91-98 the value patterns: JWT_RE = /\beyJ[a-zA-Z0-9_-]+\.[a-zA-Z0-9_-]+\.[a-zA-Z0-9_-]+/g requires THREE dot-separated segments; BEARER_RE requires a literal 'Bearer ' prefix; SUPABASE_TOKEN_RE matches only sb-*-auth-token / sbp_ / service_role. lib/observability/sentry-scrub.ts:235-291 scrubEventCommon never touches event.transaction (the transaction NAME is not scrubbed at all). Token routes that put a credential in the path: app/intake/[token], app/manage/[token], app/cancel/[token], app/reschedule/[token], app/portal/verify/[token], app/calendar-feed/[token]. Token formats that DEFEAT the regexes: lib/intake…
- **Hosted:** Sentry is live in production — it was merged and deployed at ed23a7a with no env kill switch in the config (the DSN is a hardcoded literal, not an env-conditional), so every production deployment initializes it. Migrations 0159 and 0160 are clinical-lineage work and touch nothing in the observability path; git log shows no commit touching lib/observability/sentry-scrub.ts after the original hardening, so the exact-head file is the deployed file. The NOLOGIN/PostgREST reachability facts do not mitigate this at all — the leak path is HTTPS egress from the Next.js process to a third-party SaaS, not a database grant.
- **Willow risk:** Directly and continuously exposes Willow's real clients. A leaked /intake/[token] replays into that client's health intake form; /manage/, /cancel/ and /reschedule/ tokens let a holder alter or cancel a real client's appointment; /portal/verify/ reaches portal access; and the practitioner calendar-feed token exposes Chloe's entire availability with no expiry and no way to revoke it in the UI. Because tracesSampleRate is 0.1 in production, this is not a rare edge case — it is the expected steady…
- **Missing evidence:** I cannot prove from source whether any such event has ALREADY been transmitted to Sentry — that requires querying the Sentry project's stored events (search by route pattern, never exporting raw URLs). I also cannot confirm from the repository whether Sentry's data region is US or EU, or the project's retention window and member list, all of which bound the actual blast radius. Sentry error event…

#### `F-DATA-001` — Owner ZIP export omits most tenant-owned data (intake, consent, clinical notes, images, payments, portal, numbing/inventory fields) while the UI presents it as a complete backup

- **Status:** OPEN · **Gate:** BEFORE_STUDIO_2 · **Reachable:** true
- **Source:** app/(app)/settings/data/actions.ts:30-41 (owner gate); :47-198 (the ONLY 16 reads: clients, sessions, electrolysis_entries, laser_entries, practitioners, client_pricing, session_blocks[13 cols], appointments, treatment_plans, treatment_plan_stages, services[id,name], practitioners[id,name], 4x record_keeping_*); :334-339 (session_block_areas read, used ONLY to build a merged label string, never its own file); :162 comment "No image binaries / storage paths / payment tables here."; :267-777 (exactly 13 CSVs + README.txt written to the zip); :747-775 README "This export contains all client records, sessions, entries, appointments, and treatment plans for your studio" and "If you ever leave Hone, your records leave with you". app/(app)/settings/data/page.tsx:58-63 "You can export everything at any time"; :66-69 "Download a ZIP of your studio data. During the pilot, use this as a backup aft…
- **Hosted:** Hosted facts supplied: migration max 0160; live payments ARE on at Willow (6 live charges) — so real payment records exist in production that this export cannot produce. Willow has ZERO non-draft sessions and there is exactly ONE legacy finalized session + ONE snapshot in a NON-Willow test studio, so the omitted clinical_record_snapshots content is currently 1 row outside Willow. Copy ledger (session_copy_operations) 0 rows; GCal tables 1 row. 0 clinical_audit_events, 0 amendments. Willow's actual intake/consent/image/portal row counts were NOT re-queried today.
- **Willow risk:** HIGH-CONFIDENCE, LIVE. Chloe is the owner of the only production studio and the page instructs her to take this ZIP as her backup after each charting day. If she relies on it, her intake forms, signed consents, dedicated clinical notes (PR #479/#477 work), every treatment photo, and all 6 live payment records are absent — and she has no way to know, because the on-screen "Not included" list names only private warnings. This directly contradicts the "your records leave with you" promise she has …
- **Missing evidence:** Willow's live row counts for client_intake_forms, client_consent_signatures, client_clinical_notes, treatment_images and client_portal_messages were not queried, so the concrete volume of currently-unexportable Willow data is unquantified. No test or CI check exists that compares export coverage to the schema, so drift since 0128 is unmeasured.

#### `F-IMPORT-001` — Quick Import commits clients and treatment memories in separate statements; a memory-insert failure strands clients permanently with no idempotent repair, and the final batch-completion update is unchecked

- **Status:** OPEN · **Gate:** BEFORE_STUDIO_2 · **Reachable:** true
- **Source:** app/(app)/settings/import/actions.ts:173-339 confirmImportAction. Three independent statements, no transaction and no RPC: :212-226 inserts import_batches; :249-264 bulk-inserts clients (on failure softVoidBatch + honest "nothing was imported"); :289-314 bulk-inserts imported_treatment_memories with exactly ONE retry (:293-300) then, on persistent failure, :306-312 soft-voids the batch and returns "N client(s) were created, but their imported treatment history could not be saved ... Re-importing will skip these clients, so their history must be added separately". :316-320 `await supabase.from("import_batches").update({completed_at}).eq("id", batchId);` — the returned error is never destructured or checked. Idempotency gap: :191-196 buildImportPlan drops groups whose action === "skip_duplicate", and existing clients are loaded at :100-113 including archived rows, so a retry of the identi…
- **Hosted:** Hosted facts do not cover import_batches / imported_treatment_memories row counts, so it is unknown whether Willow has run Quick Import in production or whether any voided batch exists. Migration max 0160 confirms 0089 (imported_treatment_memory) is applied, so the tables and their owner-INSERT policies are live.
- **Willow risk:** MEDIUM. Willow is already onboarded, so Chloe's exposure is limited to any later paste of historical rows. The acute risk is studio #2's paper-card migration: a single malformed history row or transient DB error after the client insert leaves that studio's whole client list present but permanently detached from its treatment history, with the honest-but-unhelpful error telling the operator the history must be re-entered by hand.
- **Missing evidence:** Whether Quick Import has ever been executed in production (import_batches row count, and whether any row has voided_at set or completed_at NULL) was not queried. No fault-injection test exists, so the actual behaviour of the failure branch under a real DB error is unproven.

#### `F-COMP-001` — The in-app Data settings page tells studio owners their clinical data is hosted in Canada while the privacy policy states it is in AWS US-East-1

- **Status:** OPEN · **Gate:** WILLOW_NOW · **Reachable:** true
- **Source:** app/(app)/settings/data/page.tsx:58-62 — "All client records, session notes, and pricing belong to you. Hone stores this data in Canada on your behalf."; app/(app)/settings/data/page.tsx:141 — "Data hosted in Canada. Encrypted at rest. Exportable any time." Directly contradicted by app/privacy/page.tsx:182-187 — "Personal information is stored on infrastructure provided by **Supabase**, hosted in **AWS US-East-1 (Northern Virginia, United States)**"; app/privacy/page.tsx:188-192 — "This means data may be transferred to and stored outside of Canada. The United States has different privacy laws than Canada, and US authorities may have legal access to data stored in the US under US law."; app/privacy/page.tsx:195-197 — "We are evaluating Canadian data residency options for future deployments."; app/privacy/page.tsx:396-399 — "Hone is operated from Canada with infrastructure in the United S…
- **Hosted:** Unchanged at c64366c9. Both statements are live on hone.care today: /privacy is a public marketing route and /settings/data is reachable by every studio owner, which includes Chloe at Willow. The two claims are mutually exclusive — data cannot simultaneously be "hosted in Canada" and "hosted in AWS US-East-1 (Northern Virginia, United States)" — so at least one in-app representation about the location of clinical records is false, and that is provable from source alone without any knowledge of the actual Supabase region. The privacy page's specificity, and its explicit statement that Canadian residency is only being *evaluated*, make the settings-page claim the false one on the balance of e…
- **Willow risk:** Direct and current. Chloe is the owner of a live electrolysis practice holding real client health information; she can read "Hone stores this data in Canada on your behalf" in her own settings today, and may have relied on it in her own client-facing privacy representations. If a client exercises an access or complaint right, the discrepancy between what Hone told her and what the privacy policy says is discoverable and attributable.
- **Missing evidence:** I could not verify from source the actual AWS region of the production Supabase project, the plan tier, whether backups are encrypted, or whether any subprocessor or data-processing agreement exists. Resolving which of the two statements is true requires the Supabase dashboard and the provider contract; the contradiction, however, is established without them.

#### `N-SEC-001` — Session practitioner attribution can be re-pointed, including to another studio's practitioner

- **Status:** OPEN · **Gate:** BEFORE_STUDIO_2 · **Reachable:** true
- **Source:** supabase/migrations/0001_init.sql:82 sessions.practitioner_id references practitioners(id) on delete restrict; 0003_allergies_and_session_fields.sql:12 performed_by_practitioner_id references practitioners(id) on delete set null. 0094_tenant_consistency_constraints.sql adds composite same-studio FKs for client_id and appointment_id ONLY — not for either practitioner column. 0160_immutable_clinical_lineage.sql pins studio_id/client_id/session_id/block_id but names NEITHER practitioner column.
- **Hosted:** Verified read-only against production 2026-07-30: all five practitioner FKs on public.sessions (practitioner_id, performed_by_practitioner_id, finalized_by, deleted_by, aftercare_and_risks_explained_by) are PLAIN single-column FKs to practitioners(id) — zero composite same-studio constraints. Triggers guarding the practitioner columns: 0. has_table_privilege('authenticated','public.sessions','UPDATE') = true. sessions has 1 UPDATE-capable policy, predicated on is_studio_member(studio_id) — which still holds because studio_id does not change. 6 practitioners across 4 studios.
- **Willow risk:** Willow has 2 practitioners and real clinical records; false attribution would corrupt who-treated-whom history and any future decision support or medico-legal position.
- **Missing evidence:** Not reproduced by an actual PATCH against production (writes are not authorized in this audit). Reproduction on a CI-parity database is the required next evidence.

