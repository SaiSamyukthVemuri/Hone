# Hone — Release Changelog

Human-readable shipped-change history for the current wave. All entries below are **merged
to the production branch (`claude/build-hone-saas-hOex7`) and deployed** unless noted.
Reverse-chronological within the wave is not enforced; the table is ordered by PR number.
For schema detail see [migration-ledger.md](./migration-ledger.md); for the live / OFF / held payment
posture see [current-state.md](./current-state.md).

> **⚠️ Each row was written at its PR's date and reflects the state THEN.** Several rows
> carry pre-merge or pre-apply wording ("not hosted-applied", "NOT cron-registered", "hosted
> max stays 013x") that is **no longer true**. Notably: **migration 0132 IS applied**; the
> calendar cron routes **ARE** registered in `vercel.json` (PR #430); `calendar_sync_outbox`
> and `calendar_event_links` each hold **one row**, not zero.
>
> **This preamble no longer states the current migration max or the current runtime-bearing
<!-- canonical-facts:ignore-start reason=quotes-the-superseded-preamble-numbers -->
> head.** It used to, and both went stale — it read *"the production migration max is 0157 and
> the runtime-bearing head is `96b28d6…`"* long after production had moved past both. A
<!-- canonical-facts:ignore-end -->
> changelog is a record of what shipped, not a second copy of current state. For current
> truth: hosted max is declared in [`migration-state.json`](./migration-state.json), repo max
> and next free are derived by `npm run migration:state`, and the reconciled position lives in
> [current-state.md](./current-state.md) and [migration-ledger.md](./migration-ledger.md).
>
> **Historical rows below are NOT rewritten** when today differs from their THEN-state. That is
> the point of a changelog. Only this preamble is kept current.

## Most recent wave

*Ordered by PR number, not by date — a row's position implies nothing about when it shipped.
This section previously carried a "(through 2026-07-27)" heading while containing rows dated
2026-08-19 and later.*

**Coverage gap, stated rather than hidden:** this table covers PRs **#357–#479**, **#601**,
**#629**, **#630**, and — added by the 2026-08-26 reconciliation — the **fourteen consecutive
merges from `b9e0003f` to `6786b07b`** (**#632–#645**). Roughly 150 merges between #479 and #601
— including migrations 0159–0184 — still have **no row here**. Those releases are recorded in
[migration-ledger.md](./migration-ledger.md) and in the git history; they were deliberately
**not** backfilled, because the scope of both reconciliations was current truth rather than
historical completeness. Do not read a missing row as "not shipped".

**The #632–#646 block is complete, not sampled.** It was enumerated mechanically
(`git log --merges --first-parent b9e0003f..5ad81129`) rather than recalled, so the current
state can be reconstructed from shipped history without a gap in that range. Each row records
whether the merge was **runtime-bearing**, decided by changed-path analysis rather than by the
PR title.

| PR | Migration | Status | What shipped | Notes |
|---|---|---|---|---|
| **#646** | none | **Deployed · owner-only · NOT production-exercised** | **FIN-01A Slice 1** — the owner financial truth surface at `/financials`: what the calendar held in one studio-local period, and how those appointments divided | Merged `5ad81129abfa0cd1f48bc8b51feb15a0738922db`. **Runtime-bearing** (12 deployed files, 7 tests). **No migration, RPC, new table or schema work; 0187 untouched; no external side effect.** Owner-only, and the refusal is the **first statement** of `loadFinancialsView` — before a Supabase client exists — so a practitioner triggers no studio-wide query. The route is **withheld from search** via `NON_SEARCHABLE_ROUTES`; no `NAV_ENTRIES` row. **It adds no money arithmetic**, pinned by a source guard over all three truth classes' ledger identifiers, reading exactly one table; the anchor is in **visits, not service value**, which is Slice 2. Unknown is a closed cause vocabulary and **`known(0)` is the only route by which a zero reaches the screen**, so a failed read renders a sentence naming the cause, never a zero. No browser E2E added, deliberately — an `e2e/` spec would trip the spec-count pin |
| **#648** | none | **Deployed + enabled for all studios** | **Clinical *Before today* fail-closed (F2)** — the Dashboard pipeline's four Supabase reads stop rendering a failed read as an answer | Merged `8418a75507f0006efded89ce1439faab63b73890`. **Runtime-bearing** (4 deployed files, 5 tests). **No migration, schema, RPC, DB policy or provider change; no production data mutation.** The reads destructured `data` alone — `error` did not appear in `lib/dashboard/before-today-previews.ts` at all — so a failure arrived `null`, became `[]`, and was rendered as an answer. It stayed invisible because `pickLastTreatment` accepts a session on its **live entries**, which ride on the *sessions* read: when `session_blocks` failed, `hasHistory` stayed **true** while every block-derived field collapsed. All four reads now pass one wrapper retaining PostgREST `error` **and** a rejected invocation, classified into two **independent** facts — `clinicalUnavailable` (three clinical reads) and `clientRecordUnavailable` (the clients read) — so a failed clinical read never hides true record reminders and a failed client read never blanks a history that was read. **Explicit non-claim:** `DASHBOARD_RETURNING_AS_NEW = NOT_PROVEN` — withdrawn, not asserted. **Out of scope and still open:** F3 (recency/tie authority, separate confirmed P2), F4 (latent, deferred), HIST-01A (untouched) |
| **#644** | none | **Deployed + enabled — NOT production-exercised as a payload change** | **TRUTH-01A** — the export resource registry. `lib/export/resource-registry.ts` becomes the one place a resource's export disposition is decided; a **missing decision is a build failure**; schema authority is `information_schema` introspection, never parsed migration SQL | Merged `6786b07be57a9c01ff4421378f22d7dbca68a5c9`. **Runtime-bearing** (8 files). **The export payload is byte-for-byte unchanged** — `export-emission-parity.test.ts` pins every header row against base `a1639a84` column-for-column. Roughly 59 studio-owned resources remain **pending**, ticketed `TRUTH-01B`/`TRUTH-01C`. The application-source reachability analyser was **withdrawn** as insufficiently robust and nothing replaces it. See [known-limitations.md](./known-limitations.md) **L29** |
| **#643** | none | Deployed + enabled | **Studio launch consent readiness** — readiness now requires at least one qualifying live treatment consent | Merged `9eb3c3174762c733ff553220e9c72341fc9032c5`. **Runtime-bearing** (3 files). Predicate is the intake's own live-form contract: server-resolved `studio_id`, `form_type='treatment_consent'`, `status='active'` **and** `is_live=true`. Both conditions kept even though 0072's CHECK makes the second redundant today, so a future weakening cannot turn a draft into launch evidence |
| **#642** | none | Deployed + enabled | **CLIN-01-B** — a failed clinical read is never rendered as "no history" | Merged `a3b85af22b…`. **Runtime-bearing** (6 files). Two `session_blocks` reads dropped `error`, so a failure became `null ?? []` — indistinguishable from a first-visit client on four surfaces. All four now check `unavailable` before `hasHistory`; caution fields protected on both tabs. **No migration, no new query, no widened PII read** |
| **#645** | none | Deployed + enabled for owners | Permanent **Business** navigation entry for studio owners | Merged `14be8198e5…`. **Runtime-bearing** (2 files). Owner-only; a practitioner sees no such item — absent, not disabled. Points at `/dashboard/capacity`. **Nav visibility is presentation and protects nothing** — the route's own server-side owner check remains the authority |
| **#641** | none | Test-only | **OWNER-CAP Slice 1 browser coverage** — nine tests for `/dashboard/capacity` | Merged `a1639a84e3…`. Counted **runtime-bearing** by path analysis (1 file) but adds no product behaviour. Proves an owner reaches the page, a practitioner is **refused in place** (the product renders a refusal; it does not redirect), and rebooking names are real links landing on the right client |
| **#639** | none | **Documentation + tests only — NOT runtime-bearing** | Design-engineering skills | Merged `0cc9a042…`. Touches no `app/`, `lib/`, `components/`, `supabase/` or `scripts/` path |
| **#638** | none | **Deployed + enabled for owners — NOT production-exercised** | **OWNER-CAP Slice 1** — owner-only, read-only `/dashboard/capacity`: which active treatment clients have fallen off the calendar | Merged `6e5283db54…`. **Runtime-bearing** (5 files). **No migration. No RPC.** The role check runs **before any capacity read is issued**. One Data API statement, one snapshot, open-plan evidence embedded as a `count` rather than rows. No usage measured; none claimed |
| **#637** | none | **Deployed — NO STUDIO ENABLED — NOT exercised** | **WAIT-02B Stage B1** — disclose waitlist prospects; turn the Stage-A kill switch into a Stage-B activation report | Merged `1013a97b2e…`. **Runtime-bearing** (4 files). **No migration.** `app/privacy/page.tsx` now covers **Prospective clients**. ⚠️ **Gate 4 became report-only** — it no longer fails a build. Activation needs **two** allowlists (contract sentence 9). Merging enabled nothing, for anybody. See **L25** |
| **#640** | none (records `0187`) | **Documentation + tests only — NOT runtime-bearing** | Canonical hosted-state record for the `0187` production apply | Merged `46660c21…`. Touches no runtime path |
| **#636** | **0187** | **DB applied · deployed · enabled — NOT production-exercised (0 rows)** | **PAY-SETTLE** — record how a visit was actually settled, without a fake charge. `public.appointment_settlements` plus `record_appointment_settlement`, `waive_appointment_fee`, `supersede_appointment_settlement`, and the service-role `reconcile_card_payment_succeeded` | Merged `f9ad0f727503ec7aaa6208aa4aa7e5c84ad5eb1e`. **Runtime-bearing** (15 files). **Migration-first:** 0187 applied 2026-08-24 from the exact reviewed head `eb7e8240`, **before** this merge existed. **`method` has no `card` and no `hone` member**, so a fabricated card attestation is *unrepresentable*. Wrote **zero rows** and backfilled nothing — `unknown` stays an absence. Replaces `claim_session_payment_charge_attempt` with a byte-identical signature and unchanged ACL |
| **#634** | none (records `0186`) | **Documentation + tests only in intent** | Canonical hosted-state record for the `0186` production apply | Merged `4b9636f66f…`. Path analysis counts **1 runtime file** — `scripts/migration-state.mjs` — so it is not purely documentation, though it changes no product behaviour |
| **#635** | none | Deployed + enabled | Dashboard snapshot scroll fix | Merged `f2de18be…`. **Runtime-bearing** (1 file: `app/(app)/dashboard/practice-snapshot.tsx`) |
| **#633** | none | Deployed + enabled | **UI-01C** — pending navigation feedback | Merged `ecb6a1ce…`. **Runtime-bearing** (4 files). Ships `PendingLink` / `PendingContainerLink` |
| **#632** | **0186** | **DB applied · deployed · enabled by default** | **Intake reminders at 24h / 2h**, replacing the 0098 7d/3d write path | Merged `5bbd37a5ceaeb105e65840971392823a2e68aabd`. **Runtime-bearing** (13 files). 0186 adds exactly one column — `studios.send_intake_reminders`, boolean NOT NULL DEFAULT TRUE — plus a comment; **no DML anywhere in the file**. **The 0098 7d/3d columns, indexes and both email branches remain intact and are historical** — the application simply stops writing them |
| **#630** | none (records `0185`) | **Documentation + tests only — NOT runtime-bearing** | Canonical hosted-state record for the `0185` production apply; re-pinned the carried checksum chain | Merged `b9e0003fa5809b328fffeb8d352af319138bd531`. **Advanced the branch HEAD above the runtime-bearing SHA without changing anything Hone does** — the diff touches no `app/`, `lib/`, `components/`, `supabase/` or `scripts/` path. Deployed as `dpl_5jGQkF4PjsoyiyKJCqRc6o4bsxwu` (Ready) |
| **#629** | **0185** | **DB applied · deployed · DARK — no studio enabled, 0 rows, not exercised** | **WAIT-02B Stage A** — durable studio-scoped new-client waitlist: `new_client_waitlist_entries` plus `join_new_client_waitlist` / `remove_new_client_waitlist_entry`. Moves the commit point for a waitlist request off the studio notification email and onto a committed database row | **CODE-FIRST AND DARK — the reverse of 0183/0184's migration-first ordering, deliberately.** Merged `48f0238900c07bd5d2dfed5c1ebbd832e77fdc50` and deployed (`dpl_8iRpfwjbYQHXmbNqrUJnnvFjqH35`, Ready) **before** migration `0185` was applied on 2026-08-23. `NEW_CLIENT_WAITLIST_DURABLE_STUDIO_SLUGS` is **absent from Vercel Production**; **Willow is not enabled** and continues to serve WAIT-01. Ships an **inverted build gate** (`scripts/check-production-env-gates.mjs`) that **fails a production build** while the durable allowlist enables any studio. Stage B not started — see [known-limitations.md](./known-limitations.md) L25 |
| **#601** | none | Willow pilot activated; controlled canary exercised | New-client waitlist admission control (**WAIT-01** — commit point is the studio notification email, not a database row) | Release record: [releases/2026-08-19-willow-new-client-waitlist.md](./releases/2026-08-19-willow-new-client-waitlist.md) |
| **#478** | **0157** | **Deployed + enabled — NOT production-exercised; human acceptance pending** | Whole-session copy — "Copy areas & settings from last session": editable ephemeral preview, zero writes before an explicit commit, one atomic commit, source locking + stale-source rejection, idempotency + provenance ledger | **Migration-first** — 0157 applied 2026-07-27T02:01:29Z, *before* the merge at 13:12:34Z. Additive: one table + four SECURITY DEFINER functions (`search_path=""`); no backfill, no existing clinical object mutated. Commit RPC is **service-role only**. Reusable setup only — **minutes and outcomes never copied**; galvanic intensity forced to literal NULL. **`session_copy_operations` = 0 rows — no real copy has ever been performed.** Merge SHA `96b28d62a5f3b9acd67d00b24c80caebd6a66e5d`; Vercel prod `dpl_nZ6UBkGhK8vTAs8butVWwqNFXqmb` |
| **#479** | none (code-only) | **Deployed + enabled — human acceptance pending** | Phase A charting correction: ONE unified *Treatment observations & skin response* box; galvanic-intensity retirement; exact `0.733 seconds` thermolysis display; *Thermolysis pulse count* relabel; larger *Additional notes* | Reaction-driven analytics consume the unified representation; legacy `reaction_type` folded in so historical rows still surface. **Galvanic intensity history preserved — the column was not dropped**; only new writes and ordinary display drop it (`galvanic_ma` / `galvanic_duration_seconds` remain **active**). No migration, no data operation, no flag change. Merge SHA `3cabdcaa9e196afc45db63e98eb8ca72ef0a5051` |
| #477 | **0156** | Live | Conditional numbing notes — optional free-text kept only when `numbing_status='used'` | Migration-first. Nullable column, no default, no backfill, no CHECK/length cap, no RLS change |
| #476 | none (code-only) | Live | Charting usability polish — collapsed add-block CTA, split chip groups, larger notes | Opening or cancelling the add-block form performs **zero writes** |
| #475 | **0155** | Live | Inventory-backed probe-lot linkage — durable same-studio pointer into `record_keeping_sterile_items` | Migration-first. Pointer-only FK, `ON DELETE SET NULL`. Nothing backfilled; no RLS change. Legacy `probe_lots` stays dormant |
| #474 / #473 | none | Live (superseded by #478) | Containment of the unsafe whole-session copy + a safe in-form "Copy settings" prefill | Outcomes never copied; galvanic intensity excluded |
| #472 | **0154** | Live | Card add/replace studio notification from the `setup_intent.succeeded` webhook | Idempotent on the mode-scoped SetupIntent; additive nullable column, no backfill |
| #471 | none | Live | Plain-text bullets in Personal Notes | No rich text, no HTML |
| #470 | none | Live | "Create treatment plan" CTA from an appointment | Deep-links to the existing form; zero auto-create |
| #469 | **0153** | Live | Explicit per-service calendar colour | Six approved colours; rose/red reserved for clinical caution |
| #468 | none | Live | Edit a pinned note in place | Optimistic concurrency on the original text |
| #467 | none | Live | Backward-packed slot anchor + source-aware conflicts (11:30 scheduling) | — |
| #465 | **0152** | Live | Actual treatment overlap HARD; configured buffer SOFT and owner-overridable | Fixes the manual-override booking blocker. Overlap remains never bypassable |
| #457–#460 | **0134–0151** | Deployed; **capacity enabled on the controlled test studio only** | Practitioner-capacity foundation, availability, scoped blocks/breaks, atomic booking + move/reassign commands, tenant-consistency FKs | **`practitioner_capacity_enabled` is FALSE at Willow; `practitioner_capacity_booking_enabled` is FALSE on every studio** |
| #459 | **0140–0141** | Deployed; **enabled on the controlled test studio only** | Studio onboarding v2 + invitation reconciliation / single authoritative consent | 0141 makes `handle_new_user()` a NO-OP — nothing fabricates consent, no membership activates merely because an Auth user was created. Nudges + analytics deferred |

---

## Earlier wave

> Merge SHAs are recorded where captured at merge time. Earlier entries in this wave were
> merged to the production branch but their exact merge SHAs are not recorded here — verify
> via `git log origin/claude/build-hone-saas-hOex7` if an exact SHA is needed. Do not treat a
> missing SHA as "not shipped."

| PR | Migration | Live status | Impact | Safety notes |
|---|---|---|---|---|
| #357 | **0108** | Live | Treatment observation chips — structured, reliable toggle chips on charting | Additive; legacy chips backfilled from `comments` on edit (unedited rows keep unstructured chips) |
| #358 | none | Live | Client-facing SMS uses 12-hour time | Code-only; no schema; no SMS behavior change beyond formatting |
| #359 | **0109** | Live | Studio 12h/24h time-format preference | Additive column, default `12h`; existing studios → 12h; machine values stay 24h |
| #360 | **0110** | Live, **default OFF** | Postcare automation (opt-in auto-send on appointment completion) | Default `manual` = no behavior change on deploy; fail-soft; skipped if Resend key / postcare text missing |
| #361 | none | Live | Calendar action modal renders 12h (respects preference) | Code-only; time-format bug fix |
| #362 | none | Live | Scrollable calendar cards + drag render 12h | Code-only; time-format bug fix |
| #363 | none | Live | Booking drawer: override + exact clicked-time booking | Code-only; no booking-validation weakening |
| #364 | none | Live | Calendar internal scroll + mobile sticky rail (usability) | Code-only; layout |
| #365 | none | Live | Owner-only blocked-time editing from the calendar | Code-only; owner-gated server-side; members read-only |
| #366 | none | Live | Client portal: practitioner "Send portal link" + "Copy login URL" + resend rate limits | Reuses existing hashed/single-use/60-min issuance; studio-scoped; per-practitioner+client 3/hour limit; no raw token exposed |
| #367 | none | Live | Portal CTA in confirmation + reminder emails; login-page expiry copy fix (30m→1h) | Token-free `/portal/login?studio=slug` CTA; no clinical data; enumeration behavior unchanged. NOTE: the portal **verify** page still says "30 minutes" — a follow-up fix |
| #368 | none | Live | Multiple photo upload (per-file validation + EXIF strip + per-file status) | UI-only; server action/sanitize/storage/RLS byte-unchanged; no all-or-nothing, no silent loss |
| #369 | none | Live | Compact marketing-consent UI on public booking | Presentational; default unchecked + never prechecked + consent-send logic unchanged |
| #370 | **0111** | Live | Client portal access events + practitioner status card (last sent / last seen / pending tasks / recent activity) | Append-only, SELECT-only RLS, service-role inserts only; no token/URL/PII columns; fail-soft logging; public login untouched. Merge SHA `7d86d88caa9f6f8578c17f99b05d47a46cd5af63` |
| #371 | none | Live | Public booking "Back to previous result" availability navigation | Client-side history stack; stepping back re-fetches + re-validates that day's slots (no stale booking); server/query/tz unchanged. Merge SHA `f006517440eb3dd11500788510d0b10107835f53` |
| #372 | **0112** | Live | Public booking horizon 1–12 months | CHECK widened `(3,4,6)`→`(1..12)`; default 3 + existing values unchanged; scan cap + recurring-break window derive from a 12-month max so a long horizon never truncates. Merge SHA `2d4b809777d98a33981753312c13bfd40bfe0c92` |
| #374 | **0113** | Live | Admin Action Audit Log (`admin_action_events`, `/admin/audit`) — records sensitive operator actions (studio creation, ops-alert resolution, demo follow-up) | **Migration-first** (0113 applied + verified before merge). Append-only; RLS enabled with **no policies** (service-role reads + writes only); no FK (audit durability); **no token/URL/IP/email/clinical/payment columns**; metadata allowlisted + redacted; fail-soft logging (never blocks the admin action). No payment/email/SMS. Merge SHA `f49ffd255b70af5a091c830dc30b68ed0614b8ae` |
| #375 | none | Live | Postcare auto-send setting discoverability (nav "Forms & Policies" → "Forms & Postcare"; removed stale "no auto-send" copy) | Code/copy-only; the delivery-mode control + auto-send behavior are unchanged (still default `manual`, owner-only). No migration/payment/email/SMS. Merge SHA `b062af4bcef1cccd957ae14e801b0db07c199145` |
| #376 | none | Live | Admin audit log discoverability (dashboard card → `/admin/audit`) | Admin-UI/nav only; `/admin/audit` stays `isAdmin`-gated; audit schema/logging unchanged; not exposed to practitioner nav. No migration/payment/email/SMS. Merge SHA `dad662b12df066125ab6f9c94aa22d5a248166ce` |
| #377 | none | Live | Portal verify-page expiry copy fix (30 min → 1 hour) — **resolves the #367 follow-up** | One-line copy fix matching the real 60-min TTL; token generation/hashing/TTL/verify/enumeration all unchanged. No migration/payment/email/SMS. Merge SHA `69f3f02681baa5d46aa5fd35ccca1e62b189c7c5` |
| #378 | none | Live | Multi-studio-user robustness — never 500 on 0 or 2+ active memberships | `.maybeSingle()` (errored on 2+) replaced with an explicit count; 0 → `/no-access`, exactly 1 → unchanged, 2+ → clean chooser state (never auto-picked). No migration; **no RLS change** (a unique constraint would block the multi-studio goal). Merge SHA `fc6ecdaba5002c95293efbfefe3daf6fec5f2c2b` |
| #379 | none | Live | Multi-studio switcher + selected-studio persistence | httpOnly `hone_selected_studio` cookie (secure in prod, sameSite=lax), re-validated against active memberships every request; switch action verifies membership server-side before setting it; `/no-access?reason=multiple-studios` chooser + account-menu "Switch studio". **Cross-studio isolation adversarially verified (0 holes).** No migration; no RLS change. Merge SHA `5d716cf49e741710a1148f24bf5ac016cc595088` |
| #380 | none | Live | Mobile calendar = single-day vertical timeline (replaced the sideways-scrollable week grid) | Mobile-only (`md:hidden`); reuses the same loaded week data + drawers; date strip, prev/next day, tap-to-book exact time, now-line, floating +. Desktop week/month unchanged; **no booking server-action change**. No migration/payment/email/SMS. Merge SHA `80050ce2fe30fd013c73ccd6aa2f382120502379` |
| #381 | none | Live | Desktop in-context appointment preview drawer | Clicking a desktop appointment opens a **read-only** summary (client/service/time/status) + "Open full details" deep link, instead of navigating away. Read-only (no query/mutation); `/calendar/[id]` unchanged; mobile #380 + block editing unchanged. No migration/payment/email/SMS. Merge SHA `e858ecd5e2074d6ce3d3cd363d13e9a0016ca940` |
| #382 | none | Live | Desktop Google/Apple-style calendar toolbar (Today / ‹ › / date range / Week·Month) | Presentational; shared by week + month; renders links over existing `?view=/?week=/?month=` params; Day view deferred. Mobile #380 + preview #381 unchanged. No migration/payment/email/SMS. Merge SHA `f2a481bdc279ce65401fd6f81d200fb49ac233b3` |
| #383 | none | Live | Desktop calendar scroll cleanup — one clean vertical scroll | Removed `overflow-x-auto` + `min-w-[840px]`/`[760px]` (columns flex `minmax(0,1fr)`); sticky header/rail, now-line, card positioning unchanged. CSS/layout-only. No migration/payment/email/SMS. Merge SHA `5de5c4e80beac2c32314c2dd64b77f1baf7713cf` |
| #384 | none | Live | Charting: non-blocking aftercare prompt at "Done charting" | Warns when `sessions.aftercare_and_risks_explained_at` is NULL; "Mark aftercare explained" (existing toggle) or "Continue without marking" — **never blocks (emergency-safe)**; toggle hardened to explicit true/false. Stamp field already exists (0085). No migration; **no `mark_appointment_complete` RPC change**; no payment/email/SMS/postcare change. Merge SHA `39a63ab44843df260719991f0aff776c865b7628` |
| #385 | none | Live | Charting: server-side treatment-area canonical validation | New writes require a canonical area (flat `AREAS`, incl. "Full face", case-normalized) **or** explicit "Other"/custom; unflagged arbitrary text rejected. All four session-block write paths + copy path covered; **legacy rows never validated on read, never rewritten**. App-layer, no migration/RLS/payment/email/SMS. treatment_plans multi-area deferred. Merge SHA `1ecaec383c530daf1471c14c42817d906a5da46b` |
| #386 | none | Live | Charting: probe-lot studio verification on write | A client `probe_lot_id` must be a well-formed UUID belonging to the caller's own studio (studio- + RLS-scoped), else rejected; free-text/manual `probe_lot_number` preserved and **never made inventory-verified**. Live block flow unchanged. App-layer, no migration/RLS/payment/email/SMS. Merge SHA `475e08a15db3b189c173305035a3b4df273ef1c8` |
| #391 | **0114** | Live | Charting: audited soft-delete ("Remove pass") for one pass in a multi-pass area | Adds `deleted_at`/`deleted_by`/`delete_reason` (+ active partial indexes) to `electrolysis_entries` + `laser_entries` (mirrors `session_blocks` 0019); removes a mistaken pass without hard-deleting the clinical record. Additive; nullable; no backfill; read paths filter voided passes. No payment/email/SMS. Merge SHA `5012612c563aac6f6d543c44e491f307b4cd2d4f` |
| #393 | **0115** | Live | Entry hard-delete hardening (closes an independent-audit finding) | Drops the residual `for delete to authenticated` RLS policy on `electrolysis_entries` + `laser_entries` (kept by 0087) and revokes `truncate, delete` from `anon, authenticated`, so passes are soft-delete-only. **Hardens** RLS (does not weaken); `service_role` unaffected. Policy/grant-only. No payment/email/SMS. Merge SHA `e5ef80ffe88585fe00935c8cb8dcda73197f5c08` |
| #395 | **0116** | Live | Calendar-feed credential hash-only at rest (closes an independent-audit finding) | Drops the raw `practitioners.calendar_feed_token` column + its unique index; keeps `calendar_feed_token_hash` (0079), which the feed route authenticates by. Existing subscriptions preserved — no forced reconnect; raw token surfaced only once at generate/rotate. Applied **code-first** (destructive drop of a written column). No payment/email/SMS. Merge SHA `b31fd3993047c36d8e11d63e737fbaf7b6fd0914` |
| #397 | **0117** | Live | `session_audit` cross-tenant INSERT hardening — a new audit row's `session_id` must belong to the caller's studio | Policy-only (INSERT `WITH CHECK` tightened); **migration-first**; closes a confirmed cross-tenant integrity-write (Studio A → Studio B session audit); 0 historical mismatches; no code change; no payment/email/SMS. Merge SHA `91539167ac723150d3a6e9efeb55298775d6190e` |
| #398 | **0118** | Live | Intake terminal-state immutability — submitted/reviewed `client_intake_forms` answers/status/attribution immutable to authenticated members | `BEFORE UPDATE` trigger (service-role exempt); **migration-first**; draft/review/reissue flows unaffected; closes a same-tenant clinical-record integrity defect; trigger-only, no code change; no payment/email/SMS. Merge SHA `11d5e582c384134c2092f696cefc41802a54303c` |
| #399 | **0119** | Live, **flag OFF** | Clinical Record — **Phase 1 finalization boundary** (draft/finalized lifecycle, `record_origin` native/legacy, append-only `clinical_record_snapshots`, aggregate-freeze triggers, native-only flag-gated `finalize_session` RPC) | Additive + inert; **migration-first**; `clinical_finalization_enabled` **default OFF**; 59 legacy sessions untouched (metadata-only `record_origin='legacy'`); **production-exercised** on Sam's controlled studio (1 synthetic finalize → immutable v1 snapshot; all lock attempts rejected even via service-role; flag then disabled, record stayed locked). No payment/email/SMS. Merge SHA `d84180fcd6d2fd2a9215fc046ed17e5f77f6ef1f` |
| #400 | **0120** | Live, **flag OFF / dormant** | Clinical Record — **Phase 2 corrections & amendments BACKEND** (append-only `clinical_record_amendments` + dedicated PHI-safe `clinical_audit_events` + snapshot lineage + atomic correction RPC + the narrow session-scoped correction permit) | Additive + inert; **migration-first**; `clinical_corrections_enabled` **default OFF** (separate from Phase 1); the Amend/Correct/History UI renders nowhere while OFF; **0 production corrections/amendments created**; **customer workflow PARKED** (generic 3-field UX unsuitable for rollout; a correction allow-list + full-chart workspace not queued). No payment/email/SMS. Merge SHA `6de3b5dfef8bc929836be3cbacef4dd1b4c61cf7` |
| #401 | none | Live | Mobile calendar fixes (Chloe): exact-date navigation, Today visibility, date strip, Book/Block chooser + block time entry | Code-only; mobile calendar UX; no booking server-action weakening; no migration/payment/email/SMS. Merge SHA `cfb98d27d3f7b8497b0007021dead14adc004e69` |
| #402 | none | Live (dormant behind the OFF clinical flags) | Amendment/correction-path **reliability & observability** — PHI-safe correlation/request ids + staged server-action diagnostics + discriminated `{ok, errorType, requestId}` result contract + persistence-verified success + prominent accessible "Nothing was saved" error UI (reference id, fields retained on failure, clears only after confirmed success, duplicate-submit protection) | App code + tests **only** — **no migration, no schema change, no flag change, no production data change**. Reachable only when `clinical_corrections_enabled` is ON (OFF everywhere → dormant). Adds full real-path coverage: browser E2E (UI → server action → PostgREST RPC → DB → history), server-action unit tests, a **PostgREST named-argument** integration test (exact `supabase.rpc(fn, { named })` shape; PASSES — a CI gap the positional DB test left open), and DB integration. **Original silent failure NOT reproduced; no confirmed historic root cause** — closes the observability gap so any recurrence surfaces a prominent error + a correlatable PHI-safe request id. No payment/email/SMS. Merge SHA `e84ff94a035702a18997a277312403142971ebff` |
| #404 | **0121/0122** | Live, **DORMANT** | Google Calendar — **Phase A** connection & OAuth foundation (`calendar_connections` non-secret metadata + member-SELECT RLS; `calendar_connection_secrets` encrypted refresh token, RLS + no browser policy + REVOKE; `google_oauth_states` hash-only state + encrypted PKCE, single-use; four studio flags, all default OFF) | Additive + **dormant**; **migration-first** (0121/0122 applied ahead of merge). Direct REST via `fetch` (no `googleapis`); least-privilege Phase-A scopes (**no** event scope). Nothing connects/runs while the connection flag is OFF. Subsequently **production-exercised** on Sam's controlled studio only (1 connection created; state consumed once; token encrypted + peer-unreadable); Willow not connected. No payment/email/SMS/booking change. Merge SHA `1bfdf7b` |
| #405 | none | Live | Willow P1-A — **signed-consent visibility**: practitioners can open the full signed consent form (`template_body_snapshot`) + see the photo-consent choice (accepted/denied + label snapshot) via a signed-consent viewer + a photo-consent summary banner | Display-only — was a pure DISPLAY gap (data already stored + RLS-readable; the query omitted the columns and the card showed only a badge). No portal/schema change; **no migration**; no consent data modified. Merge SHA `4db7bd2` |
| #406 | **0123** | Live | Willow P1-B — **remove an incorrectly-recorded treatment area**: an atomic aggregate soft-delete `SECURITY DEFINER` RPC `soft_delete_session_area` (soft-deletes the block + its block-scoped electrolysis passes + block-scoped images + a `session_audit` `area_removed` event in one transaction) + a wired Remove-area button (attached-data warning, required ≥10-char reason, draft-only) | Additive (one function); **migration-first** (0123 applied ahead of merge). `search_path` pinned; EXECUTE authenticated-only; studio derived from the row; **rejects finalized/void**; **NO hard delete**. The RPC was **NOT** called against real data (0 `area_removed` events) — production data unchanged. No payment/email/SMS. Merge SHA `306a473` |
| #407 | **0124** | Live, **DORMANT** | Google Calendar — **Phase B1** dormant outbound-sync schema & queue foundation (`calendar_event_links` mapping + durable `calendar_sync_outbox` + service-role-only `claim_calendar_sync_op` / `record_calendar_sync_result` RPCs) | Additive + **dormant**; **migration-first** (0124 applied to production + verified, then merged). **No runtime behavior**: no worker, no enqueue path, no trigger enqueues, no outbound-sync UI, no Google API call, no event scope requested; `calendar_event_links` + `calendar_sync_outbox` are empty (0 rows). Security: outbox default-deny (no anon/authenticated grants); event_links member-SELECT + no browser writes; claim/result RPCs executable only by `service_role`/`postgres`. All Google sync flags OFF; Willow not connected. **NOT production-exercised.** No payment/email/SMS/booking/availability change. Merge SHA `bcccce42f99120728da9c4e26033e848a346fac2` |
| #412 | **0125** | Live, **DORMANT** | Google Calendar — **Phase B2.3-a** intent-gated enqueue + claim boundary (durable-intent enqueue trigger over the 0124 outbox, repair primitives `repair_bump` / `repair_enqueue_orphan_link_delete`, `calendar_sync_queue_health` view; enqueue-time INTENT gate distinct from claim-time HEALTH gate; global worker control default OFF) | Additive + **dormant**; **migration-first** (0125 applied + verified, then merged). No worker drains the outbox; no intent-eligible studio in production. **#412 (0125) is DB-only — it makes no Google call and, by itself, requests no event scope** (B2.4/#424 later requested the destination-derived `calendar.app.created` scope). **NOT production-exercised.** No payment/email/SMS/booking/availability change. |
| #424 | **0131** | Live, **DORMANT** | Google Calendar — **Phase B2.4** dual-destination + destination-derived scope (retires broad `calendar.events`; **dedicated** `calendar.app.created` "Hone Appointments" calendar vs **existing-owned** `calendar.events.owned`; NULL-safe destination CHECKs) | Additive + **dormant**; **migration-first** (0131 applied + verified, then merged). **Production-exercised once on Sam's controlled studio**: one empty "Hone Appointments" destination calendar created (grants app.created=1 / events.owned=0 / broad=0; **zero events**); all sync flags OFF; Willow not connected. No payment/email/SMS/booking/availability change. Merge SHA `8a25df6` (Vercel prod `h9b58cLZtJ4w3MMrY5WU959w5tDB`) |
| #428 *(row written pre-merge as "_pending_, unmerged")* | **0132** — **APPLIED to production migration-first 2026-07-16** | **Deployed, DORMANT** *(the original "AUTHORED, not deployed / migration NOT hosted-applied / do not merge" wording below is **superseded** — 0132 is one of the 157 applied production versions)* | Google Calendar — **Phase B2.3-c1** dormant outbound event operations + provider reconciliation | Implements the dormant event-operation layer (serializer, deterministic 57-char `hone1…` event id, private `hone`/`hlk` correlation marker, execution-time stale fence, `event.create` / placeholder `event.update` create-and-bind / real `event.update` / `event.delete`, ambiguous/409/cancelled reconciliation, missing-remote **fresh-lifecycle rotation**, GET-verified placeholder orphan recovery) + migration **0132** (transactional service-role `calendar_event_link_transition` RPC bind/update/mark_deleted/rotate + corrected placeholder `last_hone_version=0` + placeholder-aware cleanup). **No route, no cron, no worker heartbeat, no worker activation, no real Google call.** `record_/claim_` + outbox authority unchanged (claim→handle→record). Two approved register amendments: machine-only private marker; GET-verified placeholder cleanup. Repo max `0132` / hosted `0131`; **migration NOT hosted-applied**; `worker_enabled` + all studio flags OFF; Willow unconnected. **Held for independent review + migration-first authorization — do not merge.** |
| #426 | **none** (no migration) | Live, **DORMANT** | Google Calendar — **Phase B2.3-b** reconciliation sweep + heartbeat + dead-row alerting + authenticated `/api/cron/calendar-reconcile` route | **Code-only — no migration** (hosted max stays **0131**); orchestrates the **existing** 0124/0125 repair RPCs + queue-health view (bounded per-studio drift detector for the intent-OFF window). The route is **CRON_SECRET-protected but NOT cron-registered** (no `vercel.json` cron) and is **not gated on `worker_enabled`**; it calls **no Google API** and builds no event transport. Two Upstash ownership-token coordinators (main reconciliation + dead-alert), never held simultaneously; fail-closed locks/cursors, fail-open heartbeat/alerts. Production has **zero** intent-eligible studios → the sweep is inert (0 outbox/link rows produced). All sync flags OFF; worker OFF; Willow not connected; **no activation**. **NOT production-exercised.** Next: **B2.3-c** outbound event execution (not built). Merge SHA `f664f0f` (Vercel prod `5h3RGRmWEjrMUjg72N4ox73wGPoG`) |
| #429 | **none** (no migration) | **Live, DORMANT (merged + deployed).** Merge `05ae9b8`, verified an ancestor of the deployed production HEAD `96b28d6`. **Also now cron-registered** at `30 9 * * *` by PR #430 — it runs daily and finds no work (worker flag off; every studio sync flag false). *(Earlier wording — "Reviewed + CI-green at impl head `f9ce6db`; becomes DEPLOYED DORMANT when the docs-only descendant merges" — is superseded and was counterfactual: the successor PR #430 was not docs-only, it changed `vercel.json`.)* Runtime no-work validation evidence remains **unknown pending verification**.** (operational completion — merge-commit CI, production deploy, and the separately-recorded authorized no-work validation — recorded in the PR closeout, not here) | Google Calendar — **Phase B2.3-c2** authenticated, bounded worker-drain route `/api/cron/calendar-sync` (Node runtime, `force-dynamic`, `no-store`): `CRON_SECRET` auth before any admin client/claim + PHI-free 400 on any caller query param; drains `claim_calendar_sync_op` → `handleCalendarSyncJob` (c1 operations map, via the ONE server-only seam `worker-runtime.ts`) → `record_calendar_sync_result`, distinguishing handler result from durable record status; fixed bounds (batch 5, ≤3 batches/≤15 claimed; the 50s value is the job-**admission window**, with the platform ceiling pinned as a literal `export const maxDuration = 180` in the route — never via `vercel.json` — for ≥120s completion headroom); fail-open `gcal_worker:last_run` heartbeat + bounded fail-open alerts | **Code-only — no migration** (repo max stays **0132**). Token-refresh coordinator is a NEW **Upstash**-backed `RefreshCoordinator` (`gcal_refresh:lock:<connectionId>`, fail-closed, fixed 120s TTL) replacing the un-wireable `createPgRefreshCoordinator` — a narrow per-connection mutex, **not a third worker coordinator**; **no `pg`, no new env var, no new infrastructure**. Shared process access-token cache for the token manager + the c1 401 invalidator. The production connection store now preserves refresh-secret read errors (typed `RefreshSecretReadError` → `retry_transient`, never `markReconnectRequired`), so a transient DB error can't force `reconnect_required`. **NOT cron-registered** (c3 owns scheduling), `worker_enabled` stays false, all studio sync flags stay false, `calendar_sync_outbox` + `calendar_event_links` remain 0, **no real Google appointment-event call**, Willow not connected. **NOT production-exercised.** Next: **B2.3-c3** scheduler registration (separately authorized). |
| #430 | **none** (no migration) | **Live, DORMANT** (merged + deployed) | Google Calendar — **Phase B2.3-c3** scheduler & observability readiness: registers the two calendar routes in `vercel.json` as **DAILY** crons — `/api/cron/calendar-reconcile` `0 9 * * *` + `/api/cron/calendar-sync` `30 9 * * *` (staggered after the 08:00 `materialize-recurring-breaks` cron; single source of truth `lib/cron/calendar-cron-schedule.ts`, pinned by `cron-config.test.ts`). **Registration did NOT activate synchronization.** | **Merged + deployed DORMANT.** Final head `d07147b`; merge SHA `4e4fe5b` (Vercel prod `4PTuoeHChfKXiVkTK5CmREeqAhbF`). **Code-only — no migration** (max stays **0132**). Dormancy held: `worker_enabled=false`, every studio outbound-sync / inbound-busy / two-way-update flag **false**, no queue row seeded, **no real Google appointment-event operation occurred**, Willow unconnected. **Scheduled-run evidence (accurate — NOT a validation-pass claim):** the schedules registered successfully with no failure alert, no mutation, and no Google appointment-event operation; direct historical HTTP execution evidence was unavailable (Vercel CLI log retention expired) and the production Upstash heartbeat values were not locally available (retrieving production secrets was intentionally avoided), so **C2 remained formally merged-unvalidated at that checkpoint — an observability evidence gap, not evidence of a runtime failure**. |
| #431 | **0133** | Live | Practitioner **Move appointment** — atomic **same-record** move of a confirmed, future appointment (NOT cancel+rebook: id + client/service/practitioner/payment/clinical/token/calendar-link all preserved; only `starts_at`/`ends_at` change). One SECURITY DEFINER RPC `practitioner_move_appointment` (service-role-only; active-practitioner check; `FOR UPDATE`; optimistic concurrency on expected endpoints; 23P01 conflict rolls back). One shared responsive workflow (mobile bottom sheet / tablet+desktop modal) from the detail page + desktop preview drawer; own-reservation slot exclusion; best-effort PHI-free "time changed" email after commit | **Additive** (one RPC; no column/data/trigger change); **migration-first** (0133 applied + verified, then merged). No Google/Stripe touch; no sync flag/worker/connection changed; **no production appointment moved by this PR** (0 `action='moved'` audit rows; anon denied `42501`). Merge SHA `ca7a708` (Vercel prod `DwcvmJVXyfLc2JGypU7RuvegzgVp`) |
| #433 | **none** (no migration) | Live | **Cancellation email names who cancelled.** The practitioner-facing cancellation email now shows **Client**, **Cancelled by** (`<name> — Client / Practitioner / Studio owner / System`), **Original time**, **Service + duration**, and **Reason**, with a clearer subject (`Appointment cancelled by <name>: <service> on <Month Day>`) | Code-only; the actor is **derived server-side** — public token cancel = the appointment's own client record; practitioner cancel = the authenticated practitioner/owner (display name, safe email fallback) — never from the browser. Only display fields (no IDs/tokens/raw audit JSON/notes). **No cancellation-state or payment change.** Merge SHA `a0eefd4` (Vercel prod `GGD1rUPDWn1S1h16RudhrCsVByvs`) |
| #434 | **none** (no migration) | Live | **Move appointment — owner custom-time override.** Adds a studio-**owner-only** *Custom time* mode (a manually entered studio-local time that **may be outside regular operating hours**, gated on an explicit availability-override acknowledgement), plus **server-side revalidation** of standard moves against the current generated slot list (by start instant). One shared responsive dialog on mobile/tablet/desktop | **Code-only — no migration** (repo == hosted max stays **0133**; no `0134`). Both modes use the **same atomic 0133 RPC** `practitioner_move_appointment`; same appointment ID + client/service/practitioner/payment/clinical/calendar-link relationships preserved; custom time does **not** bypass confirmed-appointment / buffer / timed-block / recurring-break / full-day-blockout conflicts (`23P01` rolls back). Owner role resolved server-side (never a browser `isOwner`). **No production appointment moved during verification;** Google worker + all sync flags OFF; Willow unconnected; no Stripe/payment change. Merge SHA `89e77f0` (Vercel prod `HiqfCZU2NYSwL3wWw8Nb2hGEKvam`); PR #433 cancellation-email actor naming remains deployed. **Deployed, awaiting Chloe's controlled custom-time human test.** |

## Migration-first PRs in this wave

The migration-bearing PRs (#357/0108, #359/0109, #360/0110, #370/0111, #372/0112, **#374/0113**,
**#391/0114**, **#393/0115**, **#397/0117**, **#398/0118**, **#399/0119**, **#400/0120**,
**#404/0121+0122**, **#406/0123**, **#407/0124**, **#412/0125**, **#424/0131**, **#431/0133**) were
shipped **migration-first**: the migration was applied to production and verified **before** the
code merge/deploy. **#395/0116** is the one deliberate exception — a **code-first** apply, because
it destructively drops a column the deployed code still wrote, so the code that stopped writing it
was deployed first, then the column dropped. (**#401**, **#402**, **#405**, and **#426** are code-only — no migration; #426 = Google Calendar B2.3-b reconciliation, hosted max stays 0131. The non-calendar tail 0126–0130 shipped in this window is enumerated in the [migration ledger](./migration-ledger.md).)
See [../runbooks/migration-first-process.md](../runbooks/migration-first-process.md).

## Provenance

This changelog was reconstructed at the 2026-07-08 documentation repair from PR reports and
git history; it is not auto-generated. A generated changelog (from `git log` / merged PRs) is
a documentation follow-up.
