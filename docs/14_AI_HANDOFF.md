# 14 AI handoff

**If you are an AI agent continuing work on Hone, read this first.**

## Current production status (as of the probe-lot auto-populate feature)

- **Charting: probe lot/batch auto-populate + confirm** (Chloe feedback, Feature A — the deferred half; **no migration**). New server helper `getLatestProbeLotByProbeKey(studioId)` returns a studio-scoped `probe_key → latest lot` map from `session_blocks` (prefers confirmed, then newest; excludes null probe_key / blank lot / deleted). The charting form (`block-setup-form.tsx`) auto-populates the lot field **unconfirmed** for the selected probe ("Suggested from last use. Please confirm this lot/batch is correct." + "Confirm lot/batch"); editing un-confirms + marks manual so a probe switch never clobbers a typed value; no prior lot → blank. **Replaced** the old studio-wide sterile-item suggestion on this field. Reuses `probe_key`/`probe_lot_number`/`probe_lot_confirmed` — no schema change. **No payment/env/Stripe/live-payment change; `check-stripe-gates` 15 PASS.** Production DB stays **0102**.

## Current production status (as of the pulse-delay charting field)

- **Charting: pulse delay between high-frequency pulses** (Chloe feedback, **PR A of a 2-part split**; the probe lot/batch auto-populate is **deferred to PR B**, no migration). **Migration 0102** adds `electrolysis_entries.pulse_delay_seconds numeric(4,2)` nullable + a range CHECK (`null or 0.03–1.90`) — additive, existing/single-pulse rows stay valid. When `pulse_count > 1`, both live charting forms show a "Pulse delay" field (seconds, default 0.5, range 0.03–1.90) that saves through both write paths and displays in `entry-row.tsx` ("0.50s delay"); null when `pulse_count <= 1`. **No payment/env/Stripe/live-payment change; `check-stripe-gates` 15 PASS.** **Repo migration max → 0102** (apply to prod via the migration-first process after merge, when ready — prod is still at 0101 until then).

## Current production status (as of the intake-submit notification)

- **In-app notification on intake submit** (no migration, no email/SMS). `submitIntakeAction` now fires the PR #164 `recordPractitionerNotification` helper (`intake_submitted` event, added to the helper allowlist + the `PractitionerNotificationEventType` union) **in the winner branch of the atomic `in_progress → submitted` transition**, so it notifies the studio exactly once per submission (resubmit/retry never double-notifies). Studio-scoped, studio-wide visibility (`practitionerId: null`), RLS-gated to the studio. Privacy-safe: title "Intake submitted", body "{client name} submitted an intake form." (fallback "A client…"), href `/clients/{client_id}/intake` — no intake answers, no token, no health columns. Fire-and-forget: never rolls back the submit; insert failure logs a warning ops alert. **No payment/env/Stripe/live-payment change; `check-stripe-gates` 15 PASS.** Production DB stays **0101**, live payments disabled.

## Current production status (as of the PR C runbook)

- **Controlled live-payment env-flip + first-live-charge runbook — FINALIZED (docs-only, PR C).** `docs/16 §17.14` now holds the authoritative operator runbook: go/no-go checklist, legal/accounting sign-off requirements (live receipt + client-facing card-authorization wording), Willow live Connect readiness, live webhook endpoint + events, exact Vercel Production env vars + flip order, first-live-charge procedure, read-only SQL, surfaces to watch, rollback/kill-switch, no-go conditions, out-of-scope. **Documentation only — no env, no Stripe, no payment, no webhook endpoint, no code/copy/migration/SDK change; live payments remain OFF.** The env flip + first charge are **separate operator actions** taken after this merges, after **legal/accounting sign-off** (mandatory), and after every go-condition is met. **If legal reworders the receipt/card-auth copy, a separate copy-only PR must merge before the flip.** Production stays **0101**, live disabled.

## Current production status (as of the internal-copy fast-follow)

- **Internal payment copy fast-follow** (post-#323; **copy/tests/docs only — no runtime/env/migration/Stripe change; live payments still disabled**). Neutralized the ~52 internal practitioner/fee-card "test charge"/"test-mode"/"no live card" strings that #323 deferred, so nothing reads false after the #324 env flip. `session-payment-prepare-card.tsx` + `ManualFeeChargeCard.tsx`: labels → mode-independent ("Run test charge"→"Run charge"), reassurance warnings → mode-neutral cautions (dropped false-in-live "no live card"/"test-mode" claims). `PaymentsSettings.tsx`: neutralized the checklist labels; the mode-gated **test-mode banner is preserved** (`showTestBanner`). The #323 mode-gated portal card-authorization copy + the settings banner remain the explicit test-mode warning while live is off. Safety-net copy-map tests REPURPOSED to assert neutral/mode-safe strings. **This narrows the #324 gate:** internal copy is no longer a blocker — the remaining copy precondition is legal/accounting sign-off on the **client-facing** live receipt + card-authorization wording only. Production **0101**, live disabled.

## Current production status (as of PR #323)

- **Live-payment runtime/webhook/receipt/copy relaxation** (PR B; **still live-disabled, inert until #324**). The money paths are now **live-CAPABLE**: every dormancy guard changed from hardcoded `stripe_livemode = false` to gated on `inferStripeLivemode()` — with the env still test (`STRIPE_ALLOW_LIVE_MODE` unset → `inferStripeLivemode()=false`), behavior is byte-identical to before. Charge/refund removed their env early-returns (env/key gate at `getStripe()` remains); lineage guards compare `inferStripeLivemode()`/`livemode` (mode-consistency preserved); receipt is live-capable; webhook `shouldIgnoreLiveModeEvent` MODE-MATCHES (process events matching the deployment mode, ignore mismatched); prepare-inserts write `inferStripeLivemode()`; `hone_environment` metadata dynamic; client-facing portal card-authorization copy + dashboard "Live payments On/Off" are mode-aware. **No migration, no env, no Stripe key, no new Stripe SDK call site (`check-stripe-gates` stays 15 PASS).** Safety-lock (`live-mode-disabled`/`-blockers`/`-gate-prep`) REPOINTED to assert the env-gated model. **#324 GATE: live receipt wording + client-facing card-authorization copy need legal/accounting sign-off before the env flip.** Deferred to a copy fast-follow: ~52 internal practitioner/fee-card "test charge" labels. Production stays at **0101** (DB), live payments disabled.

- **Live-payment DB readiness — migration `0101`** (first live-enablement DB step; **live payments remain disabled**). First PR in the series to change the production DB: repo migration max **0100 → 0101** (apply to prod via migration-first after merge). (1) Replaced `payment_charge_attempts_livemode_false_check` (`stripe_livemode = false`) with the narrower `payment_charge_attempts_live_requires_account_check` (`stripe_livemode = false OR stripe_account_id is not null`) — live rows are now *representable* but a malformed live row (no account) is still rejected; `stripe_livemode` default stays `false`. (2) `claim_session_payment_charge_attempt` relaxed: removed **only** the `stripe_livemode <> false → not_ready` block (0083 body otherwise verbatim; all other guards + `service_role`-only grant unchanged). **Inert:** prepare-insert still writes `stripe_livemode := false`, the executor still returns `live_mode_blocked`, `STRIPE_ALLOW_LIVE_MODE` unset — no live row is created, no live charge runs. `manual_fee_charge_attempts_livemode_false_check` untouched; no runtime/webhook/env/Stripe-SDK/UI change. `live-mode-disabled` safety-lock passes; `check-stripe-gates` 15 PASS. Pinned by `tests/migrations/0101-*.test.ts` + `tests/db/livemode-ledger-readiness.db.test.ts`. **Prod migration max after apply: 0101.**

- **Harden Stripe write gates vs renamed clients + fix docs/16 runbook bugs** (pre-live-enablement; **no migration, no env, no Stripe SDK call change, live payments remain disabled**). (A) `check-stripe-gates.mjs` gains a **`getStripe-binding`** gate: every `getStripe()` call must bind to `const stripe = getStripe()` — a renamed/destructured/inline client (which would evade the `stripe.<ns>.<method>` catch-all, e.g. `const s = getStripe(); s.transfers.create()`) now **hard-fails**. With `stripe` enforced, the catch-all generically denies every dangerous mutating resource (transfers/payouts/subscriptions/invoices/applicationFees/charges/checkout) while leaving reads alone. The 14-line inventory (create 1 / cancel 1 / refunds 1 / charges 0 / checkout 0 + non-money + no-unclassified) is byte-unchanged; binding is a **15th** PASS line. (B) docs/16 fixes: §17.12 `STRIPE_PUBLISHABLE_KEY`→`NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`; §17.13 stale "migration max 0099"→derived-max (currently 0100). Tamper tests pin the evasions. `live-mode-disabled` safety-lock still passes; production stays at **0100**.

- **Charge descriptions by charge_reason + requires_action cancel** (pre-live-enablement; **no migration, no env, live payments remain disabled**). (A) New pure `lib/billing/charge-description.ts` `buildChargeDescription(row)` — the PaymentIntent description is now accurate per `charge_reason` (no-show / late-cancel fees no longer read "Session payment for session null"; fee rows have `session_id = null`); contains only the reason + an internal UUID, never PII. (B) `requires_action` no longer maps to terminal `failed`: a shared `finalizeRequiresActionPaymentIntent` (both create + reconcile paths) **cancels** the PI (one new `paymentIntents.cancel` site) so Stripe can't later succeed while Hone says failed (webhook reconciles only from `ready`/`pending_stripe`); if the cancel fails, it raises a critical alert and returns `needs_manual_review` with the row left `pending_stripe` (no new enum state). (C) `check-stripe-gates.mjs` gains an intentional `paymentIntents.cancel` pin (exactly 1) → **14 PASS**; create/refunds/charges/checkout = 1/1/0/0 unchanged; catch-all intact. Pinned by charge-description + session-payment-charge-source + payment-failure-persistence + check-stripe-gates tests. `live-mode-disabled` safety-lock still passes; production stays at **0100**.

- **Explicit live-mode guard for `setup_intent.succeeded`** (pre-live-enablement hardening; **no migration, no env, live payments remain disabled**). `setup_intent.succeeded` was the only card-WRITE webhook path lacking a live-mode dormancy guard — a live event would have written `client_payment_methods` even though live mode is structurally disabled. Now `handleSetupIntentSucceeded` (app/api/stripe/webhook/route.ts) calls the shared `shouldIgnoreLiveModeEvent` (now exported from lib/billing/payment-webhook-reconciliation.ts, the same guard the four money handlers use) as its FIRST step: a live event records the `stripe_webhook_livemode_event_ignored` warning ops alert and returns a sanitized `{livemodeEventIgnored:true}` summary **without throwing** (marked processed → idempotent, no retry storm) and **writes nothing / calls no Stripe API**. Test-mode events fall through unchanged. No Stripe write-path change; `check-stripe-gates` stays 13 PASS; the live-mode-disabled safety-lock still passes (this ADDS dormancy). Pinned by tests/lib/billing/payment-webhook-reconciliation-source.test.ts. Production stays at **0100**.

- **Supervised pilot readiness checklist + inspection-artifact fixes** (**no migration**). A 12-dimension read-only readiness audit found **0 P0**. Shipped: (1) new **`docs/21_SUPERVISED_PILOT_CHECKLIST.md`** — go/no-go, per-studio must-configure, pre-pilot verifications, day-1 test plan, intentionally-disabled list, and a **Data recovery** subsection (Supabase PITR/backups, plan-tier, RPO/RTO, who-can-restore, restore drill); states plainly live payments remain disabled. (2) `getClientProcedureRecords` sessions query now filters `.is("deleted_at", null)` (mig 0013) so **soft-deleted sessions no longer appear in Procedure Records / the inspection print/export** (lot traceability was already block-filtered). (3) The unfiltered procedures **print** shows a truncation notice ("Showing the most recent 30 records. Filter by client for a complete procedure log.") at the cap; cap is now the named `UNFILTERED_PROCEDURE_RECORD_LIMIT` (value 30, unchanged). (4) Returning-client booking copy no longer promises "book follow-up sessions" → "manage your upcoming appointments." **CRON_SECRET** documented in the checklist only (not added to the env-gate, to avoid a build-breaking false failure). **No migration/RLS/schema/payment/live/env/cron-behavior change.** Pinned by procedure-record-filter + pilot-readiness-318 + the new procedure-records-soft-delete.db test. Stripe gates 13 PASS; production stays at **0100**.

## Current production status (as of PR #317)

- **Record Keeping pilot QA polish** (post-#316, **no migration**). A read-only QA pass found 0 P0/P1; shipped the small polish items: (1) `SterilePrint` appends a **print-safe expiry marker** to the Expiry date — "(expired)"/"(expires today)"/"(expires soon)" via `supplyExpiryPrintMarker` + `todayInTz(studio.timezone)` (so the printed/exported inspection record flags expiry; color-independent). (2) Export README notes expiry is derivable from `expiry_date` (CSV schema unchanged). (3) **"expires today" parity** with disinfectants — `supplyExpiryState` gains a `"today"` state + a shared `supplyExpiryLabel` used by Records badges, the dashboard card, and (lowercased) the print marker; `"today"` folds into the "expiring within 30 days" banner count. (4) A db-integration test for `getExpiringSterileItems` (RLS-scoped: expired+today+within-horizon incl. boundary; excludes null/future-beyond-horizon/other-studio). **No migration/RLS/schema change; copy-last still never copies `lot_number`; manufacturer custom values still round-trip; no payment/live change.** Pinned by supply-expiry + probe-expiry-copy-last + the new expiring-sterile-items.db test. Stripe gates 13 PASS; production stays at **0100**.

- **Probe/sterile expiry reminders + copy-last + manufacturer dropdown** (Chloe feedback, **no migration**). UI-layer feature over the existing `record_keeping_sterile_items` (which already has `expiry_date`, free-text `manufacturer_name`, `lot_number`). (1) Manufacturer is now a dropdown (Protec/Ballet/Sterex + "Other" free-text) — `ManufacturerPicker` submits one `manufacturer_name` via a hidden input; column stays free text so legacy values round-trip on edit. (2) "Copy last entry" on the Add form prefills from the latest loaded record but **never `lot_number`**. (3) Records rows show expiry state (expired = red + "Expired"; ≤30 days = amber + "Expires soon") + a summary banner (N expired · M expiring), via pure `lib/record-keeping/expiry.ts`, studio-local `todayInTz`. (4) A studio-scoped `getExpiringSterileItems` query feeds a dashboard **"Supplies expiring"** card (no email/SMS/push, no cron; hidden when clear). **No migration, no RLS/schema change, studio isolation unchanged, no payment/live change.** Pinned by tests/lib/record-keeping/supply-expiry.test.ts + tests/app/records/probe-expiry-copy-last.test.ts. Production stays at **0100**.

- **Storage.objects isolation audit + manual-check reminder** (pre-live review, **no migration**). A fresh evidence-based audit of Supabase Storage / `storage.objects` (before real client files) found **0 P0/P1 blockers**: single `treatment-images` bucket private (0092/0093, verify-production-asserted); `storage.objects` is **service-role-only** (0093 drops the 0092 authenticated policies, deny-by-default; db-lane pinned); server-derived object keys; signed URLs gated by studio+client ownership + `validateTreatmentImagePath`; soft-delete only; export excludes binaries/paths. Only gap = verification precision: `storage.objects` policies aren't introspectable from the linked query role, and the `verify-production.mjs` **printed** manual-checks section omitted the storage.objects dashboard check. PR #315 adds that line (**print-only**, no logic change) + sharpens docs/16 §17.13 (service-role-only; no authenticated/anon access to treatment-images; no foreign-bucket OR-combine). Pinned by `tests/scripts/verify-production.test.ts`. **No migration, no RLS/storage-policy change, no service-role broadening, no runtime/payment/live change.** Production stays at **0100**; live payments disabled.

- **PR #314** — derived `verify-production.mjs` migration max from `supabase/migrations/` (no more stale `0099`; resolves to 0100) + added `components/` to the `check-stripe-gates.mjs` scan scope (gate result unchanged: money 1/1/0/0 + non-money + catch-all) + fixed a stale `lib/stripe/server.ts` comment. Tooling/test/docs-only.

- **Service-role inventory + allowlist** (pre-live P1, **no migration**). `createAdminClient()` bypasses RLS; every runtime call site under `app/`/`lib/` (61 files) is now allowlisted in `tests/security/service-role-allowlist.ts` with `{ path, purpose, why, scopeGuard }`, enforced by `tests/security/service-role-allowlist.test.ts`: the live call-site set must **exactly equal** the allowlist (new/removed usage fails), each entry needs purpose/why/scopeGuard, and each **scopeGuard symbol must be present in its file** (dropped guard fails). `createAdminClient` is the only service-role constructor (no direct `SERVICE_ROLE_KEY` elsewhere). It's an inventory/drift gate, not a per-query proof — high-risk session-less surfaces still need deeper audits. Complements `admin-server-boundary.test.ts` (browser boundary). **No migration, no RLS/schema/runtime change** (spot-checks found no unsafe usage). Docs 03 §1a. Stripe gates unchanged; production stays at **0100**.

- **Record Keeping ZIP/export completeness** (pre-live P1, **no migration**). The owner-only studio ZIP export (`exportStudioDataAction`) now includes the four record-keeping / inspection tables it previously omitted: `record_keeping_sterile_items.csv`, `record_keeping_disinfectants.csv`, `record_keeping_exposure_incidents.csv` (owner-only), and a **reduced** `record_keeping_audit_events.csv` (record type/id, action, changed-field names, actor, timestamp — **not** the `changes` value-snapshots or `metadata`, so exposure-incident PII isn't duplicated). Every new query is studio-scoped (`.eq("studio_id", studio.id)`) via the **RLS client** (not admin), so exposure incidents + their audit rows stay owner-only (enforced twice: the `role === "owner"` gate + migration 0088's owner-only RLS SELECT). README gained a SENSITIVE DATA warning; the fail-closed `audit_logs` `studio_export` entry is preserved. **No migration, no schema/RLS change, no image metadata/binaries/storage-paths/signed-URLs, no payment export, no intake export change.** Deferred: `treatment_images.csv` metadata, payment tables. Pinned by tests/app/settings/data/export-owner-gate.test.ts + the audit-trail read-only guard. Stripe gates unchanged; production stays at **0100**.

- **Postcare send-state correctness** (pre-live P1, **migration 0100**). Stops overclaiming postcare as "sent" when the provider send fails. Migration 0100 adds nullable `appointments.postcare_email_claimed_at`/`_failed_at`/`_last_error`/`_last_attempt_at` (additive/backfill-safe; no RLS/enum/trigger/destructive; existing `sent_at` rows untouched). `sendPostcareEmailAction` now **claims `claimed_at`** (guarded by `sent_at IS NULL` + no fresh claim, ~5-min stale-reclaim, `.select("id")`) + bumps attempts + `last_attempt_at` **without** stamping `sent_at`; stamps `sent_at` **only after Resend success** (clearing `_failed_at`/`_last_error`/`_claimed_at`); on failure sets `_failed_at` + a **safe/generic** `_last_error` (no raw payload/PII) + clears the claim, leaving `sent_at` NULL on first send (a failed resend keeps a prior real `sent_at`). UI states: Sent / Send-failed-try-again / Sending… / Not-sent / resend-failed-after-success (keeps sent + "Last resend failed" sub-note); "sent" = handed to provider, never delivered/received/opened. **Production migration 0099 → 0100** (migration-first process). Pinned by tests/migrations/0100-postcare-send-state.test.ts + tests/app/calendar/postcare-send-state.test.ts. Stripe gates unchanged (money 1/1/0/0 + non-money pinned + catch-all). No payments/live-payment change.

- **Payment failure-persistence alert hardening** (pre-live P1, **no migration**). Made failed/non-success Stripe-outcome persistence failures as observable as success-persistence failures in `lib/billing/session-payment-charge.ts:writeFailedOutcome`: the DB-error branch now raises a **critical** `session_payment_failed_write_failed` (was stderr-only, no alert), and the zero-row branch is **promoted warning→critical** `session_payment_failed_write_zero_rows` — symmetric with `writeSucceededOutcome` (PR #281). `studioId`/`clientId` threaded into all 3 call sites; safe details only (`attempt_id`/`attempted_status:"failed"`/`stripe_status`/`db_code`; ids-only, no card/name/email/phone/notes/raw payload). Both events auto-surface in `/admin/payments/manual-review` via the existing `session_payment_` prefix — no manual-review change. **Alerting/observability only** — no payment behavior change, no new Stripe calls, no live-payment enablement, no migration. Pinned by `tests/lib/billing/payment-failure-persistence.test.ts`. Stripe gates remain money 1/1/0/0 + non-money pinned + catch-all; production stays at **0099**.

- **Expanded Stripe source gates to all Stripe writes** (pre-live review finding, **no migration**). `scripts/check-stripe-gates.mjs` is now a complete Stripe-write inventory: **money movement unchanged at 1/1/0/0** (output byte-compatible with the pre-#309 gate / `verify-production.mjs`); **six non-money writes exactly-count pinned** to one file each (`customers.create`/`setupIntents.create`→`lib/stripe/setup-intent.ts`; `accounts.create`/`accountLinks.create`/`accounts.createLoginLink`→`lib/stripe/account.ts`; browser `confirmSetup`→`app/portal/PortalPaymentMethodForm.tsx`); and a **hard-fail catch-all** (`no-unclassified-stripe-writes`) that fails on any new Stripe mutating call (new verb/namespace/file, or browser `confirmPayment`/`confirmCardPayment`), naming the offending file/method. Non-money rules opt into comment-stripping so prose mentions don't inflate counts; read-only `.retrieve`/`.list` are excluded. **Source-gate/read-only only** — no runtime/live-mode/payment/refund/SetupIntent/Connect/webhook/env/migration change; live payments remain disabled. Pinned by `tests/scripts/check-stripe-gates.test.ts`. Stripe gates remain 1/1/0/0; production stays at **0099**.

- **Read-only production verification script/runbook** (P0 pre-live gap, **no migration**). `scripts/verify-production.mjs` is an operator-run, **read-only** pre-flight (run `node --env-file=.env.local scripts/verify-production.mjs` from the production-linked Mac) that proves remote prod matches the repo's required state before live payments: remote migration max **0099**; the 0093 (private treatment-image bucket + `treatment_images` policies + integrity trigger), 0097, 0098 (columns + indexes + RPC branches), 0099 effects; RLS on the curated critical tables (incl. payments + `record_keeping_*`); zero unresolved critical payment ops alerts (count only); Stripe gates 1/1/0/0 (spawns `check-stripe-gates.mjs`); fresh reminder heartbeat (≤45 min, Upstash). Reads via `supabase db query --linked` only — **no writes, no migration, no cron, no email, no Stripe writes**; prints only PASS/FAIL/INCOMPLETE + scalars (no secrets/PII/rows); **fails closed** (any FAIL/INCOMPLETE → exit non-zero; missing Upstash → heartbeat INCOMPLETE, not PASS); separates `PRODUCTION VERIFIED ✓` (automated) from the manual dashboard checks. Not a CI gate, not a live-payment enablement step. Runbook: **docs/16 §17.13** (cross-ref docs/10). Validated read-only against prod (11/11 automated PASS; heartbeat INCOMPLETE locally). Pinned by `tests/scripts/verify-production.test.ts`. Stripe gates unchanged (1/1/0/0); production stays at **0099**. Deferred: automating the manual Vercel/Stripe/log checks.

- **Treatment photo notes/captions** (Chloe pilot feedback, **migration 0099**). Practitioners can add/edit a short note under each treatment photo. Migration 0099 adds nullable `treatment_images.practitioner_note text` (additive/backfill-safe; no RLS change — existing `members update` policy covers it; no trigger change — 0093 freezes only identity columns; no enum/destructive/storage change). `updateTreatmentImageNoteAction` is a metadata-only UPDATE via the RLS client (not admin), scoped by `id + studio_id + client_id + deleted_at IS NULL` with a `.select("id")` row-affected check (mirrors `archiveTreatmentImageAction`); trims, whitespace→NULL, caps at 1,000 chars (`TREATMENT_NOTE_MAX_LENGTH` in a plain `note-constants.ts`), generic errors, no token/PII log, revalidates. UI `PhotoNoteEditor` under each photo on the card + modal (Add note / Edit note + inline textarea Save/Cancel), practitioner-only. `page.tsx` selects `practitioner_note` → `row.note`; `TreatmentImage` type += the column. Upload/sanitizer/storage/signed-URL/archive/attach unchanged. **Production migration 0098 → 0099** (migration-first process). Pinned by tests/migrations/0099-treatment-image-notes.test.ts + tests/app/clients/treatment-photo-notes.test.ts. Stripe gates unchanged (1/1/0/0). Deferred: note audit/history.

- **Intake form reminder emails** (Chloe pilot feedback, **migration 0098**). The appointment-reminders cron now sends 7-day + 3-day intake-form reminders for confirmed appointments whose latest intake is still `in_progress`, reusing the existing claim-before-send idempotency. Migration 0098 adds per-appointment columns (`intake_reminder_7d/3d_sent_at`/`_send_attempts`/`_claimed_at`; additive/backfill-safe) + 2 partial indexes + extends `claim_email_send`/`record_email_result` with `intake_reminder_7d`/`_3d` branches (existing branches byte-for-byte; no RLS/enum/destructive change). `sendIntakeReminderPass` (2h windows centered on 7d/3d, ≥ cadence): resolve latest intake with the admin client (studio+client scoped) → skip submitted/reviewed/missing/no-email → claim → re-check confirmed → mint fresh 14-day link (`generateIntakeLinkUrl`; saved answers preserved) → send `buildIntakeReminderEmail` → `record_email_result` → stamp PR #303 metadata (`emailed:true`) on success. Logs ids/kinds only (no token/PII). No SMS, no per-studio toggle, no extra cadence. **Production migration 0097 → 0098** (apply via the migration-first process). Pinned by tests/migrations/0098-intake-reminder-columns.test.ts + tests/app/api/cron/intake-reminder.test.ts + tests/lib/email/intake-reminder-template.test.ts. Stripe gates unchanged (1/1/0/0). Deferred: SMS, per-studio toggle, preference center.

- **Calendar appointment card visual refresh** (Fresha readability inspiration only, **no migration**, render-only). Appointment blocks read stronger + clearer: `lib/calendar/service-colors.ts` deepens the per-service palette from `bg-*-50`/`border-l-*-400` to `bg-*-100`/`border-l-*-500` (calm/clinical; deterministic hash + neutral fallback unchanged; **rose/red still excluded** for allergy/EpiPen); the appointment card left accent → `border-l-4`; a visible **time range** ("9:00–10:00", new pure `timeRangeLabel` in `calendar-format.ts`, from existing `starts_at`/`ends_at`); and a 3-line hierarchy (time range → bold client name → service/modality on its own line), short blocks one dense line. Terminal/cancelled dim, click/link, positioning math, blocked-time cards, and open-hours shading all unchanged. No booking/smart-scheduling/data/query/migration change; no Fresha assets copied. Pinned by tests/app/calendar/appointment-card-refresh.test.ts. Stripe gates unchanged (1/1/0/0). Migration remains 0097. Deferred follow-ups: open-hours band, gridline tuning, month-chip polish, mobile day-view.
- **Treatment photo UX cleanup — labels + session date** (Chloe pilot feedback, **no migration**, render-only). The Treatment Photos gallery card + larger-preview modal stop showing the raw `.jpg` `original_filename`; a session-attached photo is titled "Session {session date}", else "Uploaded {date}"; image alt/aria are human ("Treatment photo"). `treatmentPhotoScopeLabel` (`photo-context.ts`) block scope → "Treatment area photo" (was "Block photo"), matching the upload selector — one consistent label. Session date comes from a read-only embed `sessions ( started_at )` added to the existing image query (via the `treatment_images.session_id` FK) — no schema/sanitizer/storage/signed-URL/archive/session-attach change, no migration. Deferred (need migration/product decision): per-photo notes, removing "Client photo", unifying Session vs Treatment-area, full photo-context redesign. Pinned by tests/app/clients/treatment-photo-labels.test.ts (+ updated treatment-photo-context.test.ts). Stripe gates unchanged (1/1/0/0). Production migration remains 0097.
- **Smart intake resend status / expiry visibility** (Chloe pilot feedback, **migration 0097**). Intake link expiry was stateless (only inside the signed token); resend/copy wrote nothing, so the UI couldn't show accurate status. Migration 0097 adds three nullable/default-safe display-mirror columns to `client_intake_forms` (`intake_link_last_sent_at`, `intake_link_expires_at`, `intake_link_send_count`) — no token/raw-token storage, no RLS/status-enum change. A shared helper `stampIntakeLinkIssued` (`lib/intake/queries.ts`, admin client, no RLS change) stamps them at every mint path (booking/reschedule + `resendIntakeEmailAction` = emailed; `getIntakeLinkAction` Copy link = refresh expiry+count but not last_sent_at; `requestIntakeUpdateAction` = emailed iff it emailed); `responses` never touched (saved answers preserved). `computeIntakeLinkStatus` (`lib/intake/link-status.ts`, pure) drives the card: normal (emailed/expires/days-left/"not submitted"), close-to-expiry ≤3d + expired ("Send a fresh link"), legacy started_at fallback. Button: Send fresh link / Resend again (count≥2) / Resend intake link. No delivery/receipt/opened/completed overclaim; render-only, no cron. Both surfaces (overview + dedicated intake page) pass the status. **Production migration 0096 → 0097.** Pinned by tests/app/clients/intake-resend-status.test.ts (+ existing intake-resend.test.ts green). Stripe gates unchanged (1/1/0/0). Deferred: sent_by, content snapshot, delivery receipt, precare tracking, inspector export.
- **Postcare sent visibility** (Chloe pilot feedback D, **no migration**, read-only). Surfaces the existing postcare send timestamp (`appointments.postcare_email_sent_at` + `postcare_email_send_attempts`, migration 0043) more clearly: the appointment detail (`PostcareSendButton`) shows "✓ Postcare sent {date}" (+ "· N attempts") or an explicit "Not sent yet.", and the client profile appointment timeline (`getAppointmentsForClientProfile` now selects the two columns read-only; `ClientAppointmentTimeline` renders a per-row "Postcare sent {date}") gives a per-client postcare history. Copy says "sent" (recorded send), never "delivered"/"received" — no delivery claim. `sendPostcareEmailAction` unchanged; nothing sent on render. Deferred to a later PR: `sent_by`, content snapshot, delivery receipt, precare-sent tracking, inspector export/audit table. No schema/migration. Pinned by tests/app/calendar/postcare-sent-visibility.test.ts. Stripe gates unchanged (1/1/0/0).
- **Settings Availability / Breaks & Blocks consolidation** (Chloe pilot feedback B, **no migration**, IA/render-only). The separate "Breaks & blocks" settings tab is folded into **Availability** (`/settings/availability`): `RecurringBreaksSection` + `TimedBlocksSection` moved into `app/(app)/settings/availability/` (action import → `./actions`, same centralized `availability/actions.ts`), render there alongside weekly hours + whole-day blockouts, and the copy clarifies the four concepts. The "Breaks & blocks" nav item is removed and `/settings/calendar` `redirect()`s to `/settings/availability` (safe deep link). Owner-only gate + all existing actions preserved. **DB tables not merged** (`studio_blockouts` vs `studio_timed_blocks` stay distinct — future schema decision). No booking-slot change, no calendar rendering change, no schema/migration. Pinned by tests/app/settings/availability-consolidation.test.ts. Stripe gates unchanged (1/1/0/0).
- **Calendar blocked-time visibility + open-hours clarity** (Chloe pilot feedback B/C, **no migration**, calendar-render-only). Month view now shows blocked time: `renderMonthView` (`app/(app)/calendar/page.tsx`) loads the same block sources as the week view (`getBlockouts` + `getTimedBlocksForRange` + `getRecurringBreakOccurrencesForRange` — existing range fetchers, no new query) and `groupMonthBlockedByDate` (`app/(app)/calendar/month-blocked.ts`) feeds `MonthView` a per-day summary rendered as a compact muted chip. The week full-day blockout overlay now shows its reason (`displayBlockoutLabel`, fallback "Blocked") instead of the hard-coded "Blocked"; recurring-break + timed-block labels are unchanged. Open hours read clearer: the out-of-hours tint in `DayColumn` is a calmer-but-clearer `bg-neutral-200/70` + hairline edge (was the too-subtle `bg-neutral-100/80`). Shared block-label helpers moved from the client `DayColumn.tsx` into the server-safe `calendar-format.ts`. No schema/migration, no booking-slot logic change, no settings IA change. Pinned by tests/app/calendar/block-visibility.test.ts. Stripe gates unchanged (1/1/0/0).
- **Calendar return-to-date navigation** (Chloe pilot feedback C, **no migration**, routing/render-only). Returning from an appointment now lands the practitioner back on the view/date they came from, not today. The week page (`app/(app)/calendar/page.tsx`) appends a safe return context (`?view=week&week=<weekStart>`) to each appointment-detail link via a `returnTo` prop on `DayColumn`, and the detail page (`app/(app)/calendar/[id]/page.tsx`) builds its back link from `view`/`week`/`month` searchParams through `calendarReturnHref` (`app/(app)/calendar/calendar-return.ts`). **No open-redirect:** the helper never echoes a URL — it reconstructs `/calendar` + a validated subset (`view` ∈ {week,month}; `week`/`month` strict `YYYY-MM-DD`), so the back link is always internal and falls back to bare `/calendar`. No schema/migration, no appointment mutation, no other calendar rendering changed. Pinned by tests/app/calendar/return-nav.test.ts. Stripe gates unchanged (1/1/0/0).
- **Smart / packed booking slots** (Chloe pilot feedback A, **no migration**, algorithm-only). Public booking availability no longer offers a fixed every-15-minute grid. `getAvailableSlots` (`lib/booking/slots.ts`) now anchors candidate starts to (1) the opening time and (2) **immediately after each existing reservation's protected end** (`candidateMs.add(c.end)` — appointment `ends_at` already includes its buffer per 0029, so no double-count), plus a **coarse hourly fallback** (`FALLBACK_GRANULARITY_MINUTES=60`, generated via `utcInstantFromLocal` per step so DST is preserved) so an empty day still offers a few choices. Each candidate must fit a full `duration` before close and clear every conflict via the same half-open overlap rule. So an empty 10:00–17:00 day shows 10:00/11:00/…/16:00 (not 10:00/10:15/10:30), and a 10:00–11:00 appointment (15-min buffer) offers 11:15 next. **No schema/RLS change, no migration, no new query, no conflict-model change**; public booking display + re-verify + next-available all share the unchanged `getAvailableSlots`, so a shown slot stays bookable and the DB exclusion protections are intact. Pinned by tests/lib/booking/slots-smart-scheduling.test.ts. No payment/Stripe/cron/intake/Treatment-Photo/Record-Keeping change; Stripe gates unchanged (1/1/0/0). Live payments remain disabled. (Other Chloe feedback — settings IA, calendar visibility/nav, postcare audit, photo UX — are separately-scoped follow-ups.)

- **Controlled live-payment enablement prep** (PR #297, **no migration**, docs + CI safety-lock — **enables nothing**). Live payments stay **DISABLED**. Consolidated the enablement procedure into one ordered checklist at **docs/16 §17.12** (in order: business/legal → live Stripe account → live webhook → drop the `payment_charge_attempts_livemode_false_check` DB CHECK → relax the claim-RPC mirror → charge/refund/receipt runtime guards → webhook `shouldIgnoreLiveModeEvent` → card-auth livemode scoping → static gates green → **env flip LAST** (`sk_live_*` + `pk_live_*` + live `whsec_*` + `STRIPE_ALLOW_LIVE_MODE=true`)). **Flipping the env flag alone is NOT enough.** Added a read-only CI safety-lock `tests/lib/billing/live-mode-disabled.test.ts` that source-greps every dormancy layer (env opt-in clause, preview/dev-forbidden, the DB CHECK + claim-RPC `not_ready` mirror, charge/refund `live_mode_blocked`, receipt `stripe_livemode !== false` + hardcoded `livemode:false`, card-auth `.eq("stripe_livemode", livemode)`, webhook ignore + `stripe_webhook_livemode_event_ignored`, the 1/1/0/0 Stripe-gate rules) — so relaxing any gate also requires editing this test (the real enablement PR). Shipped backstops: #281 persistence/`needs_manual_review`, #290 manual-review queue, #291 `OPS_ALERT_EMAILS` required, #201+#296 owner-only refunds. No gate/env/migration change; Stripe gates unchanged (1/1/0/0). **Controlled live-payment enablement has not started.**

- **Refund owner gate — defense in depth** (PR #296, **no migration**). Refund initiation was already studio-owner-only (PR #201): both action callers (`refundPaymentChargeAttemptAction`, `refundFeeAttemptAction`) require `practitioner.role === "owner"`, and the single `refunds.create` (`lib/billing/payment-refund.ts`) is reachable only from those two; webhook is reconcile-only, admin manual-review is read-only. **The latent gap:** owner-only lived only in the action layer — the helper re-checked studio scope + live-mode but not role. PR #296 adds a **helper-level owner re-check** inside `refundPaymentChargeAttempt` (reads the actor's `practitioners.role` scoped to `(practitionerId, studioId)`, returns `not_authorized`) **before the claim UPDATE and `refunds.create`**, so a future caller can't bypass owner-only; plus `logInternal("payment_refund_denied_not_owner", …)` safe-ID-only logging on both action denial paths (previously silent). **Reads only the existing `practitioners.role` column — no migration, no schema/RLS change, no new refund surface, no charge-auth change.** `refunds.create` stays exactly one; Stripe gates unchanged (1/1/0/0). Pinned by tests/lib/billing/payment-refund-source.test.ts. **Live payments remain disabled; controlled live-payment enablement has not started.**

- **Record Keeping print: disinfectant replace-by + status** (PR #295, **no migration**, render-only). Makes the `/records/print` disinfectants section match the in-app Records screen. PR #280 (migration 0096) added `discard_due_date` ("replace by") + the pure read-time status helper (`lib/record-keeping/disinfectant-status.ts`), shown in-app — but the **inspector print view omitted both**. Now `DisinfectantsPrint` takes the studio `timezone`, computes the studio-local `today` via the same `todayInTz`, and prints a **"Replace by"** line (date or "Not set") + a **"Replace status"** line (`disinfectantStatusLabel`) for overdue/due-today/due-soon batches (`disinfectantDueStatus` + `isDisinfectantAlert`). **Reuses the shipped helpers; `getDisinfectantRecords` already selects the columns — no query/schema/RLS change, no migration.** Optional sterile-operator-name + procedure probe-lot-confirmed add-ons were **skipped** (they'd require a query change, out of scope). Pinned by tests/app/records/print-export.test.ts. No cron/reminder/payment/Stripe/intake/Treatment-Photo change; Stripe gates unchanged (1/1/0/0). **Live payments remain disabled; controlled live-payment enablement has not started.**

- **Intake link resend / refresh** (PR #293, **no migration**, UX/discoverability only). Pilot blocker: a client's expired intake link, with no obvious "resend" for Chloe. The backend already supported it — `resendIntakeEmailAction` mints a fresh 14-day token for the *same* intake row (preserving saved answers; the token is a stateless HMAC with no DB `expires_at`) and re-emails — but it was buried as a per-row "Resend email" in the intake history, while the prominent card ("Request intake update") started a *new blank* intake. PR #293 adds a primary **"Resend intake link"** CTA (`IntakeResendCard.tsx`, reusing `resendIntakeEmailAction` + a `getIntakeLinkAction` Copy-link fallback) on `/clients/[id]/intake` for in-progress intakes, **above** the now-secondary, relabeled **"Send a new intake form"** card; plus a best-effort "previous link may have expired" hint when the in-progress intake is older than the shared `INTAKE_LINK_TTL_DAYS` (14). **Reuses the existing studio-member-gated, no-PII-logging actions verbatim — no new server action, no token-model/TTL/route/submit change, no migration.** Copy: keeps-answers + "Links expire after 14 days"; success "Intake link sent."; no-email → Copy link. Pinned by tests/app/clients/intake-resend.test.ts. No Stripe/payment/cron/reminder/Treatment-Photo/Record-Keeping change; Stripe gates unchanged (1/1/0/0). **Live payments remain disabled; controlled live-payment enablement has not started.** (The Record Keeping print fix is queued as #294.) **Follow-up hotfix:** production verification found the CTA was on the *dedicated* `/clients/[id]/intake` page, but the practitioner-facing "Health & Forms" tab is the inline `activeTab === "health"` card on `app/(app)/clients/[id]/page.tsx` — which for an in_progress intake had no link/CTA. The hotfix renders the same `IntakeResendCard` in that overview card's in_progress branch (+ a "View intake →" link); single-file, reuses the existing component/actions, no migration.

- **Treatment photo upload pre/post-buffer size hardening** (PR #292, **no migration**). Treatment Photos reliability/security hardening — two defense-in-depth byte-length guards in `uploadTreatmentImageAction`, both reusing the single-source `TREATMENT_IMAGE_MAX_BYTES` (15 MB) + `validateTreatmentImageUpload`. The pipeline already validated `file.size` + MIME before `arrayBuffer()` and ran the authoritative Sharp sanitizer (real-format detect, 100 MP pixel limit, fake-MIME guard, EXIF/GPS/XMP/ICC strip, sanitized bytes stored, no public URLs), but the cap trusted the client-reported `file.size` and the re-encoded output was never byte-capped. **Guard (1):** after `arrayBuffer()` and **before** the sanitizer, re-validate the **actual buffered length** (`> 0`, `<= 15 MB`) independently of `file.size`. **Guard (2):** after the sanitizer and **before** the storage upload, cap the **sanitized output length** (`<= 15 MB`). Generic errors only; no Sharp/storage leak. **No sanitizer/storage/signed-URL/bucket/UI change, no migration, no new constant.** Defense-in-depth (low live risk for a real server-action `File`). Pinned by tests/app/clients/treatment-image-upload-bounds.test.ts; existing Treatment Photos suites stay green. No calendar/payment/charting/record-keeping/reminder change; Stripe gates unchanged (1/1/0/0). **Live payments remain disabled; controlled live-payment enablement has not started.**

- **Critical ops-alert delivery verification** (PR #291, **no migration**, env gate + tests + docs). Operations reliability hardening before live payments. Critical ops alerts (payment / storage / cron / webhook failures) email `OPS_ALERT_EMAILS` via `lib/ops/alert-email.ts` (after the durable `ops_alerts` row, never throws) — but that var was never enforced, so a production deploy with it unset/empty made the email a silent no-op (alert lives only as a DB row + `/admin/ops-alerts`). PR #291 extends the PR #262 production env gate (`scripts/check-production-env-gates.mjs`, wired into `npm run build`) with a **second gate**: when `VERCEL_ENV === "production"`, require `OPS_ALERT_EMAILS` to parse to **≥1 recipient** (mirrors the runtime `parseOpsAlertEmails` via `opsAlertRecipientCount()`, so whitespace-only / comma-only also fail), own label `ops-alert-delivery-env`, **names-only output (addresses never printed)**, build exits non-zero if either gate fails. **Outside production it stays optional** (SKIP on local/CI/preview). **No runtime change** to `alert-email.ts`/`alerts.ts` (already gated-to-critical, after-write, never-throws, redacted), no new UI, no new alert provider, no real send, no migration. Also corrected two factually-stale docs (docs/10 + docs/11 said `OPS_ALERT_EMAILS` was "reserved / not read" — stale since PR #193). Adversarial/inspection workflow confirmed; Stripe gates unchanged (1/1/0/0). Pinned by tests/scripts/check-production-env-gates.test.ts. **Live payments remain disabled; controlled live-payment enablement has not started.**

- **Payment manual-review queue** (PR #290, **no migration**, read-only admin). Payment operations hardening before live payments. PR #281 made `needs_manual_review` a real outcome + critical ops alert but not an operator workflow; PR #290 adds a focused, **read-only** admin page `/admin/payments/manual-review` (admin-only via the existing `ADMIN_EMAILS`/`isAdmin` gate + a defensive page re-check; `force-dynamic`; service-role read) with two derived sections that **mirror docs/16 §17.7 queries (1) and (3)/(6)**: (1) `payment_charge_attempts` stuck in `status='pending_stripe'` > 60 min; (2) **unresolved critical payment ops alerts only** (`session_payment_*` / `payment_intent_*` / `payment_refund_*` / `payment_charge_*` / `charge_refunded_*` / `stripe_webhook_*` — warnings + `card_on_file_setup_failed` stay on `/admin/ops-alerts`). There is **no `needs_manual_review` DB status** — the row stays `pending_stripe`, so the queue is derived, not stored. New pure `lib/billing/payment-manual-review.ts` (selection + safe view-models) + the page + a `/admin` QuickLink. **Privacy:** ids only (no client names), amount/currency, Hone status, `failure_code`, non-secret PaymentIntent id, live/test-mode flag, alert event/severity, **redacted** alert message; view-models copy a fixed safe allowlist (no client name/notes/`failure_message_safe`/card/customer leak). **Strictly read-only:** no resolve/retry/refund/repair button, no payment mutation, no Stripe API call — resolution stays on the existing `/admin/ops-alerts` action (the queue links there). A failed read throws a generic error (no provider-message leak) rather than a false-empty queue. Adversarially reviewed (5 lenses: privacy/read-only/access/selection/robustness — all SAFE; one read-error-handling fix applied). No charge/refund/webhook/payment-success change; Stripe gates unchanged (1/1/0/0). Pinned by tests/app/admin/payment-manual-review.test.ts. **Live payments remain disabled; controlled live-payment enablement has not started.**

- **Calendar feed privacy hardening** (PR #289, **no migration**). The iCal subscription feed (`/calendar-feed/<token>.ics`) is a **bearer secret** that Google/Apple/Outlook store with the event contents — and its default `DESCRIPTION` leaked `Client: <client name>` + `Type: <modality>`. The default ICS is now privacy-preserving: the route's appointment SELECT was narrowed to **`id, starts_at, ends_at`** (no longer fetches client name or modality), and each event is `SUMMARY:Hone appointment` + a generic `DESCRIPTION` ("Appointment scheduled in Hone. Open Hone for details.") + an auth-gated `View in Hone: <origin>/calendar/<id>` deep link (no token); stable PII-free `UID:<appt>@hone.care`; `LOCATION` = the studio's already-public address; accurate start/end. **NO** client name / email / phone / address / intake / notes / modality / body-area / status / token / signed URL / storage path / Stripe data appears. Cancelled appointments stay excluded; headers (`Referrer-Policy: no-referrer`, `X-Robots-Tag: noindex`, `Cache-Control: no-store`) and the hash-at-rest token lookup (PR #182/0079) are unchanged; existing feed tokens keep working. **Output change only — no migration, no header/middleware/token/UI change.** Deferred backlog: per-studio opt-in to include client names, token rotation/revoke UI, last-used telemetry. Pinned by tests/app/calendar-feed/feed-privacy.test.ts (+ existing route-hash-lookup pins). No booking/reminder/payment/Treatment-Photos/charting change; Stripe gates unchanged. **Live payments remain disabled; controlled live-payment enablement has not started.**

- **Notification test / CI reliability checkpoint** (PR #288, **no migration, test-only — no production change**). De-flaked `tests/lib/notifications/practitioner-notifications.test.ts` (a prior full-suite timeout on the "invalid event type … returns void synchronously" smoke; passed when rerun alone). **Root cause = test isolation, not a code bug:** the invalid-event smoke left the helper's intended **fire-and-forget** ops-alert chain (`void (async () => { await import("@/lib/ops/alerts"); await recordOpsAlert(...) })()`) pending in the background, and that un-mocked dynamic-import + `createAdminClient` work outliving the test was the suspected timeout source under `pool: "forks"` contention. The `recordPractitionerNotification` never-throws fire-and-forget contract is correct production behavior, so **nothing in `lib/notifications/*` changed**. The runtime smoke now `vi.mock`s `@/lib/ops/alerts` + `@/lib/supabase/admin-server`, **flushes microtasks** so the chain settles inside the test, and asserts: invalid event → `undefined` / no throw / exactly one `practitioner_notification_invalid_event_type` warning alert / no `createAdminClient` (no insert); valid event → void + swallowed admin-client throw via `practitioner_notification_insert_threw`. **Reliability:** target test 10/10 + full `npm test` green (the flake did not reproduce — consistent with green CI; the value is determinism-by-construction). Repro is prod-write-safe (vitest loads no `.env.local`; `createAdminClient` throws on missing env, so the fire-and-forget `recordOpsAlert` can't reach prod). No migration; no notification/reminder/cron/ops-alert/payment/Treatment-Photos/charting behavior change; Stripe gates unchanged. **Live payments remain disabled; controlled live-payment enablement has not started.**

- **Treatment image archive scope + zero-row handling** (PR #287, **no migration**). Treatment Photos data-integrity hardening: `archiveTreatmentImageAction` scoped its conditional UPDATE by `id + studio_id` only (not `client_id`) and never confirmed a row changed — so a Client A route call with Client B's (same-studio) image id archived the **wrong client's** photo (RLS is studio-grained; 0093 only freezes identity columns, so a same-studio `deleted_at` flip on another client's row is a legitimate DB write), and a nonexistent/already-archived/wrong-client id updated **zero rows** yet returned `{ ok: true }`. The archive UPDATE is now scoped by `id + studio_id + client_id + deleted_at IS NULL` and proves exactly one row changed via `.select("id")`; a zero-row/null result returns a generic "Treatment photo not found." (never revealing another client's image, never revalidating the page, never reporting success). The DB-error path keeps the generic "Could not archive the image." Archive is the only by-id metadata mutation — upload (client-scoped insert, PR #284) and signing (a read) are unchanged. **No storage/bucket/signing/sanitizer/RLS/schema change; no migration** (0092/0093 protect storage/path/identity but can't constrain which route client an archive action targets — a pure app-level gap). Pinned by tests/app/clients/treatment-image-archive-scope.test.ts. No charting/record-keeping/payment/reminder change; Stripe gates unchanged. **Live payments remain disabled; controlled live-payment enablement has not started.**

- **Clinical lineage enforcement for charting actions** (PR #286, **no migration**). Clinical data-integrity hardening: the `block-actions.ts` charting actions validated that a session belongs to the **studio** but not to the **route client**, so a same-studio **wrong-client write** (Client A route + Client B's session/block/entry id, from a stale tab / bug / tampered form) could corrupt treatment memory — migration 0094 enforces same-studio parent consistency (block∈session / entry∈block∈session) but does not constrain which route client an action targets. New shared `lib/sessions/session-lineage.ts` `assertSessionForClient(studioId, clientId, sessionId)` (queries `sessions` by `id + studio_id + client_id + deleted_at null`; throws a generic "Treatment session not found.") is now called by **all 6** `block-actions.ts` actions (create/update/copy/soft-delete block + create/update treatment-area-with-entry) before any write; every block/entry write stays scoped by the client-validated `session_id`, so the full `studio → client → session → block → entry` chain is enforced (0094 supplies block∈session / entry∈block∈session). The session-level `actions.ts` was already client-aware (`assertSessionVisible`) and is unchanged. **Session-level only is necessary + sufficient** (the writes are already session-scoped); explicit per-level block/entry query helpers were rejected as redundant. Defense-in-depth above RLS; no schema/RLS/charting-behavior change; valid same-client writes unaffected. Pinned by tests/app/sessions/clinical-lineage.test.ts. No payment / Treatment-Photos / reminder / record-keeping change; Stripe gates unchanged. **Live payments remain disabled; controlled live-payment enablement has not started.**

- **Central ops-alert message redaction** (PR #285, **no migration**). Privacy/security hardening: `recordOpsAlert` previously redacted `safe_details` but only **truncated** the free-text `message`, so a provider `error.message` (Supabase / Stripe / storage / Twilio / cron / app exceptions) could leak PII / signed URLs / storage paths / tokens / Stripe secrets / DB internals into the stderr log, the `ops_alerts.message` column, the admin Ops-alerts page, AND the critical operator email. New pure module `lib/ops/redact.ts` (`redactOpsAlertMessage` + `redactOpsAlertValue`/`redactOpsAlertDetails`); `recordOpsAlert` now runs `redactOpsAlertMessage(input.message)` **centrally** before every sink — one fix covers all ~30+ call sites (no per-site change needed). Scrubs emails / phones / `Bearer`+`authorization` / `CRON_SECRET` / JWTs / Supabase signed URLs / URL token+signature params / appointment-cancel tokens / `treatment-images`+raw `<uuid>/<uuid>/` storage paths / Stripe `sk_`/`rk_`/`whsec_`/`pi_…_secret_` / named secret fields / high-entropy tokens; **preserves** non-secret Stripe object ids (`pi_`/`ch_`/`re_`/`cus_`/…) + UUIDs (protect-and-restore) so reconciliation context survives; typed columns untouched; deterministic + idempotent. Minimal call-site hygiene: the riskiest treatment-images storage-error alerts now use generic messages. `safe_details` redaction preserved (alias `redactSafeDetails`) and now also scrubs embedded patterns in string values. No migration / schema / RLS / payment / reminder / Treatment-Photos behavior change; Stripe gates unchanged; no new alert UI or notification channel; no public/client exposure. Pinned by tests/lib/ops/redact.test.ts (+ existing tests/lib/ops/alerts.test.ts). **Live payments remain disabled; controlled live-payment enablement has not started.**

- **Attach Treatment Photos to session / block at upload** (PR #284, **no migration**). Product/moat feature: the Treatment Photos upload form (`/clients/<id>/images`) now has a **"Photo context"** selector — Client photo (default) / Session photo / Treatment area photo — that populates the already-existing nullable `treatment_images.session_id` / `session_block_id` columns (migration 0092). The upload action (`app/(app)/clients/[id]/images/actions.ts`) **validates every submitted id server-side** (session ∈ studio+client; block ∈ its session + studio, with the session **derived from the block row**) before storing, rejecting cross-studio/cross-client/mismatched ids with a generic error and storing nothing — mirroring, and backstopped by, the **0093 `enforce_treatment_image_integrity` trigger**. `page.tsx` loads recent sessions (bounded, with block area fields) via the studio+client-scoped RLS client; `TreatmentImagesManager.tsx` renders the selector (mobile-friendly, client-only fallback, area labels, "Will attach as: …" summary) with ids only as `<option>` values. The PR #274 context tags then render automatically for populated rows. **Storage/security unchanged** — private bucket, service-mediated, sanitizer + EXIF strip, path validation, orphan cleanup, **no public URLs**, no client/portal exposure. **No migration / schema / RLS / signed-URL change; no annotation/comparison/thumbnails/bulk** (deferred); an in-charting attach entry point is deferred. Pinned by tests/app/clients/treatment-photo-attach.test.ts (+ existing image/context-tag/DB-hardening tests). Stripe gates unchanged. **Live payments remain disabled; controlled live-payment enablement has not started.**

- **Reminder scheduler alerting + runbook** (PR #283, **no migration**). Reliability hardening only. Makes a stale/missing external appointment-reminder scheduler **actionable** instead of a passive admin card. PR #265 added the Upstash heartbeat + `/admin` **Reminder scheduler** card (healthy ≤45 min / stale / missing); PR #283 adds `recordReminderSchedulerHealthAlert()` (`lib/cron/reminder-heartbeat.ts`) — reads the heartbeat, and records ONE **deduped** ops alert when stale (`reminder_scheduler_stale`, warning) or missing (`reminder_scheduler_missing`, critical → emails `OPS_ALERT_EMAILS`). **Dedupe** = skip if an unresolved `ops_alerts` row already exists for that event (reuses `resolved_at`; no migration). **No auto-resolve** (operator resolves manually on `/admin/ops-alerts` after fixing the scheduler). **Runs on the EXISTING daily `materialize-recurring-breaks` cron** (`0 8 * * *`), best-effort + wrapped, so it fires automatically and independently of the dead external every-15-min scheduler; it **never sends reminders** and never calls `/api/cron/appointment-reminders`. `safe_details` are timing-only (`status` / `last_success_at` / `age_minutes` / `cadence_minutes` / `stale_after_minutes` / `checked_at`) — no `CRON_SECRET`, Authorization header, client PII, reminder content, or provider payload. **Detection latency ≤ ~24h** (daily cadence); the admin card remains the **real-time read-only** view. No new scheduler/cron/UI/migration; reminder send/timing, cron auth, the heartbeat write, and the Stripe gates are unchanged. Full runbook (failure meanings + operator steps): docs/08 §"Reminder scheduler alerting + runbook (PR #283)". Pinned by tests/lib/cron/reminder-heartbeat.test.ts + tests/app/cron/reminder-heartbeat-wiring.test.ts. **Live payments remain disabled; controlled live-payment enablement has not started.**



- **Payment reconciliation + controlled live-payment readiness runbook** (PR #282, **no migration, docs-only**). Readiness + reconciliation **only — NOT live-payment enablement.** Added **docs/16 §17** — the authoritative post-#281 reconciliation + controlled-enablement runbook: current status (live payments **disabled**; PR #281 success persistence **complete**; success requires Stripe success **plus** Hone ledger persistence; the PR #179 webhook `payment_intent.succeeded` handler remains the eventual-consistency **backstop**); required pre-enablement gates; a **Before / During / After** first-live-payment checklist (one studio, one operator, one small payment, stop on mismatch); **forbidden actions without owner approval** (set `STRIPE_ALLOW_LIVE_MODE=true`, add live Stripe keys, run live charges, broadly enable card-required flows); a **rollback plan** (disable the live-mode flag → revert `sk_live_`→`sk_test_` → pause charging → inspect Ops alerts + Stripe dashboard → document); a webhook replay procedure; and six **read-only (SELECT-only) reconciliation SQL queries** over `payment_charge_attempts` / `ops_alerts` / `stripe_events` (stuck `pending_stripe`; Stripe-PI-present-but-local-not-succeeded; the #281 `session_payment_succeeded_write_failed` / `_zero_rows` criticals; refund-review alerts; unprocessed/unmapped webhook events; recent payment criticals). Operators already see these criticals on the admin **Ops alerts** page (`/admin/ops-alerts`, PR #193). Also fixed the stale docs/16 §7.1/§7.2 references to the deleted `manual-fee-charge.ts`/`manual_fee_charge_attempts`, and added pointers in docs/06, docs/10, docs/12, docs/18. **No migration, no runtime/charge/refund/webhook behavior change, no new admin UI, no executable prod-connecting script, no Stripe key/env change.** Stripe gates unchanged (one `paymentIntents.create`, one `refunds.create`, zero `charges.create`, zero `checkout.sessions`). Pinned by tests/docs/payment-reconciliation-readiness.test.ts (existing tests/docs/live-payments-readiness.test.ts pins remain green). **Live payments remain disabled; controlled live-payment enablement has not started — it is still a separate future PR.**

- **Payment success persistence is authoritative** (PR #281, **no migration**). Closes a deep-review live-payment blocker: `runSessionPaymentCharge` (`lib/billing/session-payment-charge.ts`) could return a normal `ok:true, outcome:"succeeded"` after Stripe succeeded **even if Hone failed to persist that success on the local `payment_charge_attempts` ledger** (DB error) or the conditional UPDATE affected **zero rows** — money moved but the result told the practitioner it was cleanly done. **The rule is now authoritative: a NORMAL `succeeded` result requires Stripe success AND a proven local-ledger write.** `writeSucceededOutcome` went from `Promise<void>` to `Promise<SuccessPersistenceResult>` = `{persisted:true} | {persisted:false; reason:"db_error"|"zero_rows"}`; the **DB-error** branch now raises a **critical** ops alert `session_payment_succeeded_write_failed` (PR #263 only logged it to stderr) and the **zero-row** branch keeps its critical `session_payment_succeeded_write_zero_rows` alert. **Both** success callers — the create/confirm path and `reconcileExistingPaymentIntent` — branch: `persisted:true` → the existing succeeded result; `persisted:false` → `ok:false, outcome:"needs_manual_review"` (an existing, indeterminate, **non-success** outcome) with a safe message ("Stripe reported the payment as succeeded, but Hone could not confirm the local payment record. Review the payment in Stripe and Hone before retrying.") + non-sensitive reconciliation ids (`stripePaymentIntentId`, `attemptId`), **no retry** (deterministic idempotency key + Stripe 24h replay already prevent a double charge). **No migration** (manual-review is a return value, not a DB status; the row stays `pending_stripe`), no new public status, no UI work, no env/Stripe-key change; mirrors the refund helper (`payment-refund.ts`), which already did this. **Webhook reconciliation (PR #179) remains the eventual-consistency backstop** — a `payment_intent.succeeded` event still flips a `pending_stripe` row to `succeeded`. Preserved: claim-before-charge, idempotency, live-mode hard block, exactly one `paymentIntents.create` / one `refunds.create`, webhook, metadata, Stripe gates. Pinned by tests/lib/billing/payment-success-persistence.test.ts + the new DB-error critical-alert assertion in tests/lib/billing/payment-outcome-zero-row.test.ts. **Live payments remain disabled; controlled live-payment enablement has not started — the remaining docs/16 readiness items are still deferred.**

- **Chloe record-keeping feedback — disinfectants + exposure incidents** (PR #280, migration **0096 — NOT yet applied to production**). Record-Keeping-only changes (no charting touched): (1) **Disinfectant discard / replace-by date** — a new `record_keeping_disinfectants.discard_due_date` (nullable), kept distinct from `date_prepared` (made) and `date_discarded` (actually discarded); the Records page shows a **read-time** "Due today" / "Overdue" / "Due soon" badge from it (computed in the studio's timezone — **no cron / bell / email / SMS**; those are **deferred**, see docs/08). (2) **Operator dropdown** — the disinfectant operator is now a same-studio staff dropdown (current user + active practitioners) with a free-text **"Other"** fallback; populates the existing `operator_practitioner_id` (column already existed) + `operator_name`; the server resolves the name from a **studio-scoped** lookup so a cross-studio id can never attach; edit now writes the FK too; legacy free-text operators still render. (3) **Exposure-incident person selector** — Client / Staff-or-self / Other; picking a same-studio **Client** autofills name/phone/address (editable), **Staff** autofills name; **Other** is manual. Autofill only pre-fills the existing free-text columns — **no client/practitioner FK is stored**, so the owner-only read posture (0088) is unchanged. Migration **0096** adds one nullable column; additive, legacy rows read safely. **No charting / Treatment Photos / payment / live-mode / booking / intake change; no RLS change.** Proactive disinfectant bell/email/SMS reminders are **deferred** (Option A chosen). Pinned by tests/lib/record-keeping/disinfectant-status.test.ts, tests/app/records/record-keeping-feedback.test.ts, tests/migrations/0096-disinfectant-discard-due-date.test.ts, tests/db/record-keeping-discard-due.db.test.ts. **0096 must be applied to production BEFORE the #280 deploy serves traffic** (the app writes the new column; 0096 is backward-compatible, so apply-0096-then-merge).

- **Chloe real-charting feedback — session charting UX + bug fixes** (PR #279, migration **0095 — NOT yet applied to production**). Charting-only changes from Chloe's same-day feedback after charting real treatments: (1) a **Numbing** control (Not recorded / No numbing used / Numbing used → `session_blocks.numbing_status`, NULL = Not recorded); (2) the body-map no longer floods the whole **Arms** zone when **Underarms** (a sub-area) is picked — a subtle "contains the selected area" state, exact area via the chip + "Area being charted" line; (3) **probe lot/batch** can be **suggested** from the studio's sterile-item records and must be **explicitly confirmed** (`session_blocks.probe_lot_confirmed`; never auto-confirmed; manual entry still works); (4) **Energy level** moved under **Treatment readings**; (5) **OmniBlend** reading layout — galvanic before thermolysis, no thermolysis duration, no galvanic intensity (other modalities unchanged pending Chloe); (6) **Client tolerance** is now label-based (Comfortable … Needed pause/stopped early) over the unchanged 1-5 `tolerance_rating` storage; (7) **Treatment observation** chips are reliable **toggles** (tap to add, tap again to remove, reflected in the notes text; reaction chips stay single-select with "No visible reaction" mutually exclusive). Migration **0095** adds `session_blocks.numbing_status` (CHECK none/used) + `probe_lot_confirmed boolean default false` — additive, legacy rows read safely. **No payment / live-mode / Treatment Photos / booking / intake change; no RLS change.** Record-keeping feedback (disinfectant discard reminder, operator dropdown, exposure-incident person selector) is **deferred to PR #280**. Pinned by tests/lib/sessions/clinical-response.test.ts, tests/lib/comments.test.ts, tests/app/sessions/charting-feedback.test.ts, tests/app/sessions/body-map-area-picker.test.ts, tests/migrations/0095-charting-numbing-probe-lot.test.ts, tests/db/charting-numbing-probe-lot.db.test.ts. **0095 must not be applied to production until explicitly approved — and, UNLIKE 0094, it must be applied BEFORE the #279 deploy serves traffic** (the app writes the new columns; 0095 is backward-compatible with the current pre-#279 app, so the safe sequence is apply-0095-then-merge — see docs/10).

- **Tenant consistency constraints for sensitive child tables** (PR #278, migration **0094 — NOT yet applied to production**). DB hardening: several clinical/import child tables enforced `studio_id` via RLS but did not prove their PARENT rows were same-studio (a row could carry studio_id=A while pointing at a client/session/appointment/import-batch from studio B). 0094 adds **composite same-studio foreign keys** (the project's existing tenant pattern — the payment subsystem + `treatment_images` 0093 trigger already enforced this) to `sessions` (client + appointment), `session_blocks` (session), `client_intake_forms` (client), `imported_treatment_memories` (client + import batch), `treatment_plans` (client), and `electrolysis_entries` (attached block must belong to its own session) — plus the parent unique constraints they need (`sessions(studio_id,id)`, `session_blocks(session_id,id)`, `import_batches(studio_id,id)`; `clients`/`appointments` already had theirs). **Composite FKs, not triggers** (avoids the PR #276 SET-NULL cascade pitfall; PG17 column-list `ON DELETE SET NULL (col)` keeps SET-NULL parents clean). Each composite FK **replaces** the prior single-column FK (mirroring its ON DELETE; behavior-preserving) so each table pair keeps exactly one relationship — two FKs between a pair break PostgREST embedded selects (`electrolysis_entries_session_id_fkey` is kept, a different pair). Cascade/soft-delete preserved. **No RLS change, no payment/live-mode change.** The only app change: **3 PostgREST embed hints** updated from `alias:<fk_column>(…)` to the bare table name (`session-payment-eligibility.ts`, `record-keeping/queries.ts`, `global-search-actions.ts`) — the column-hint resolved via the dropped single FKs; bare embeds resolve via the composite FK with no response-shape change (appointments→clients was unchanged, so its `client:client_id(name)` embed stays). Production preflight (read-only, 2026-06-28): **0 existing cross-studio rows** across all hardened tables. Pinned by tests/db/tenant-consistency.db.test.ts (cross-studio insert/update rejected, valid + cascade + set-null work, RLS reads still blocked) + tests/migrations/0094-tenant-consistency-constraints.test.ts (SQL pin). **0094 must not be applied to production until explicitly approved after merge.** Stripe gates unchanged; live payments remain disabled.

## Earlier production status (as of PR #277)

- **Treatment image content validation + EXIF stripping** (PR #277, **no migration**). Upload hardening on top of PR #276: the upload action no longer trusts the browser MIME/bytes. A new server-only sharp module (`lib/images/treatment-image-sanitize.ts`, Node runtime) decodes the actual bytes, rejects anything that is not a genuine JPEG/PNG/WebP (SVG, HEIC/HEIF, PDF, HTML, video, corrupt data, **fake-MIME** where declared ≠ detected, empty, decompression bombs via `limitInputPixels`), and **re-encodes the same format WITHOUT metadata** (`.rotate()` bakes orientation then drops the tag; no metadata is preserved) so **EXIF/GPS/XMP/ICC are stripped**. `uploadTreatmentImageAction` now uploads the **sanitized bytes** and stores the **sanitized content type + sanitized size**; path/extension derive from the sanitized output; original filename stays display-only. PR #276 path/signer/parent/immutability/orphan-cleanup hardening is intact. **Dependency:** `sharp@^0.34.5` promoted from a transitive (Next image optimizer) to a **direct** dependency — already on Vercel, no native-build risk; minimal lockfile change. No public URLs / no `getPublicUrl`; practitioner-only; live payments remain disabled. Pinned by tests/lib/images/treatment-image-sanitize.test.ts (accept jpeg/png/webp; reject fake-PDF/HTML/SVG/HEIC/corrupt/empty/mismatch; EXIF stripped; deterministic output) + tests/app/clients/treatment-image-content-validation.test.ts (wiring: sanitized bytes/type/size stored, invalid → no upload, no public exposure). Stripe gates unchanged (one paymentIntents.create).

## Earlier production status (as of PR #276)

- **Treatment Photos storage boundary hardening** (PR #276, migration **0093 — NOT yet applied to production**). Hardens the PR #271–#274 treatment-image feature; **no new product feature, no UI change, no payment change.** Migration 0093: keeps the `treatment-images` bucket private; **drops the authenticated `storage.objects` select/insert policies** so objects are **service-role only** (no direct member access — closes the bypass); adds CHECK constraints binding `storage_bucket='treatment-images'` + `storage_path` to the row's own `<studio_id>/<client_id>/<file>.<ext>` shape (+ "block requires session"); and a trigger that enforces client∈studio / session∈studio+client / block∈session+studio and **freezes identity columns post-insert** (archive only flips `deleted_at`/`deleted_by`). App: a central `validateTreatmentImagePath` helper (`lib/images/treatment-images.ts`) is wired into BOTH the signed-URL action and the page pre-signer — a forged/malformed/cross-studio path is **rejected, never signed** (critical ops alert on rejection); the upload now raises a **critical** `treatment_image_orphan_cleanup_failed` alert if post-insert object cleanup fails. Practitioner-only; private bucket; short-TTL signed URLs only; **no public URLs / no `getPublicUrl`**; signed URLs never stored. Pinned by tests/lib/images/treatment-images.test.ts (path validator) + tests/migrations/0093-harden-treatment-image-storage.test.ts (SQL pin) + tests/db/treatment-image-hardening.db.test.ts (db lane: direct-object bypass blocked, forged path/bucket/parent rejected, identity immutable, soft-archive + service path still work) + tests/app/clients/treatment-image-hardening.test.ts (wiring). **0093 must not be applied to production until explicitly approved after merge.** Image-content validation / EXIF stripping and the session/block attach write-path remain deferred. Stripe gates unchanged (one paymentIntents.create). Live payments still disabled.

## Earlier production status (as of PR #274)

- **Treatment photo context tags** (PR #274, no migration). Each Treatment Photo card (and the larger preview) now shows **treatment-context tags** derived display-only from EXISTING metadata — **no schema/storage/RLS/signed-URL-security change.** A scope badge ("Client photo" → "Session photo" → "Block photo", most specific wins), an area badge when a block is attached ("Treatment area: <area · side · detail>" via the canonical `sessionBlockSideLabel`, or "Area not recorded"), the existing "Uploaded <date>", and a muted "Clinical reference" label. Context comes from `treatment_images.session_id`/`session_block_id` (migration 0092) + the embedded `session_blocks` area fields `primary_area`/`side`/`custom_area_detail` (migration 0039); `page.tsx` computes labels **server-side** (pure helper `app/(app)/clients/[id]/images/photo-context.ts`) so **no raw UUIDs/storage paths/bucket names/signed-URL text reach the client.** Uploads remain client-only today (so every current photo shows "Client photo"); session/block/area badges activate once a future write-path attaches photos to a session/block (deferred — no speculative schema). No annotation/OCR/AI, no side-by-side comparison, no consent/export, no public/client exposure. Pinned by tests/app/clients/treatment-photo-context.test.ts; PR #271/#272/#273 tests still pass. Stripe gates unchanged (one paymentIntents.create). Live payments still disabled; appointment reminders still depend on the external 15-min scheduler (PR #258).

- **Treatment Photos inline gallery preview** (PR #273, no migration). Photos now render **inline** in the practitioner-only gallery (visible in the client record) instead of behind a new-tab "View" link. **No storage/RLS/signed-URL-security/schema change.** The page server-pre-signs a short-TTL preview URL per image (after the studio-scoped RLS load) and the grid renders inline `<img>`; clicking opens an **in-app modal** (`role="dialog"`, no dependency) that lazily mints a fresh signed URL via the existing `getTreatmentImageSignedUrlAction` (ownership-checked). Fallback "Image not available" on null/failed preview. Security unchanged: private bucket, **no public URLs / no `getPublicUrl`**, short-TTL signed URLs only, **never stored in DB**, `storage_path` stays server-side, practitioner-only, no client/public exposure (CSP `img-src` already allows `https:`). An external before/after photo-management product was product-category inspiration only (no external UI/assets/positioning/export copied). Deferred: thumbnail pipeline, side-by-side comparison, consent/sharing, export, annotation/OCR/AI. Pinned by tests/app/clients/treatment-photos-preview.test.ts; PR #271/#272 tests still pass. Stripe gates unchanged (one paymentIntents.create). Live payments still disabled; appointment reminders still depend on the external 15-min scheduler (PR #258).

- **Treatment Photos gallery UI polish** (PR #272, no migration). UI/navigation polish of the PR #271 secure image storage — **no storage/RLS/signed-URL/validation/security change.** The `/clients/<id>/images` route now surfaces as a top-level **"Treatment Photos"** link in the client tab bar (`components/profile-tab-bar.tsx`) instead of being buried under Health & Forms (`ProfileTab` unchanged — it's a route link, not an in-page tab). The page heading is "Treatment Photos"; the rough native file picker is replaced by a styled **"Choose image"** card (native `<input type=file>` kept accessible/`sr-only`) that shows the selected filename + accepted types (JPEG/PNG/WebP, 15 MB) + an **"Attach image"** button disabled until a file is chosen; photos render as a gallery card grid (filename + date + View + Archive) with an empty state. Thumbnails deferred (would require eagerly signing each image — a signed-URL flow change); viewing stays on-demand. No annotation/drawing/OCR/AI, no public/client exposure, no new dependency. Pinned by tests/app/clients/treatment-photos-ui.test.ts; PR #271 tests still pass. Stripe gates unchanged (one paymentIntents.create). Live payments still disabled; appointment reminders still depend on the external 15-min scheduler (PR #258).

- **Secure treatment image storage V1** (PR #271, migration 0092 — **applied in production**). Practitioner-only image storage so treatment memory can include visual reference material. A **private** Supabase bucket `treatment-images` (`public=false`, **no public URLs**) + a `treatment_images` metadata table (studio_id + client_id required; session_id/session_block_id optional; storage path/bucket, sanitized filename, content_type, size_bytes, uploaded_by, soft-delete deleted_at). UI at `/clients/<id>/images` (surfaced as the **Treatment Photos** tab-bar link, PR #272). **Two-plane access:** metadata via the RLS client (`is_studio_member` select/insert/update, no delete, revoke truncate/delete); upload + short-TTL `createSignedUrl` via the service-role client ONLY after the server action re-checks the row/client belongs to the caller's studio (studio.id never from input; server-generated `<studio_id>/<client_id>/<uuid>.<ext>` path; client paths never signed). Validation: MIME allowlist jpeg/png/webp, 15 MB cap, reject SVG/PDF, sanitize filename. No annotation/drawing/OCR/AI/public exposure/template builder (deferred); the legacy unwired `photos` table was not reused. **Migration 0092 was applied to production (verified: private bucket `treatment-images` exists + `public=false`, table + RLS live, no manual fallback needed).** Pinned by tests/lib/images/treatment-images.test.ts + tests/migrations/0092-treatment-images.test.ts + tests/db/treatment-images.db.test.ts + tests/app/clients/treatment-images.test.ts. Stripe gates unchanged (one paymentIntents.create). Live payments still disabled; appointment reminders still depend on the external 15-min scheduler (PR #258).

- **Body-map treatment-area picker** (PR #270, no migration). Adds a built-in **body-map** picker to the session-block charting form: a **schematic body figure (inline SVG vector shapes — no image asset/upload/canvas/drawing/OCR/AI)** with clickable broad zones (Face / Neck / Torso / Arms / Legs / Bikini-intimate / Other); picking a zone reveals its specific area chips, which set the same `session_blocks.primary_area`, with the region-grouped `AreaPicker` kept below as the "or choose from the list" fallback. Zone→area mapping (`lib/sessions/body-zones.ts`) is built entirely from existing `AREA_REGIONS` keys (no invented areas). Reuse-only: existing structured fields (migration 0039), unchanged save path (area optional; clearing clears side + specifics via a shared `onAreaChange`), no migration/new-dependency/new-DB-field. New charting-only component `components/body-map-area-picker.tsx`; the shared `AreaPicker`/`AREA_REGIONS` are unchanged so the treatment-plan editor is unaffected. Jane body-chart references were product-category inspiration only — no Jane UI/assets/thumbnails/URLs copied. Deferred: anatomical image assets, image storage/upload, photo annotation, drawing/sketch canvas, OCR/AI, full template builder. Pinned by tests/app/sessions/body-map-area-picker.test.ts; PR #268/#269 tests still pass. Stripe gates unchanged (one paymentIntents.create). Live payments still disabled; appointment reminders still depend on the external 15-min scheduler (PR #258).

- **Visual treatment-area picker** (PR #269, no migration). Frames the existing region-grouped `AreaPicker` (chips over `AREA_REGIONS`) inside a **"Chart part" → "Treatment area" card** on the session-block charting form, with a live **"Area being charted: …"** preview (area + side + specifics) and an **"Area not recorded"** fallback. **Deliberately a simple visual grouped picker, NOT an anatomical body-map** — image storage/upload, photo annotation, sketch/drawing canvas, OCR/AI, and a full chart-template builder are deferred. Reuse-only: existing `AreaPicker` + `AREA_REGIONS` + `session_blocks.primary_area/side/custom_area_detail` (migration 0039) — no migration, no new field/dependency/component; the shared `AreaPicker` is unchanged (treatment-plan editor unaffected); save behavior + legacy compatibility unchanged (area stays optional). Jane chart-part/body-chart references were product-category inspiration only — no Jane UI/wording/assets/thumbnails/URLs copied. Pinned by tests/app/sessions/visual-area-picker.test.ts; PR #268 chart-parts tests still pass. Stripe gates unchanged (one paymentIntents.create). Live payments still disabled; appointment reminders still depend on the external 15-min scheduler (PR #258).

- **Chart parts: treatment area visible** (PR #268, no migration). Lightweight "chart parts" framing whose first part is the **treatment area / area being charted**, made visible across charting, saved entries, and treatment memory. **Charting context only — not image storage, body-map/drawing, sketch, photo/file upload, OCR/AI, or medical advice.** Reuses existing fields (`session_blocks.primary_area/side/custom_area_detail` from migration 0039, captured area-first by the `AreaPicker`; `ImportedTreatmentMemory.treatment_area_text`; `treatment_plans.treatment_areas`) — no migration, no new field/dependency. V1 adds a "Recorded area" / "Area not recorded" eyebrow on saved entries, a labeled "Treatment area" line + "Area not recorded" fallback + "Latest recorded setup — <area>" on the Before Today / treatment-memory card, and an explicit "Imported area" label (kept in its separate provenance-noted section). Treatment Intelligence already aggregates per-area (unchanged). Jane's "Chart Parts" guide used ONLY as product-category inspiration — no Jane UI/wording/assets/thumbnails/URLs copied (source-grep banned). Deferred: image storage, body-map/drawing, sketch, file/photo upload, OCR/AI, full template builder. Pinned by tests/app/clients/chart-parts-area.test.ts + the extended before-today test. Stripe gates unchanged (one paymentIntents.create). Live payments still disabled; appointment reminders still depend on the external 15-min scheduler (PR #258).

- **Intake modality-specific review flags** (PR #267, no migration). Extends the PR #266 intake review flags so each chart-mapped flag also shows **modality/category badges** from Chloe's clinic reference chart — "Review before thermolysis", "Review before continuous/galvanic current", "Medical authorization may be required", "Precaution noted". **Still practitioner workflow support only — Hone does not make medical decisions or approve/deny treatment; it surfaces "this intake answer maps to modality-specific review."** Badges derive at read time (`lib/intake/review-flags.ts`, static `CHART_MODALITIES` keyed by flag id) from existing structured answers — no migration, no new field, no persisted/risk table, no free-text parsing, no OCR/AI, no image storage, no booking/charting block. Only rows with an existing structured field are mapped (heart/pacemaker, pregnancy, epilepsy, cancer, blood-borne hepatitis/HIV, diabetes [generic field → conservative union], blood-thinners→prolonged-bleeding); chart rows with no structured field are not invented. The avoided clinical-decision wording is never rendered. Practitioner-only (not on the public `/intake/[token]` route). Pinned by tests/lib/intake/review-flags-modality.test.ts + the extended tests/app/clients/intake-review-flags.test.ts; V1 PR #266 tests still pass. Stripe gates unchanged (one paymentIntents.create). Live payments still disabled; appointment reminders still depend on the external 15-min scheduler (PR #258).

- **Intake review flags** (PR #266, no migration). Practitioner-only "intake review needed" flags on the intake review page (`/clients/<id>/intake`), derived at read time from the structured answers already in `client_intake_forms.responses` by a pure rule table (`lib/intake/review-flags.ts`). **Hone does not make clinical decisions — it surfaces intake answers for practitioner review only.** Each flag cites its source ("Based on intake response: …") and uses allowed wording only ("Medical authorization may be required" / "Review before treatment" / "Precaution noted"); the card closes with "Use professional judgment and clinic policy." No migration, no new field, no free-text parsing, no AI/OCR, no persisted risk score, no booking/charting block; practitioner-only (not on the public `/intake/[token]` route); allergy/EpiPen keep their existing dedicated cards (not duplicated). Forbidden clinical-decision wording is banned from the new module + card by a scoped source-grep test. Deferred: a compact "Before Today" overview indicator (V2), image storage, body-area charting, AI/OCR, the #215–#265 tracker backfill. Pinned by tests/lib/intake/review-flags.test.ts + tests/app/clients/intake-review-flags.test.ts. Stripe gates unchanged (one paymentIntents.create). Live payments still disabled; appointment reminders still depend on the external 15-min scheduler (PR #258).

- **Reminder cron status signal** (PR #265, no migration). Makes the external every-15-min appointment-reminder scheduler (cron-job.org → `GET /api/cron/appointment-reminders`, `Authorization: Bearer $CRON_SECRET`, PR #258) operator-visible: a stopped scheduler otherwise produces silence (the existing `cron_route_failed`/`reminder_send_exhausted` ops_alerts only fire when the route runs and throws/exhausts). On each authorized successful run the route writes a single Upstash KV heartbeat `reminder_cron:last_success` (ISO timestamp + non-sensitive aggregate counts; best-effort/fail-open; 24h TTL); the operator-only `/admin` console reads it and `computeReminderSchedulerStatus` marks healthy (≤45 min) / stale / missing (threshold = 3× the shared `CRON_INTERVAL_MINUTES`). Reuses the prod-required Upstash (PR #262) — a single overwritten key, NOT the append-only ops_alerts (which still carries the failure direction). No new scheduler/queue/retry/dependency/migration; no reminder-send change; no customer UI; the heartbeat stores no secret/PII and is recorded only after the auth gate (401 records nothing). Pinned by tests/lib/cron/reminder-heartbeat.test.ts + tests/app/cron/reminder-heartbeat-wiring.test.ts. Stripe gates unchanged (one paymentIntents.create). Live payments still disabled; appointment reminders still depend on the external 15-min scheduler (PR #258).

- **Raw appointment cancellation_token column removed** (PR #264, migration 0091). Drops the legacy raw `appointments.cancellation_token` column so no raw appointment cancel/reschedule/manage token is retained at rest, closing the PR #260 deploy window. Migration 0091 re-creates both token-verifying RPCs HASH-ONLY (drops the `OR cancellation_token = …` dual-match branch + the raw-column INSERT/shape-routing), drops the dead 2-arg `public_cancel_appointment_with_token(text,text)` + the deploy-window hashing trigger/function, and drops the raw column (with its 0025 unique constraint + partial index); the canonical `cancellation_token_hash` column/CHECK/unique are untouched. App: the three link-rebuild surfaces (`app/(app)/calendar/actions.ts`, `app/api/cron/appointment-reminders/route.ts`) now always mint the stateless HMAC token; cancel/reschedule/manage already looked up `cancellation_token_hash` only; the raw field is removed from the `Appointment` type. Already-emitted raw links still resolve (the app hashes the URL token and matches the backfilled hash). Token/storage hardening only — no new token system, dependency, UI, link-behavior change, or payment/live-mode change; no raw token read/written/logged. **Migration-first: apply 0091 in prod + verify (`cancellation_token_hash` present; `cancellation_token` gone) — the app is drop-tolerant (`select("*")` + HMAC fallback) and the re-created RPCs stay hash-compatible, so deploy order is safe either way; the prod migration is NOT auto-run (explicit approval).** Pinned by tests/migrations/0091-drop-raw-cancellation-token.test.ts, tests/db/appointment-token-hash.db.test.ts, tests/app/book/no-raw-appointment-token.test.ts. Stripe gates unchanged (one paymentIntents.create). Live payments still disabled; appointment reminders still depend on the external 15-min scheduler (PR #258).

- **Payment outcome zero-row detection** (PR #263, no migration). Payment-OUTCOME writes (charge succeeded/failed in `lib/billing/session-payment-charge.ts`, refund succeeded/failed in `lib/billing/payment-refund.ts`, and the four Stripe-webhook reconciliations in `lib/billing/payment-webhook-reconciliation.ts`) were status-conditional `.update().eq(...)` calls that only checked the DB `error`; a conditional UPDATE matching ZERO rows (row left the guarded state via a concurrent race) returned no error and persisted nothing, so a real Stripe outcome could be silently treated as recorded. Each of the eight writes now appends `.select("id")` and, on an empty result, surfaces an explicit failure: charge `writeSucceededOutcome` → critical `session_payment_succeeded_write_zero_rows` ops_alert (money moved, row unstamped), `writeFailedOutcome` → warning; refund success/failure fold zero-row into their existing critical `payment_refund_*_write_failed` → `needs_manual_review`; the webhook reconcilers emit a per-event `*_zero_rows` warning + return `zeroRowNoMutation` instead of claiming a reconcile (the out-of-band "reconciled" alert is now gated behind rows>0). App-layer only — no migration, no dependency, no new Stripe call, no live-payment change; reuses the `.update().select("id")` + `length===0` pattern from the refund/receipt claim sites and the existing `recordOpsAlert`. Alerts carry safe ids/status only (no raw payload/PII/secret). Deferred: receipt-delivery status writes + the two idempotent id-backfills. Pinned by tests/lib/billing/payment-outcome-zero-row.test.ts. Stripe gates unchanged (one paymentIntents.create, one refunds.create). Live payments still disabled; controlled live-payment enablement not started.

- **Rate-limit production env gate** (PR #262, no migration). Public rate limiting (`lib/rate-limit/public.ts`) FAILS OPEN on missing config — `getRedis()` returns null when `UPSTASH_REDIS_REST_URL`/`UPSTASH_REDIS_REST_TOKEN` are unset and every limiter returns `{ allowed: true }`, silently removing abuse protection from public booking/portal-login/token/marketing routes. New `scripts/check-production-env-gates.mjs` (modeled on check-stripe-gates) is wired into the `build` script and **fails the Vercel production build** when either Upstash var is missing; it enforces ONLY when `VERCEL_ENV === "production"` and is a no-op SKIP for local/CI/preview (deliberately NOT keyed on `NODE_ENV`, which `next build` sets to production everywhere). Prints var **names only, never values**. Build/CI safety-script change only — no new rate limiter, dependency, schema/migration, or route behavior change. Missing config in prod = fail the deploy; a transient Upstash **outage** stays the existing runtime fail-open (throttled `ratelimit_backend_unavailable` alarm) — a limiter outage must never block a booking. **No bypass** (a presence gate isn't tripped by outages; a bypass would just re-enable the silent fail-open). Deferred: `app/portal/verify/[token]` magic-link consume has no limiter (coverage gap, mitigated by single-use atomic consume + high-entropy token). Pinned by tests/scripts/check-production-env-gates.test.ts. Stripe gates unchanged (one paymentIntents.create). Live payments still disabled; appointment reminders still depend on the external 15-min scheduler (PR #258).

- **Public booking PII log minimization** (PR #261, no migration). Privacy hardening of the unauthenticated `/book/[slug]` server logs: `app/book/[slug]/actions.ts` logged the raw normalized booker email + an internal client UUID in two error paths (`public_booking_archived_client_collision` → `normalizedEmail` + `archivedClientId`; `public_booking_unique_race_unresolved` → `normalizedEmail` + raw Postgres `message`), through a no-redaction `logInternalBookingError` → `console.error(JSON.stringify(detail))` sink. **Logging-payload change ONLY** — no logging system, dependency, migration, schema, or booking-behavior change. Every `logInternalBookingError` payload now carries safe fields only: the sqlstate `code`, `studioId`, and (where a booker email is in scope) `emailFingerprint: hashFingerprint(normalizedEmail)`; raw `normalizedEmail`, `archivedClientId`, and all raw DB `message` strings are dropped, and the collision path logs `archivedClientCollision: true`. Reuses the portal helper `hashFingerprint` (`lib/portal/tokens.ts`, salted SHA-256 over `PORTAL_FINGERPRINT_SALT`, fail-closed in prod, the same helper the portal magic-link logs use) so the same email correlates across surfaces without ever being written raw (log minimization, not anonymization). Deferred to a follow-up PII-log pass: the raw-`message`/raw-error logs on the other public token surfaces (`app/cancel|reschedule|manage|intake/[token]`, `lib/email|sms/send-appointment.ts`) and the `appointment_audit` `{email,notes}` DB write at actions.ts L796 (a write, not a log). Pinned by tests/app/book/pii-log-minimization.test.ts (payload source-pins + fingerprint determinism). Stripe gates unchanged (one paymentIntents.create). Live payments still disabled; appointment reminders still depend on the external 15-min scheduler (PR #258).

- **Appointment cancel/reschedule token hashing at rest** (PR #260, migration 0090). Security hardening: `appointments.cancellation_token` was stored raw and looked up directly; it is now hashed at rest, mirroring the calendar-feed playbook (migration 0079). Migration 0090 adds `cancellation_token_hash` (SHA-256 hex, CHECK + partial unique), `extensions.digest` backfills every existing row, and a `BEFORE INSERT/UPDATE` trigger auto-hashes any raw token whenever the hash is absent (deploy-window + compat net). New TS helper `hashAppointmentToken` (lib/booking/appointment-token.ts) is the lookup hash; a DB-integration test pins it equal to the SQL `digest`. **New appointments store ONLY the hash** (raw column NULL); the public `/cancel`, `/reschedule`, `/manage` resolvers hash the incoming URL token and match the hash column. The raw column was **kept transitionally** (already-emailed `/cancel/<raw>` links still resolve via the backfilled hash); **PR #264 (migration 0091) dropped the raw column, the deploy-window trigger, and the dual-match raw RPC branches once the window closed** (the RPCs are now hash-only). Both token-verifying RPCs (`public_cancel_appointment_with_token`, `reschedule_appointment`) originally dual-matched `hash OR raw` for the in-flight deploy window and the reschedule INSERT routed the new token by shape — both removed in PR #264. Surfaces that rebuild a link after creation (portal Manage, reminder email/SMS via cron, internal-booking confirmation) now mint the stateless HMAC token (`generateCancellationToken`, expires at appointment start); **`/reschedule` now accepts the HMAC fallback** that `/cancel`/`/manage` already had (removing the column-only asymmetry); booking/reschedule confirmation emails still carry the raw token (in-memory at creation). Pinned by tests/lib/booking/appointment-token-hash.test.ts (helper), tests/db/appointment-token-hash.db.test.ts (TS↔SQL hash agreement, trigger, hash-only storage, CHECK, partial unique, both RPCs by hash + raw deploy-window tolerance), and e2e/appointment-token-hash.spec.ts (booked → minted token resolves on all three surfaces) + the existing e2e/mobile-ux.spec.ts. **Migration-first:** apply 0090 in prod + verify column/backfill BEFORE merging app code. No Stripe/payments/billing/booking-business-rule change. Live payments still disabled; appointment reminders still depend on the external 15-min scheduler (PR #258).

- **Imported treatment memory in Before Today** (PR #259, no migration). Surfaces imported (paper/Jane/Fresha/spreadsheet) history from Quick Import (PR #257) inside the **Before Today** briefing on the client Overview — delivering the "Before-Today surfacing" deferred by PR #252/#257. Read-only: no migration, no dependency, no schema/parser/importer change, no service role. Reuses the PR #252 RLS-backed `getImportedTreatmentMemoriesForClient` (studio+client-scoped, voided excluded, newest-first, capped) + `ImportedMemoryView` (provenance labels + `IMPORTED_PROVENANCE_NOTE`). The pure `buildBeforeToday` is unchanged (preserving the dashboard-preview "3 reads" pin); `app/(app)/clients/[id]/page.tsx` loads imported memory in its `Promise.all` (cap 5) and passes it as a second prop to `BeforeTodayCard`, which renders a visually distinct amber "Imported treatment memory" section (after the live block, shown even with no live history) with safe fields (source label, date, treatment area, modality, method, probe type/size/lot, tolerance, reaction, caution, next-visit, aftercare, imported note; empties skipped), a "Showing the latest N of M" cap line, and a "Review against the original record if needed." footer. No edit/void/merge/convert, no dashboard-preview change, no AI/OCR. Pinned by tests/app/clients/before-today.test.ts (new imported-section + page-wiring pins), the reused tests/lib/imported-treatment-memory.test.ts + tests/db/imported-treatment-memory.db.test.ts, and e2e/before-today-imported.spec.ts (Quick Import → open client → imported section shows, labelled not-charted-live, no overflow). Stripe gates unchanged (one paymentIntents.create). Live payments still disabled; appointment reminders still depend on the external 15-min scheduler (PR #258).

- **Cron and reminder reliability fix** (PR #258, no migration). Fixed the empty `vercel.json` `"crons": []` (three active routes were unscheduled in-repo, depending on an external scheduler) and the 2h-reminder schedule/window mismatch. Review surfaced that the **production Vercel plan caps cron cadence at once-per-day** (a `*/15` cron is rejected at deploy), so — per the operator's decision — `vercel.json` schedules only the **daily** `/api/cron/materialize-recurring-breaks` (`0 8 * * *`), and `/api/cron/appointment-reminders` runs from an **external every-15-min scheduler** (cron-job.org, `Bearer $CRON_SECRET`); it becomes a `*/15` `vercel.json` cron if the project moves to Vercel Pro. `/api/cron/no-show-check` is intentionally unscheduled (disabled non-mutating stub). Vercel Cron / the external scheduler both send `Authorization: Bearer ${CRON_SECRET}` (validated by `lib/cron/auth.ts`) — no auth change. The 2h window `[105,135]` (30 min) was silently missing ~half of appointment minute-offsets under hourly cadence; at `*/15` (30 = 2× cadence) every offset is covered and a single skipped fire still leaves a grid point in-window. Cadence + windows are a single source of truth in `lib/cron/reminder-schedule.ts` (shared by route + tests). Claim-before-send / idempotency / 3-strike unchanged. Added: a `reminder_send_exhausted` ops alert (warning, safe metadata only — no client PII/token/free-text) when a reminder exhausts `MAX_ATTEMPTS`, and a `status='confirmed'` re-check immediately before send (don't remind a cancelled appointment). Pinned by tests/lib/cron/reminder-schedule.test.ts (offset coverage; hourly+30min fails; 24h safe) and tests/app/cron-config.test.ts (vercel.json schedules each active route; cadence covers the 2h window; claim/status/alert/auth pins). docs/08 + docs/10 updated. Stripe gates unchanged (one paymentIntents.create). Live payments still disabled.

- **Quick Import V1** (PR #257, no migration). Owner-only `/settings/import` brings clients + basic historical treatment memory over from CSV/TSV **paste or file**, **preview-first**, into the PR #252 `imported_treatment_memories` schema — "start using Hone without retyping your whole practice." NOT OCR, AI extraction, API sync, destructive merge, overwrite, live charting, or public onboarding. No migration, no dependency, no service role: reuses the 0089 owner-INSERT RLS (owner writes batch + memory via the authenticated client; the audit trigger auto-records), the 0087 clients member-INSERT + `createClientAction` insert/dedup shape, `assertOwner`, the settings owner-tab nav, and the `{ok,error}` client-island pattern. Pure pipeline `lib/import/quick-import.ts` (small local RFC4180-ish CSV/TSV parser, header aliases, loose normalization, identity grouping, dup detection, insert mapping) + two actions (`previewImportAction` no writes; `confirmImportAction` near-atomic bulk inserts: batch → clients → memories, soft-void batch on failure). Create-only (confident dup by email/phone/name+DOB = skip; weak name-only = warn, never merge); never overwrite/merge/delete; multi-area rows group into one client (same-name + conflicting contact/DOB never merged); one memory per content row; imported rows never written to sessions/appointments and labelled "Imported history, not charted live in Hone."; raw text never stored/logged; 2000-row cap; no AI/OCR/upload/external/SMS/email/payments. `general_notes`→client `notes`. Deferred: merge/update existing, batch-void UI, Before-Today surfacing. Pinned by tests/lib/import/quick-import.test.ts (28), tests/app/settings/import/quick-import-action.test.ts (owner gate + safety negatives), tests/db/quick-import.db.test.ts (owner write path, 0 sessions/appointments, non-owner blocked), e2e/quick-import.spec.ts (paste→preview→confirm, grouping, anon→/login, no-studio→/no-access). Stripe gates unchanged (one paymentIntents.create). Live payments still disabled.

- **Admin studio detail privacy follow-up** (PR #256, no migration). Closes the carried-forward gap from PR #255: `/admin/studios/[id]` previously rendered raw client **names** (and selected `clients.select("id, name, created_at")`). It now shows operational metadata + **aggregate counts + setup-health flags only**, matching the console's privacy posture. Pure read-narrowing — no migration, no dependency; reuses the console count/`Flag` idiom, `createAdminClient()`, and the `app/admin/layout.tsx` `isAdmin` gate (unchanged). `app/admin/studios/[id]/page.tsx` `loadStudioDetail` now runs ONE explicit-column `studios` select with embedded `practitioners/clients/services/studio_availability_default/appointments/imported_treatment_memories(count)` aggregates + one owner `pending_invitations(status)` query (no more `clients`/`practitioners`/`pending_invitations` row tables). Renders a metadata grid (booking slug → `/book/<slug>`, timezone, owner email, owner-invite status), a counts grid (with an "Aggregate counts only" disclaimer), setup-health flags (Owner active / Services / Availability / Booking slug / Live payments disabled), and links to the console + wizard. Selects/renders NO client names/contacts, treatment notes, imported-memory contents, exposure incidents, payment internals, Stripe ids, tokens, or audit JSON. Pinned by tests/app/admin/admin-studio-detail.test.ts and e2e/new-studio-wizard.spec.ts (operator opens a seeded studio's detail page → counts/setup render, the seeded client name does NOT appear, no overflow). Admin remains operator-only; Stripe gates unchanged (one paymentIntents.create). Live payments still disabled.

- **Admin Console V1** (PR #255, no migration). Upgrades `/admin` into a small internal operator console focused on invite-only studio setup, so the PR #254 New Studio Wizard is discoverable (CTA + nav link, no remembered URLs). Pure UI/read upgrade — no migration, no dependency, no new tracking, no server action; reuses the `app/admin/layout.tsx` `isAdmin` gate, `createAdminClient()`, and the existing embedded `(count)` pattern. `app/admin/page.tsx` now shows: a "Live payments are disabled." banner; a Studio setup card → `/admin/studios/new`; four overview count cards (total studios, pending/accepted owner invites, studios needing an owner); an enhanced studios table (name → detail, slug → `/book/<slug>`, owner email, timezone, owner-invite status, setup-health flags Owner/Services/Availability); and operator quick links — from two cheap reads (`studios` + embedded `practitioners/clients/services/studio_availability_default` counts, and an owner `pending_invitations(studio_id,status)` query). Privacy: operational metadata + aggregate counts ONLY — no client names/notes, exposure incidents, imported-memory contents, payment internals, Stripe ids, tokens, or audit JSON (pinned by negative greps). The pre-existing practitioners/waitlist/demo sections are kept as-is. Pinned by tests/app/admin/admin-console.test.ts and e2e/new-studio-wizard.spec.ts (operator sees the console, no overflow, CTA → wizard). Stripe gates unchanged (one paymentIntents.create). Live payments still disabled.

- **Internal New Studio Wizard V1** (PR #254, no migration). Automates ONLY the operator's manual studio bootstrap (docs/20 §2.1/§2.2) behind an internal, operator-only `/admin/studios/new` page — NOT public self-serve onboarding, billing, Stripe Connect, team management, or email automation. Almost entirely reuse, zero new dependency, no migration: reuses `isAdmin`/`ADMIN_EMAILS` (`lib/admin.ts`, fail-closed in prod — NOT a new `HONE_OPERATOR_EMAILS`), the `app/admin/layout.tsx` gate, the `assertAdmin` re-check, and `createAdminClient()` (`server-only`). The action `createStudioWithOwnerInvite` (`app/admin/studios/new/actions.ts`) re-checks `isAdmin`, validates via the pure `parseNewStudioInput` (`lib/studios/new-studio.ts`: required fields, lowercase/url-safe/non-reserved slug, valid IANA timezone, normalized email), pre-checks slug uniqueness + duplicate pending invite, then via service-role inserts a `studios` row (name/owner_email/slug/timezone only — fees NULL, no Stripe) + an `owner` `pending_invitations` row, compensating-deleting the studio if the invite insert fails (no orphan). It NEVER inserts a practitioner — `handle_new_user()` still creates the owner on the invited first sign-in. RLS unchanged: `studios` has no INSERT policy and `pending_invitations` INSERT is owner-only, so a normal user cannot do this; the service-role path runs ONLY after the `isAdmin` gate. One PR #253 touch: a narrow, fail-closed middleware carve-out lets `isAdmin` operators reach `/admin/*` without a studio (a no-studio NON-admin still → `/no-access`), fixing a latent post-#253 admin lockout without weakening invite-only. Pinned by tests/lib/studios/new-studio.test.ts, tests/app/admin/new-studio-wizard.test.ts, tests/db/new-studio-wizard.db.test.ts, e2e/new-studio-wizard.spec.ts. Stripe gates unchanged (one paymentIntents.create). Live payments still disabled.

- **Invite-only auth and no-studio gate** (PR #253, no migration). Hardens Hone as INVITE-ONLY for supervised studios (Chloe/Laura); no public self-serve signup, no public studio creation. Audit finding: the DB layer was already invite-only (so no migration) — `handle_new_user()` (0081) creates a practitioner only with a matching `pending_invitations` row; uninvited sign-ins get an `auth.users` row but NO studio/practitioner; magic-link gates `shouldCreateUser` on an invitation; RLS leaves `studios` with NO INSERT policy and `practitioners`/`pending_invitations` owner-only INSERT; there is no `/signup` route or signup CTA. The real gap was UX/guarding: a no-studio user hit a raw 500. Fixes (no schema/payment/business-rule change): a redirecting guard `requirePractitionerWithStudio()` (anon→`/login`, authed-no-studio→`/no-access`) on the app-shell layout; `getCurrentPractitionerWithStudio()` keeps throwing as the backstop for server-action POSTs; a **middleware** studio gate that redirects no-studio users to `/no-access` BEFORE any (app) render (race-free, no page throws, no studio data loaded; exempts public routes + `/no-access` + its sign-out); a new `app/(auth)/no-access/page.tsx` safe gate ("No studio access yet" + Sign out + Contact Hone; self-checks anon→`/login`, has-studio→`/dashboard`; no app nav, no studio reads, noindex); and explicit invite-only copy on the login page. Public booking + marketing unchanged. Pinned by e2e/invite-only.spec.ts (real uninvited sign-in → `/no-access`; every app route redirects there; anonymous → `/login`), tests/db/invite-only-posture.db.test.ts (9; no-studio/member can't create studio/membership/invitation or escalate; owner can invite; handle_new_user provisions from an invitation), tests/app/invite-only-auth.test.ts (source pins). Stripe gates unchanged (one paymentIntents.create). Live payments still disabled; no paid self-serve onboarding.

- **Imported Treatment Memory schema** (PR #252, migration 0089). A SAFE DESTINATION for historical treatment memory migrated from paper cards / Jane / Fresha / spreadsheets, deliberately SEPARATE from live charting (imports are messy, possibly incomplete, manually typed, and must NOT be written as charted-in-Hone records — `sessions.practitioner_id` is NOT NULL, `modality` is CHECK-constrained, `started_at` defaults to now()). **Schema + read-model only**: no importer UI, CSV/TSV parser, OCR, AI extraction, image upload, API sync, scraping, or surfacing. Migration 0089 adds three tables: `import_batches` (the required unit of void/rollback; `source_type` CHECK paper_card/jane/fresha/spreadsheet/other; soft-void columns), `imported_treatment_memories` (durable rows; `client_id` REQUIRED, `import_batch_id` REQUIRED `on delete restrict`; clinical-ish values kept as TEXT plus `occurred_on` date AND messy `occurred_on_text`; per-row soft-void), and `imported_treatment_memory_audit_events` (dedicated append-only audit, separate from the record-keeping audit so it touches nothing there). Stores NO raw CSV/TSV/file/scan/image/OCR content. **RLS:** member SELECT (`is_studio_member`), owner-only INSERT+UPDATE (`is_studio_owner`), NO delete policy (correction is soft voiding, not hard delete); the audit table is SELECT-only for members, written only by a SECURITY DEFINER trigger (reusing `record_keeping_audit_actor`). **Read helper** `lib/imported-treatment-memory.ts` (pure builder + RLS-backed loaders) excludes voided by default, orders occurred_on desc nulls last then imported_at desc, caps, returns safe provenance labels ("Imported history, not charted live in Hone."), no service role, no sensitive data. Curated types (`ImportBatch` / `ImportedTreatmentMemory` / `ImportedTreatmentMemoryAuditEvent`) added + registered in the drift check. **Migration-first:** apply 0089 to production and verify tables/policies/grants/types BEFORE merging app code (this PR's app code is a read helper only, not yet wired to any surface). Quick Import V1 (PR #253) will need conservative client identity matching (no false merges) before writing memory rows. Pinned by tests/migrations/0089-* (shape), tests/lib/imported-treatment-memory.test.ts (pure builder + safety), tests/db/imported-treatment-memory.db.test.ts (18 live RLS proofs). Stripe gates unchanged (one paymentIntents.create). Live payments still disabled.

- **Pilot Love Loop V1** (PR #250, no migration). A tiny, MANUAL feedback layer to learn whether the supervised pilot (Chloe, Laura) finds Hone useful enough to tell other electrologists — NOT a referral engine, growth automation, AI, or marketing automation. No persistence, no analytics, no automated send, no contact access, no referral/tracking links. Everything is a plain `mailto:hello@hone.care` link (the established support address) opening the practitioner's own mail client. Pure builder `buildPilotFeedbackMailto(surface, intent)` (`lib/pilot/feedback-mailto.ts`) takes ENUM inputs only, so no client/treatment/system-sensitive data can reach the subject/body (body is the surface label + sentiment + an empty placeholder). A quiet server-component `PilotFeedbackPrompt` ("Was this useful?") sits on the Daily Prep Brief and Follow-up Assistant cards; a compact "Pilot learning" card ("Send feedback" / "Know another electrologist?") sits near the bottom of the Dashboard. Before Today was deferred. Optional marketing addition: a short "Built from real treatment rooms." origin-story section on the homepage (copy only, no claims/metrics, preserves treatment-memory positioning). No booking/intake/charting/Calendar/Record-Keeping/payment/RLS/schema change. Pinned by tests/lib/pilot/feedback-mailto.test.ts (9) + tests/app/marketing-homepage.test.ts and exercised by the core memory loop E2E. Stripe gates unchanged. Live payments still disabled.

- **Missing Records / Follow-up Assistant V1** (PR #249, no migration). The SECOND agentic-style workflow (after Daily Prep Brief, PR #241), same posture: RULES-BASED ONLY — no AI, no model call, no provider, no chatbot, no autonomous action, no writes/sends/charges. It surfaces recorded workflow gaps and follow-ups from recent appointments so the practitioner can finish the record: completed-but-uncharted ("Charting needed"), aftercare/risks not marked, probe lot missing from a treatment area, intake `in_progress` ("Intake incomplete"), and for-next-visit note with no upcoming appointment ("Follow-up"). The caution/watch item type was deferred (already covered by "Clients needing attention"). Pure builder `buildMissingRecordsAssistant` (priority order, dedup per client/type, cap 6, safe wording) + bounded studio-scoped loader `getMissingRecordsAssistant` in `lib/dashboard/missing-records-assistant.ts`; gap rules mirror `lib/sessions/before-today.ts` (probe lot / aftercare) and `lib/dashboard/next-action.ts` (completed-not-charted). Loader reads (read-only, RLS-backed): recent sessions + blocks, recent completed appointments with a `session_blocks!inner` charted check, upcoming appointments for follow-up clients, latest intake status. UI is a compact "Follow-up assistant" card under Practice Snapshot (`app/(app)/dashboard/follow-up-assistant.tsx`), link-only to existing safe routes, "Showing N of M" when capped, calm empty state "Nothing needs follow-up right now." Reads no sensitive surface (no exposure incidents, payment internals, Stripe ids, raw tokens, audit JSON); no service-role, no public route, no model/provider call. Pinned by tests/lib/dashboard/missing-records-assistant.test.ts (21) and exercised by the core memory loop E2E. Stripe gates unchanged (one paymentIntents.create). Live payments still disabled.

- **Marketing mature-SaaS visual polish** (PR #248, public-only). A live review found the homepage still read less mature than polished practice SaaS (Jane/Fresha/GlossGenius/Mangomint used only as benchmarks, not copied; category stays "Treatment memory for electrologists."). Four visual/layout-only fixes, copy preserved: (1) the **Privacy & Trust** section's awkward five-card 3+2 grid became a two-column section — left claim + summary + privacy-policy link, right ONE compact checklist card with all five claims (stacks on mobile); (2) **section rhythm** now strictly alternates band/default backgrounds (Records caveat card switched to white to stay legible on its band); (3) the **proof strip** items became white pills (less uppercase) with a slower marquee (38s → 46s), still contained and reduced-motion-aware; (4) the **Calendar-vs-Hone** cards now sit in the same `AppWindow` chrome as the hero and Before Today. No fake security badges/certifications/HIPAA/bank-level claims; no fake social proof. "Treatment memory for electrologists." still leads; no AI/agentic in the hero; no "Agentic support" nav item; demo data only; no medical/compliance/AI overclaims; no live-payment claim. Public marketing only (`app/page.tsx`, `app/globals.css`); no app/payment/RLS/schema/migration change. Pinned by tests/app/marketing-homepage.test.ts (27) + e2e/marketing-homepage.spec.ts. Live payments still disabled.

- **Marketing comparison and proof strip polish** (PR #247, public-only). A live review of the PR #246 homepage found two weak spots, both fixed visual/layout-only. (1) The proof strip wrapped awkwardly as a static uppercase row; it is now a **contained, edge-faded marquee ticker** (`.hone-marquee` in `globals.css`) — the track holds the five approved proof items twice and translates -50% for a continuous loop, `overflow:hidden` keeps it from widening the page, a mask fades both edges, and `prefers-reduced-motion` stops the animation. Proof items unchanged; still no fake testimonials/logos/usage numbers. (2) The Calendar-vs-Hone cards (bare row lists) became **two product-style cards** — a limited appointment card (Calendar-only · "Appointment data": 10:00 AM / Maya R. / Electrolysis / Confirmed) and a treatment-memory card (Hone · "Treatment memory") echoing Before Today (Remember-today band, Last-recorded chips Upper lip · Sterex · Lot L-204, Tolerance 4/5 · Mild redness, "Aftercare not marked last session"). Micro-context labels clarify the contrast; the unused `CheckRow`/`DashRow` atoms were removed; lead copy shortened. Jane/Fresha used only as benchmarks, not copied. "Treatment memory for electrologists." still leads; no AI/agentic in the hero; no "Agentic support" nav item; demo data only; no medical/compliance/AI overclaims; no fake social proof; no live-payment claim. Public marketing only (`app/page.tsx`, `app/globals.css`); no app/payment/RLS/schema/migration change. Pinned by tests/app/marketing-homepage.test.ts (27) + e2e/marketing-homepage.spec.ts. Live payments still disabled.

- **Marketing visual system and product proof polish** (PR #246, public-only). The product had outrun the site's visual polish (Chloe/Sam feedback): the copy was right but it did not yet read like a mature vertical SaaS. A small reusable visual system was added (Jane used only as a quality benchmark, not copied), with the human copy preserved and AI/agentic still toned down: an `AppWindow` chrome frame on the hero and the Before Today centerpiece so they read as real app screens; the calendar-vs-Hone contrast rebuilt as a thin dashed card vs a checkmarked card; a credible **proof strip** under the hero (Built with working electrologists · Mobile-tested treatment workflows · Browser-tested treatment-memory loop · Lot traceability built in · Founder-led setup) with NO fake testimonials/logos/usage numbers; **Before Today** promoted to a full-width centerpiece (recorded-history wording only); the privacy section as green check-cards; a "Review Before Today" action on the Daily Prep mockup; and faint alternating band backgrounds for section rhythm (`SectionShell tone="band"`, new `band` palette token, `Reveal` style passthrough). "Treatment memory for electrologists." still leads; no AI/agentic in the hero; no "Agentic support" nav item; demo data only (Maya R. / Jordan L. / Alex P. / Demo Studio / lot L-204 / Sterex); no medical/compliance/AI overclaims; no fake social proof; no live-payment claim. Public marketing only (`app/page.tsx`, `app/_components/Reveal.tsx`, `app/_components/marketingNav.ts`); no app/payment/RLS/schema/migration change. Pinned by tests/app/marketing-homepage.test.ts (25) + e2e/marketing-homepage.spec.ts. Live payments still disabled.

- **Marketing site visual density follow-up** (public-only). Pilot feedback (Chloe) was that PR #244's copy was right but the page felt too sparse (too much white space; the Records and Smarter-prep sections read as floating text). Visual proof was added while keeping every word of the human copy and without reintroducing AI/SaaS language: the Records section gained a printable **Procedure record** mockup (Maya R. / Sterex · L-204 / Aftercare Marked / "Print this client's procedure record") + supporting chips, with the public-health caveat moved to a small footnote card; the Smarter-prep section gained a **Daily prep "Tomorrow morning"** mockup (Maya R. / Jordan L. / Alex P., "Based on recorded Hone data.") matching the live rules-based Daily Prep Brief (PR #241); the hero card gained two proof tiles; and section padding was tightened. Jane's site was used only as a quality benchmark, not copied. "Treatment memory for electrologists." still leads; no AI/agentic in the hero; no "Agentic support" nav item; demo data only; no medical/compliance/AI overclaims; no live-payment claim. Public marketing only (`app/page.tsx`); no app/payment/RLS/schema/migration change. Pinned by tests/app/marketing-homepage.test.ts (20) + e2e/marketing-homepage.spec.ts. Shipped as a follow-up PR on top of merged PR #244. Live payments still disabled.

- **Marketing site human copy rewrite** (PR #244, public-only). Pilot feedback (Chloe) was that the site still read like it was written by AI — too polished, abstract, and "SaaS landing page", with too much startup/agentic language. The public copy was rewritten in a plain, human, practitioner-first voice. The **public category phrase is now "Treatment memory for electrologists."** (hero eyebrow + H1, footer caption, OG card, SEO metadata), and **treatment memory and records are the lead story, not AI/agentic.** Same eight-section skeleton, every heading/paragraph rewritten human: "Treatment memory for electrologists." → "Your calendar shows the appointment. Hone shows what to remember." → "Before, during, and after the appointment." → "What Hone remembers." → "Pull the record when you need it." → "Smarter prep, without autopilot." → "Your client records should stay yours." → "Founding pilot." / "See if Hone fits your studio." **AI/agentic language is out of the main pitch:** removed the "Agentic support, but practitioner-controlled." heading, the five-bullet internal-policy list, "No autonomous clinical decisions.", the hero "No autonomous treatment decisions." line, the "Agentic support" nav item, and "no AI model call." The forward-looking section states the docs/22 boundary once, in human words ("Future smart features should help with prep and drafts. They should not diagnose, recommend treatment settings, send messages, charge cards, or change records without you."). **docs/22 stays the internal agentic safety contract**; the public site no longer reads like the policy. Filler words banned (operating memory layer, AI-powered, intelligent assistant, seamless, empower, optimize, next-generation, all-in-one, unlock, transform your workflow, …) and no AI/agentic wording in the hero — all pinned. Visuals from PR #243 kept with plainer labels; demo data only (Maya R. / Demo Studio / lot L-204 / Sterex). Public marketing only (`app/page.tsx`, `app/layout.tsx`, `app/opengraph-image.tsx`, `app/_components/Marketing*`, `marketingNav.ts`): no authenticated app, payment, RLS, schema, or migration change. Pinned by tests/app/marketing-homepage.test.ts (20) + e2e/marketing-homepage.spec.ts. Live payments still disabled.

## Earlier production status (as of PR #243)

- **Marketing site YC-style cleanup** (PR #243, public-only). The PR #242 homepage was tightened from thirteen repetitive sections to eight: hero (one combined visual), calendar-vs-Hone comparison, How Hone works (before/during/after), product proof (Before Today / charting / procedure records / Daily Prep Brief as compact cards), Record Keeping (with the local public-health caveat), ONE merged "Agentic support, but practitioner-controlled." section, privacy/trust, and pricing/CTA. The standalone mobile/iPad section was dropped; copy was sharpened and de-duplicated; the nav trimmed to Product/Records/Agentic support/Pricing/Sign in + Book walkthrough. The required docs/22 safety phrases stay (Assistant not decider, Draft not send, Flag not diagnose, Summarize do not invent, Human confirmation before external actions, No autonomous clinical decisions); Daily Prep Brief still described live + rules-based; anonymized demo data only; no medical/compliance/AI overclaims; no live-payment claim. Public marketing only: no authenticated app, payment, RLS, schema, or migration change. Pinned by tests/app/marketing-homepage.test.ts + e2e/marketing-homepage.spec.ts. Live payments still disabled.

## Earlier production status (as of PR #242)

- **Marketing site treatment-memory overhaul** (PR #242, public-only). The public homepage (`app/page.tsx`) is repositioned around the product thesis: Hone is the treatment memory system for permanent hair removal studios, not booking/practice-management/AI-advice/payment software. New arc and sections: hero ("Treatment memory for permanent hair removal studios."), calendar-vs-Hone comparison, Before Today, Daily Prep Brief (described LIVE + rules-based, matching PR #241), charting, Record Keeping (with the local public-health responsibility caveat), mobile/iPad, agentic practice support, agentic safety (aligned with docs/22), privacy/trust, $19/month founding pilot, final CTA. Visuals are coded mockups in the app's card/chip language with anonymized demo data ONLY (Maya R. / Demo Studio / lot L-204 / Sterex); never Chloe/Laura/Willow or real data. SEO metadata leads with the category ("Hone | Treatment Memory for Electrologists"). Shared marketing header/nav gained story-section anchors + a Book walkthrough CTA, overflow-safe via an lg breakpoint and a CTA in the mobile menu. No medical/compliance/AI overclaims; no claim live payments are active. Public marketing only: no authenticated app, booking, intake, charting, Calendar, Record Keeping, payment, RLS, schema, or migration change. Pinned by tests/app/marketing-homepage.test.ts + e2e/marketing-homepage.spec.ts. Live payments still disabled.

## Earlier production status (as of PR #241)

- **Daily Prep Brief V1** (PR #241, no migration). The first agentic-style workflow, RULES-BASED ONLY: no AI, no model call, no provider integration, no chatbot, no autonomous action. A pure helper (`lib/dashboard/daily-prep-brief.ts`) turns facts the Dashboard already loads (visible appointments, Before Today previews, the PR #236 linked-session charting state, intake status) into a deterministic, priority-ordered prep list rendered as a compact "Daily prep brief" card directly under Today. Zero new queries: the Before Today preview gained three passthrough fields already computed by `buildBeforeToday` (nextVisitNote, cautionNote, granular reminders); the Today-row preview is unchanged. Priority: 1 next-visit/caution note, 2 intake not reviewed, 3 charting needed, 4 missing-record reminder, 5 new client, 6 already charted. Recorded-history wording only; reads no sensitive surface (no exposure incidents / payment internals / Stripe ids / raw tokens / audit JSON); studio-scoped, no service-role, no public route, no model/provider call; links only to `/clients/<id>`; writes and sends nothing. This implements the docs/22 "Daily Prep Brief V1" workflow per the agentic safety plan. The remaining two workflows (Missing Records / Follow-up Assistant, Draft-only Client Message Assistant) are still future PRs. Live payments still disabled.

## Earlier production status (as of PR #240)

- **Agentic readiness and safety plan** (PR #240, docs-only). The strategic direction is to make Hone agentic, but safely: an operating memory system that prepares, flags, summarizes, and drafts from recorded treatment history, never a chatbot and never a decider. **No AI runtime, model call, endpoint, migration, schema, RLS change, or payment capability is added by this PR.** It adds `docs/22_AGENTIC_READINESS_AND_SAFETY.md`, the rules any agentic build must satisfy first: assistant not decider, draft not send, flag not diagnose, summarize recorded history (do not invent), require human confirmation before any external action, never silently mutate clinical history, never auto-charge, never auto-message clients. The doc fixes the safe read surfaces, the excluded sensitive surfaces (exposure incidents + audit payloads, payment internals, Stripe ids, raw appointment + calendar feed tokens, auth/session, cross-studio, anything outside RLS, mirroring Global Search V1 PR #232 and the exposure-incident owner tier PR #222), the hard prohibitions, per-action human confirmation rules, the safe-wording vocabulary, the first three future workflows (Daily Prep Brief V1, Missing Records / Follow-up Assistant, Draft-only Client Message Assistant), and the audit + RLS posture (same studio-scoped access, no service-role broad AI search, no cross-studio memory, no public AI endpoints, exposure incidents stay owner-tiered, Stripe grep gates still apply). The agentic roadmap starts with this safety plan; the next possible PR is Daily Prep Brief V1, read-and-draft only. Live payments still disabled.

## Earlier production status (as of PR #238)

- **Chloe pilot feedback cleanup** (PR #238, no migration). Her retest was strongly positive ("in a heartbeat now"); five UX-only fixes closed the remaining friction: mobile search input is text-base so iOS stops auto-zooming on focus (user zoom untouched); client page sections are a native select on phones (md+ keeps the tab row; same pick() navigation); the session page gains a Finish up section (explains per-piece saving, Done charting -> client Sessions tab, Review appointment & billing -> /calendar/<appt> when linked; links only, no new write path); Records section reads "Procedure records" with "Choose a client" + one-client helper (filter/print behavior unchanged, formal printed title unchanged); Dashboard puts Today first and collapses the Getting started card to a "Setup complete" footer link once all auto steps are done (incomplete keeps the card below Today; /getting-started route unchanged). Live payments still disabled.

## Earlier production status (as of PR #237)

- **Before Today visual hierarchy** (PR #237, no migration). The client-page briefing card now reads in pre-treatment order: Remember today (watch + For-next-visit note) first as the dominant blue band; Last treatment snapshot (date, areas, wrapping chips: modality, frequency, probe, lot, energy level, minutes); Client response (last recorded) with tolerance/reaction chips and the last treatment's reaction notes; Record reminders last. Pure reorder of lib/sessions/before-today.ts + components/before-today-card.tsx; two new pure input fields (blockMinutes, blockReactionNotes) mapped from already-selected block columns; no new queries, no rule changes. New-client empty state explains memory appears after the first charted session. Dashboard Today compact preview deliberately unchanged (latestSetupLine kept for it). Both E2E specs assert the new sections + phone overflow. Live payments still disabled.

## Earlier production status (as of PR #236)

- **Dashboard Today next actions** (PR #236, no migration). Each Today row shows ONE primary action from a pure resolver (lib/dashboard/next-action.ts): linked charted session -> View session + Charted chip; linked empty session -> Continue charting; completed uncharted -> Chart appointment + Charting needed chip; cancelled/no-show -> Open client; upcoming returning -> Review Before Today; upcoming new -> Open client. Two batched session/blocks reads; row body keeps the calendar link, action is a sibling button. Both E2E specs assert the action branches. Live payments still disabled.

## Earlier production status (as of PR #235)

- **Mobile charting comfort** (PR #235, no migration). The session page gains a "Risks & aftercare" section (same AftercareExplainedToggle + markAftercareExplainedAction as the Records row; no new write path) so the stamp is markable where charting happens; side chips bumped to comfortable touch size; sticky save bar deferred (documented). Mobile E2E now charts end to end at 390px with per-control reachability + viewport assertions and confirms the Before Today loop; iPad opens the charted session page with overflow asserted. Live payments still disabled.

## Earlier production status (as of PR #234)

- **Mobile header and search sheet polish** (PR #234, no migration). Mobile Search and Menu panels are viewport-fixed sheets under the header (fixed inset-x-3 top-16; the old icon-anchored absolute panel pushed the search input off-screen left). Menu identity block compact ("My account" headline when the display name is an email; studio · role; address as small tertiary text). Desktop search/account dropdown untouched. E2E geometry assertions (panels/input/Close fully inside viewport) + Close/Escape dismissal added. Also: a public-surface mobile sanity sweep (booking, confirmation, manage/cancel/reschedule, invalid token, intake) asserting no overflow and no authenticated-shell leakage; zero fixes needed; deeper /portal mobile polish deferred. Live payments still disabled.

## Earlier production status (as of PR #233)

- **Mobile client page polish** (PR #233, no migration). Client detail header is mobile-first (compact name, small Edit button, stacked contacts); Log session + Book appointment paired in one action row with a short helper; profile tab bar is a contained one-row scroller on phones (desktop unchanged). No action/business-logic change. E2E client-detail step extended (actions reachable, six tabs reachable, pinned notes visible, overflow assertions). Live payments still disabled.

## Earlier production status (as of PR #232)

- **Global Search V1** (PR #232, no migration). Header search on both breakpoints (desktop inline input; mobile magnifier icon + panel): clients (name/email/phone), appointments (client/service/status keyword), treatment memory (areas, cautions, reactions, probe labels/lots, next-session notes; safe labels), Records (sterile lots w/ traceability link, disinfectants), page shortcuts. Single user-scoped "use server" action, explicit studio_id on every query, caps (12 total), app-internal hrefs only; exposure incidents/audit payloads/payment internals/tokens excluded and pinned. No AI/index/external service; trigram-index follow-up documented. E2E coverage in both specs. Live payments still disabled.

## Earlier production status (as of PR #231)

- **Account menu + header navigation polish** (PR #231, no migration). Desktop primary nav = Dashboard/Clients/Calendar/Records; Settings/Admin/Sign out moved into a new account dropdown (first-name trigger, aria-expanded, profile block with name/studio/role, Settings + Getting Started + Admin + Sign out; same dismissal model as MobileMenu). Mobile Menu reordered as an account panel (profile block on top, nav, then Settings/Getting Started/Admin/Sign out). Bell unchanged on both breakpoints. All PR #228-#230 header/calendar behaviors retained and E2E-asserted. Live payments still disabled.

## Earlier production status (as of PR #230)

- **Mobile menu outside-tap dismissal** (PR #230, no migration). MobileMenu closes on any pointerdown outside its root (listener active only while open); link-tap/current-page/Escape/Sign-out dismissal from PR #229 unchanged; bell stays independently usable. Also: the authenticated Hone wordmark is a Dashboard link again (aria-label "Go to Dashboard"; old Today-tab conflict no longer applies; marketing header untouched). E2E outside-tap + wordmark-navigation steps added; all PR #228/#229 mobile assertions retained. Live payments still disabled.

## Earlier production status (as of PR #229)

- **Mobile header polish** (PR #229, no migration). Mobile Menu is now a client component that auto-closes on every link tap (incl. current page), Escape, and Sign out (`app/(app)/MobileMenu.tsx`; the PR #228 details element stayed open across client-side navigations). Notifications moved from a nav tab to a header bell on both breakpoints (server-rendered link + unread badge; accessible name carries the count); desktop Notifications tab removed. Notifications page/badge logic and PR #228 calendar touch-safety unchanged (E2E regression green). Live payments still disabled.

## Earlier production status (as of PR #228)

- **Mobile and iPad UX stabilization** (PR #228, no migration). Phone nav overflow fixed: full nav row is md+ only; phones get a details/summary Menu (all destinations + badge + Sign out). Calendar touch safety: grid drag/click-create is mouse-only, `touch-action: manipulation` restores native scrolling, explicit coarse-pointer-only "+" button per day column opens the quick-book drawer (time editable); week grid scrolls inside its card on phones (min-w + existing overflow-x-auto). No booking/business-rule change. Proven by e2e/mobile-ux.spec.ts (iPhone + iPad + desktop-regression contexts) plus 10 source pins; manual native-gesture smoke in docs/12. Live payments still disabled.

## Earlier production status (as of PR #227)

- **Browser E2E core memory loop** (PR #227, no migration). Playwright (Chromium, one spec) drives the full treatment-memory loop against a LOCAL production build + LOCAL Supabase stack: booking, intake, REAL magic-link login via Mailpit (no auth bypass; invite-path account), dashboard + charted-24h wording, charting with clinical-memory fields, second booking, Before Today memory, filtered procedure record + print + aftercare mark, anonymous lockout. New separate `browser-e2e` CI job (local stack, no secrets, traces on failure only); `npm run test:e2e` locally after `supabase start` + `db reset`. Local-only by construction (hosted URLs refused; supabase-demo JWTs only; dummy provider keys). supabase/config.toml allow-lists the localhost:3111 E2E origin (local-only setting). Pins in tests/scripts/e2e-guardrails.test.ts. Live payments still disabled.

## Earlier production status (as of PR #226)

- **Post-hardening docs drift cleanup** (PR #226, docs/source-pin only). Root-level and operational docs corrected to the post-#218/#220/#221/#222/#223/#224/#225 reality: canonical charge path `session-payment-charge.ts` (CONTRIBUTING, docs/11, docs/14 guidance blocks), `payment_charge_attempts` as the canonical CHECK-pinned ledger (README), db-integration lane + types drift check acknowledged (README, docs/00, docs/03), supervised-pilot vs paid-launch line stated (docs/00), docs/11 dormancy recipes fixed to query both ledgers and expect the right gate output. 11 pins in tests/docs/docs-drift-pr226.test.ts. No runtime change. Live payments still disabled.

## Earlier production status (as of PR #225)

- **Charted-within-24h metric** (PR #225, no migration). Practice Snapshot card: completed appointments (status `completed`, `ends_at` in the rolling last 7 days) vs those whose earliest non-deleted linked session_block was created within 24h of `ends_at` (inclusive). Pure `summarizeChartedWithin24h` + three batched user-scoped reads in `getPracticeDashboardMetrics`; sessions with zero areas do not count as charted; studio-level only, never per-practitioner; no score/compliance wording (pinned). Empty state "No recent completed sessions yet." No RLS/payment change. Live payments still disabled.

## Earlier production status (as of PR #224)

- **New studio setup runbook** (PR #224, docs-only). `docs/20_NEW_STUDIO_SETUP_RUNBOOK.md`: internal operator checklist for safely creating Studio #2 (inputs; two approved SQL inserts: studio + owner invitation via the 0081 invite path; in-app config for the rest; surface verification; Willow isolation checks; ZZ-TEST smoke with delete-hardening-respecting cleanup; do-not-touch list; known limitations). Not a user-facing onboarding surface; no runtime/migration/RLS/payment change. Safety content pinned in tests/docs/new-studio-runbook.test.ts. Live payments still disabled.

## Earlier production status (as of PR #223)

- **Per-client procedure record filter + print** (PR #223, no migration). Records → Client Procedure Records: client select + optional studio-timezone date range (GET form, shareable URL); print accepts the same params (`/records/print?section=procedures&clientId&from&to`) with a "Filtered: client ..." header line and a clear empty state. Default 30-most-recent view unchanged; filtered pulls capped at 200 (cap explained in UI). Shared `utcInstantsForLocalDayRange` keeps screen and print identical; params sanitized via `normalizeProcedureRecordFilter`. machine_frequency now shown on items lines where recorded. No RLS change; studio scoping + user-scoped client unchanged; PR #222 exposure tier untouched. Live payments still disabled.

## Earlier production status (as of PR #222)

- **Exposure incident owner access tier** (PR #222, **migration 0088, policy-only; apply to prod BEFORE merge after SQL approval**). `record_keeping_exposure_incidents`: SELECT/UPDATE owner-only (`is_studio_owner`), INSERT stays member-wide (staff can report without browsing history), still no DELETE. Audit SELECT carve-out: exposure-incident audit rows owner-only (they carry old/new values); other record types unchanged; immutability untouched. UI: owner-only note for non-owners on Records + Print (Add form stays); update action role-checks. Privacy hardening before multi-practitioner studios; no change for solo Willow. 10 new DB-lane tests. Live payments still disabled.

## Earlier production status (as of PR #221)

- **Generated types drift check** (PR #221, no migration). `scripts/check-db-types.mjs` (`npm run check:db-types`; runs in the `db-integration` CI job after `npm run test:db`) regenerates types from the LOCAL migrated database (`supabase gen types typescript --local`, hardcoded local) and exact-matches column sets both directions for 15 curated tables vs the hand-rolled `lib/types/database.ts`, pins 11 recent columns individually, and asserts DB-side presence of the relied-upon payment/webhook columns. First run caught six live columns missing from the app types (calendar_feed_token_hash, terms/privacy stamps, normalized_email); declarations added (types-only, additive). Hosted/non-localhost env URLs refused; no production access. Pins in `tests/scripts/db-types-drift.test.ts`. Deferred: nullability comparison, non-curated tables. Live payments still disabled.

## Earlier production status (as of PR #220)

- **DB/RLS integration test harness** (PR #220, no migration). `tests/db/` + `npm run test:db` (vitest.db.config.ts) run against a LOCAL Supabase Postgres only: `supabase db start && supabase db reset --local` applies migrations 0001-current from scratch, then the suites exercise real RLS/triggers/claim-RPCs/constraints as the `authenticated` role (request.jwt.claims simulation). v1 covers cross-studio isolation, audit immutability + triggers, the 0087 delete posture, the double-booking constraint (incl. buffer trigger), and claim_email_send / claim_session_payment_charge_attempt. New CI job `db-integration` (separate from the fast lane; no secrets, no --linked). The harness refuses non-localhost/hosted URLs by construction; guardrails pinned in `tests/scripts/db-harness-guardrails.test.ts`. Deferred: generated-types drift check, portal/anon token-route policies, storage policies, browser E2E. Live payments still disabled; payment runtime untouched.

## Earlier production status (as of PR #218)

- **Dead legacy fee executor removed** (PR #218, no migration). `lib/billing/manual-fee-charge.ts` deleted (zero runtime imports since #196); legacy `manual_fee_charge_attempts` stays as read-only history (one SELECT in eligibility). Stripe gate tightened: paymentIntents.create exactly 1 (unified executor only); other gates unchanged. 36 per-PR gate-pin clones consolidated into the canonical gates test. Live payments still disabled; docs/18 blockers unchanged.

## Earlier production status (as of PR #217)

- **Clinical RLS delete hardening** (PR #217, **migration 0087, policy-only; apply to prod BEFORE merge after SQL approval**). Production catalog confirmed live FOR ALL policies let authenticated members hard-delete clinical history; 0087 replaces them with per-command policies: NO DELETE on clients/sessions/session_blocks/photos/probe_lots/intake/tags/goals/personal-notes; DELETE kept explicitly only for entries/plan-stages/client-pricing (real UI affordances). App fixes: block-cleanup soft-deletes; plan rollback closes (old delete was a silent no-op since 0024). DB/RLS integration harness still an open follow-up. Live payments still disabled.

## Earlier production status (as of PR #216)

- **Getting Started future-onboarding section removed** (PR #216, no migration). /getting-started keeps the six sections + first-consultation readiness; the "Before onboarding another practitioner or studio" section is gone (scale checklist, not user setup); the record-keeping public-health caveat relocated into the first-consultation section; live-payments-off posture unchanged; Dashboard card and honest progress counts unchanged. Live payments still disabled; docs/18 blockers unchanged.

## Earlier production status (as of PR #215)

- **Getting Started checklist** (PR #215, no migration). `/getting-started` + Dashboard progress card: six sections (basics/booking/charting/records/daily/payments) with auto-detected Done/To-do from existing data + Review guidance badges; honest progress counts auto items only; first-consultation and Laura-readiness lists; payments posture explicit (live off, reviews pending). No manual mark-done persistence in V1 (migration deliberately deferred). Live payments still disabled; docs/18 blockers unchanged.

## Earlier production status (as of PR #214)

- **Clients needing attention** (PR #214, no migration). Fourth Action-needed card + compact list on the Dashboard (top 5, "+ N more"): clients flagged by their newest watch/caution note or next-visit plan (PR #203 per-client rule) or a notable recorded reaction on the most recent charted session; tolerance shown only alongside another reason; watch-first sorting; two batched reads, 200-session cap. Recorded-history surfacing only (unsafe wording pinned absent). All deferred Dashboard follow-ups now closed except record-keeping CSV export + traceability print. Live payments still disabled; docs/18 blockers unchanged.

## Earlier production status (as of PR #213)

- **Probe lot traceability** (PR #213, no migration). Record Keeping -> Sterile Items gains a lot search + per-record Trace usage (`/records?section=sterile&lot=...`): matching sterile-item details + every treatment area recorded with the lot (client/date/area/operator/setup/aftercare status, linked). Exact normalized matching (trim + case-insensitive escaped ILIKE; never fuzzy). Traceability, not causation (pinned). Print/export for traceability deferred (screen-only v1). Live payments still disabled; docs/18 blockers unchanged.

## Earlier production status (as of PR #212)

- **Before Today previews on Dashboard Today** (PR #212, no migration). Each Today roster row shows a compact briefing (Remember line with Watch-over-Plan, Latest setup, Records reminder count) rendered from the exact PR #211 pipeline via three batched reads (never per-appointment). Full Before today card unchanged on the client Overview. Read-only; recorded-history wording. Live payments still disabled; docs/18 blockers unchanged.

## Earlier production status (as of PR #211)

- **Before Today card** (PR #211, no migration). Pre-treatment briefing on the client Overview (below Client info, above Treatment Intelligence): last treated areas+date, Remember today (watch/plan + latest reaction/tolerance, blue memory styling), latest recorded setup, and record reminders using the procedure-completeness field rules (missing lot/aftercare/DOB/phone/address). Pure assembler over data the page already loads; recorded-history wording only (overclaim words pinned absent); clean empty states. Live payments still disabled; docs/18 blockers unchanged.

## Earlier production status (as of PR #210)

- **Client Treatment Intelligence Summary** (PR #210, no migration). Treatment Intelligence card on the profile Overview (pure builder over sessions + session_blocks + entry hairs): overall sessions/areas/minutes/hairs/hairs-min/first-last treated, per-area cards (grouped case-insensitively; latest recorded setup; commonly recorded reaction; watch notes), client-level reaction/tolerance, Notes to remember (latest watch + plan). Recorded-history wording only (overclaim words pinned absent); "Not recorded" for gaps; empty state for uncharted clients. Live payments still disabled; docs/18 blockers unchanged.

## Earlier production status (as of PR #209)

- **Header navigation fit** (PR #209, no migration). Top-nav label shortened to "Records" (route /records and the "Record Keeping" page heading unchanged); nav links are whitespace-nowrap with a tighter sub-md gap so the post-#208 header fits without wrapping. UI-only. Live payments still disabled; docs/18 blockers unchanged.

## Earlier production status (as of PR #208)

- **Practice Dashboard V1** (PR #208, no migration). /dashboard (nav renamed Dashboard; still the login landing; Today roster preserved as a section) gains a period filter (today/week/month, default week, studio tz), appointment counts, booked/completed SERVICE VALUE (service-menu prices; never "revenue"; required helper copy pinned), an explicit payments-posture card (live Off / test Available / collected revenue not enabled; period test-mode counts), and action cards (incomplete procedure records / missing probe lots / aftercare not marked -> Record Keeping procedures). Clients-with-watch-notes deferred. Live payments still disabled; docs/18 blockers unchanged.

## Earlier production status (as of PR #207)

- **Record Keeping print / export** (PR #207, no migration). Protected `/records/print?section=...` renders an inspector-friendly print document per section (studio + generated UTC stamp header, all BodySafe fields, "Not recorded" for gaps, created/updated stamps, opt-in ?history=1 audit appendix); window.print() with chrome print:hidden; Print / Export button on each Record Keeping section. No CSV/PDF/email/storage. Not a compliance guarantee; public-health review still needed. Live payments still disabled.

## Earlier production status (as of PR #206)

- **Record Keeping audit trail + edit** (PR #206, **migration 0086**: `record_keeping_audit_events`, SELECT-only RLS, trigger-written via narrow security-definer functions; apply to prod BEFORE merging). Append-only for normal users (no insert/update/delete/for-all policies); events: logbook created/updated diffs, aftercare_marked/cleared, probe_lot_updated (column-scoped, no noise). Edit forms shipped for the three logbooks (no delete/archive anywhere). History expanders in the Record Keeping UI. Not a compliance guarantee; public-health review still needed. Live payments still disabled.

## Earlier production status (as of PR #205)

- **Record Keeping tab + probe lot numbers** (PR #205, **migration 0085**: three RLS record tables + `session_blocks.probe_lot_number` + `sessions.aftercare_and_risks_explained_at/_by`; additive; apply to prod BEFORE merging). Top-level `/records` tab (not under Settings) with Sterile Items / Disinfectants / Exposure Incidents (sensitive; studio-RLS) / generated Client Procedure Records ("Not recorded" for missing data; explicit reversible aftercare toggle). Probe lot/batch captured in charting (Probe section), shown in area summaries and procedure records. Record-keeping support, not a compliance guarantee; needs public-health review. Live payments still disabled.

## Earlier production status (as of PR #204)

- **Charting field order + blue From-last-visit band** (PR #204, no migration). Treatment-area form order matches Chloe's flow: area, frequency, probe, mode, modality (+EL), readings, pulse count, hairs treated, minutes performed (layout-only; payload/copy/sticky unchanged). "From last visit, for today" renders BLUE everywhere (treatment memory, not warning); same PR #203 pre-client source. Live payments still disabled; enablement blockers unchanged (docs/18 §16).

## Earlier production status (as of PR #203)

- **Sticky machine frequency + chip polish + Sessions Watch/Plan source** (PR #203, **migration 0084**: `practitioners.default_machine_frequency`, additive + nullable; apply to prod BEFORE merging). Machine frequency toggle defaults from the practitioner's last-used value (saved best-effort by the treatment-area save actions; editable per area; block row still stores the actual value). Reaction/response chips show leading + like the other observation chips. The Last treatment card's Watch/Plan band uses `pickPreClientWatchPlanSource` (newest session with any watch/plan content), matching the charting page's pre-client context. Live payments still disabled; enablement blockers unchanged (legal/accounting review + Willow Stripe checklist, docs/18 §16).

## Earlier production status (as of PR #202)

- **Payment default service label** (PR #202, UI/copy only). The Session payment prepare form shows "Booked service: <name> (<N> min)" directly under the Amount field whenever the PR #200 default resolved (both service-price and custom-pricing sources; reminder line kept; field editable; no fake label when no default). No resolver/action/executor/gate change. **Numbering: controlled live enablement (docs/18 "PR #202") shifts to the next available PR number.** PR #196 fee smoke closed; live payments still disabled; remaining enablement blockers: legal/accounting review + Willow live Stripe checklist (docs/18 §16).

## Earlier production status (as of PR #201)

- **Live payments gate preparation** (PR #201, no migration, NO live enablement). Receipt template renders a cautious live-mode variant behind `livemode` (default false; test branch byte-identical; sender pins `livemode: false` and refuses non-test rows, so live copy is unreachable; final wording needs legal/accounting review). **Refunds are owner-only** across session payments and fees ("Only the studio owner can issue a refund."). Audit decision: the ledger row is the audit record (no new table). Stale pending_stripe recovery verified already shipped (deterministic idempotency + reconciliation + ops alerts). docs/18 §16: UI copy map, Willow Stripe readiness checklist, legal/accounting checklist, PR #202 runbook (FUTURE ONLY). **PR #196 fee smoke is fully closed** (both legs backend-verified clean 2026-06-11; gate on PR #201 cleared). Live payments still disabled.

## Earlier production status (as of PR #200)

- **Last treatment warning placement + service price default** (PR #200, no migration). The "From last visit, for today" Watch/Plan box is now a flush footer band INSIDE the Last treatment card (`attached` variant on `FromLastVisitForToday`; `hasFromLastVisitContent` gate; once per Sessions tab; entries-fallback sessions no longer drop their plan note). Session payment prepare amount defaults from the booked service via the pure `resolveSessionPaymentDefault` (`lib/billing/session-payment-default-amount.ts`): client custom pricing (name-matched, newest effective, future-dated ignored) > service menu price > historical session price > blank; source copy shown, field stays editable, practitioner can override before preparing. Executor/eligibility/gates untouched; no Stripe call in defaulting. **PR #196 fee smoke still pending; live payments still disabled.**

## Earlier production status (as of PR #199)

- **Last treatment + charting redundancy + client info cleanup** (PR #199, no migration). Sessions-tab top card is now **Last treatment**: `pickLastTreatment` (pure helper in `lib/sessions/clinical-summary.ts`) picks the most recent session with charted areas or legacy entries, so a newer empty session can't blank the pre-appointment summary ("Most recent charted treatment" note when that happens; empty state "No charted treatments yet."). ONE "For next visit" surface: per-area next-visit/caution inputs removed from the charting form (caution columns untouched; data round-trips and still renders in watch lines). ONE "Performed by" surface: inline line under the session title with Edit (new `SessionPerformerLine`; `session-info-card.tsx` deleted). Detach renders inside the treatment plan card (`detachSlot`). Birthday is a plain row in Client info (no nested box/helper; edit via the card's Edit link). **PR #196 fee smoke still pending; live payments still disabled.**

## Earlier production status (as of PR #198)

- **Chloe iPad retest fixes** (PR #198, no migration). Messages tab works (`isProfileTab` had omitted "messages"); Last Session pinned to carry cautions/next-visit context; charting order readings -> Client tolerance (rating only) -> Treatment observations (reaction chips, same reaction_type field) -> For next visit; session header price block removed (performed-by editable); Overview "Client info" card (birthday + emergency + address, Edit link); calendar today contrast raised again. **PR #196 fee smoke still pending; live payments still disabled.**

## Earlier production status (as of PR #197)

- **Chloe launch polish round 3** (PR #197, no migration). Unified History group (Charted + Session history merged; walk-in fallback list); Last session keeps per-area settings; one free-text box per area in charting (Client tolerance = rating + dropdown; legacy response notes still visible); session price is a collapsed optional disclosure (no more "$0"); dedicated Messages tab (off Overview); Overview ordered pinned/allergies/Skin first; stronger calendar day separators. Backlog: retail/add-on tracking. **PR #196 fee path still needs its production test-mode fee smoke; live payments remain disabled.**

## Earlier production status (as of PR #196)

- **Payment ledger unification** (PR #196, migration 0083). Fees (no-show / late-cancel) now prepare, charge, receipt, refund, and reconcile on `payment_charge_attempts`; the legacy `manual_fee_charge_attempts` table gets no new runtime writes (historical rows readable). Claim RPC reason guard widened (0083) with three additive evidence columns. Live payments still blocked; gates unchanged. docs/18 P0 #2 closed; remaining blockers: live receipt copy + legal/accounting review, test-mode copy pass, controlled enablement.

## Earlier production status (as of PR #195)

- **Ops alert app-path smoke action** (PR #195, no migration). Admin-only "Send test critical alert" button on `/admin/ops-alerts` calls the real `recordOpsAlert` (event `smoke_test_critical_alert_app_path`), deterministically exercising the durable-row + critical-email pipeline added in PR #193. `OPS_ALERT_EMAILS` is set in Production and baked in via redeploy `8a5a3e3`.

## Earlier production status (as of PR #194)

- **Chloe launch polish round 2** (PR #194, no migration). Treatment time card reframed as a tracker (goal UI hidden); collapsible session groups (only Needs charting open; Cancelled and no-shows merged; Session history renamed + collapsible); one-tap "Copy areas and settings from last session" onto an empty chart (never copies client response; duplication-proof); client-page Last session uses the shared per-area summary; treatment-plan card owns plan context (detach-only attachment state, redundant session-number line hidden when attached); "Session price" replaces "Price paid"; "For next visit" replaces "Plan for next visit"; Overview puts allergies first and collapses messages; birthday collects an optional real YEAR on date_of_birth (no migration); calendar today contrast strengthened for iPad. Deferred: practitioner portal/intake preview, studio logo upload. **Pre-launch blocker: awaiting Chloe's health inspector documents.** Rollout: Laura after Chloe's real consultations; Teresa (laser) later; Brooks later; calendar sync not required for launch. No payment/Stripe/auth/export/reminder change (gates unchanged from PR #193).

## Earlier production status (as of PR #193)

- **Ops alerts dashboard + critical notifications** (PR #193, no migration). The docs/18 §6 P0 live-payment blocker is closed: `/admin/ops-alerts` (ADMIN_EMAILS-gated) lists unresolved alerts critical-first with safe metadata and a conditional mark-resolved action (0067's `resolved_*` columns; no migration); `recordOpsAlert` dispatches a critical-only operator email AFTER the durable insert via the new standalone `lib/ops/alert-email.ts` (reads `OPS_ALERT_EMAILS`, bare Resend client, no appointment-email import, never calls recordOpsAlert, never throws; unset env = dashboard/rows still work). **Set `OPS_ALERT_EMAILS` in Production to activate emails.** Remaining live-payment blockers: ledger unification (#194), live receipt copy + legal/accounting review (#195), test-mode copy + controlled enablement (#196). Stripe gates unchanged from PR #192.

## Earlier production status (as of PR #192)

- **Live payments readiness audit** (PR #192, docs + gate-pin tests only; no runtime change). `docs/18_LIVE_PAYMENTS_AUDIT.md` is now the current readiness picture: **NOT READY FOR LIVE PAYMENTS (4/10; internal test-mode only)**. P0 blockers: ops-alert human visibility, fee-ledger unification, live receipt copy, legal/accounting review, test-mode copy pass. Agreed sequence: #193 ops alerts dashboard/notifications, #194 ledger unification, #195 gate preparation, #196 controlled enablement. Stripe gates verified unchanged and strong.

## Earlier production status (as of PR #191)

- **Treatment memory UX cleanup** (PR #191, no migration). Eight fixes from Chloe's practitioner smoke of PR #190: plan-area seed applies only to a session's first treatment area (no auto-filled repeat areas); "Copy settings from last treatment area" copies the full configuration (now incl. minutes), prefers a same-named area, and explains what it copied; "Plan for next visit" shows explicit saved/cleared/error feedback (action returns a result); back navigation from session pages lands on the client's Sessions tab; last-session summaries are PER treatment area (`areas[]` + `watchLines[]` in `lib/sessions/clinical-summary.ts`, shared render in `components/last-session-summary.tsx`); the duplicate amber/blue warning boxes merged into one "From last visit, for today" box (Watch + Plan); the charting form is bucketed into Treatment observations / Client/skin response / For next visit; the Sessions tab is ordered treatment time, last session, Needs charting above Upcoming, then "Session history" (renamed from "All sessions"). Practitioner copy says "treatment area", never "block". No payment/Stripe/auth/export/reminder change (gates unchanged from PR #190).

## Earlier production status (as of PR #190)

- **Clinical memory moat, phase 1** (PR #190, migration 0082). `session_blocks` gains structured client response (`tolerance_rating` CHECK 1..5, `reaction_type` CHECK 7-value vocabulary, `reaction_notes`, `caution_for_next_session`, `caution_note`); `sessions` gains `next_session_note`. Captured in the charting form's optional "Client response" section and the session page's "Plan for next visit" card. Surfaced at the point of care through the shared unit-tested `lib/sessions/clinical-summary.ts` formatter: upgraded appointment detail "Last session" card (areas, settings, probe, worst tolerance, reactions, caution, next-visit note), new-session "Previous session context" panel, and a "From last visit, for today" charting banner. All additive and nullable; pre-#190 records render unchanged; no RLS change; no payment/Stripe/auth/export/reminder change (gates unchanged from PR #189).

## Earlier production status (as of PR #189)

- **Pilot-safety fixes: email claim, export gate, invite-only login** (PR #189, migration 0080). (1) The 24h/2h reminder cron now claims each row atomically via `claim_email_send` (mirror of the SMS claim from 0049: conditional UPDATE on sent-is-null + 3-attempt cap + 5-minute stale-claim window; new `confirmation/reminder_24h/reminder_2h_claimed_at` columns) before calling Resend, and records outcomes via `record_email_result` (stamps sent_at on success, clears the claim, no double-increment); overlapping cron runs can no longer double-send, and both RPCs are service_role-only. Unclaimed one-shot email paths keep `record_email_attempt`. (2) `exportStudioDataAction` is owner-only with a generic refusal, and every successful export writes a fail-closed `audit_logs` row (`studio_export`, actor, studio, filename + file list + row counts). (3) Practitioner login is invite-only at two layers: the magic-link request runs through a server action gating `shouldCreateUser` on a pending invitation (generic responses, no enumeration oracle), and migration 0081 removed `handle_new_user()`'s no-invite fresh-studio fallback so even Google OAuth (which cannot pass shouldCreateUser) provisions no studio/practitioner for uninvited users; the invited arm (inviting-studio placement + terms stamping) is unchanged. No payment, Stripe (gates unchanged from PR #187), live-mode, portal, SMS, calendar-feed, or availability change.

## Cumulative status through PR #188 (2026-06-10)

HISTORICAL snapshot as of PR #188; the rolling "Current production status" entries at the top of this doc supersede it. Kept for the detailed per-PR history below.

**Payment status:** Test-mode session payments are built end-to-end. Live payments are still blocked. Fees are not active.

**Stripe gate status** (pinned by `scripts/check-stripe-gates.mjs` + per-PR tests):

```text
paymentIntents.create   exactly 2  (lib/billing/manual-fee-charge.ts, lib/billing/session-payment-charge.ts)
                                   [superseded: exactly 1 in session-payment-charge.ts since PR #218]
refunds.create          exactly 1  (lib/billing/payment-refund.ts)
charges.create          0
checkout.sessions       0
STRIPE_ALLOW_LIVE_MODE  guarded    (string appears only in lib/stripe/server.ts error message)
```

**Completed (PRs #170-#187):**

```text
#170  Card authorization current-version gate
#171  Canonical payment_charge_attempts ledger
#172  Prepare session payment
#173  Run Stripe test charge
#174  Payment status UX
#175  Test-mode receipts
#177  Card authorization pointer refresh
#178  Test-mode refunds
#179  Webhook reconciliation
#180  Appointment completion/session-start workflow unblock
#181  Session completion to billing UI cleanup
#182  Calendar feed token hash-at-rest phase 1
#183  Portal last_seen_at fix
#184  DST two-pass conversion fix
#185  localTimeString 24:xx normalization
#186  Explicit server-only dependency
#187  Waitlist/demo public form rate limits
```

**Still open:**

```text
Live payment readiness
Legal review of card authorization wording
Tax/HST decision
Statement descriptor review
Off-session SetupIntent confirmation before live
Live runbook
Dispute response runbook
Willow live Stripe onboarding
Supervised first live charge
Late cancellation/no-show fee charging
manual_fee_charge_attempts unification/retirement
Calendar feed phase 2
Docs/launch checklist final polish
```

**Calendar feed phase 2:** Not started. A parked WIP commit exists locally (branch `claude/calendar-feed-token-hash-phase-2`, not pushed). Do not proceed until real Google/Apple calendar subscriptions are confirmed still polling cleanly after phase 1. Renumber the old parked PR/migration labels before using.

## Earlier production status (as of PR #187)

- **Waitlist + demo request rate limiting** (PR #187, no migration). The anonymous landing-page actions `submitWaitlistEntry` and `submitDemoRequest` previously had no rate limit. Both now route through `lib/rate-limit/public.ts` via new `limitWaitlistSubmit` / `limitDemoRequestSubmit` helpers (shared `limitMarketingForm` implementation): 5/hour per IP + 2/day per normalized email, checked after validation and before the Supabase insert, with namespaced Redis prefixes (`rl:waitlist_*`, `rl:demo_*`), SHA-256-hashed identifiers, the shared generic `RATE_LIMIT_MESSAGE` refusal copy, and the module's standard FAIL-OPEN posture on Upstash outage. No raw body or PII logging. No payment behavior change, no Stripe behavior change (gates unchanged from PR #186), no migration, no email/SMS sending, no portal or calendar feed change.

## Earlier production status (as of PR #186)

- **Explicit server-only dependency** (PR #186, no migration). `server-only@0.0.1` is now declared in `dependencies`. The 23 runtime server modules using `import "server-only"` as a client-bundle security boundary previously resolved the package only through Next's internal vendored alias; it was missing from package.json and the lockfile, so a Next upgrade could have silently weakened the boundary. No runtime behavior change intended; the Vitest stub alias is unchanged and takes precedence in tests. `tests/dependencies/server-only-explicit.test.ts` pins the declaration. No payment behavior change, no Stripe behavior change (gates unchanged from PR #185), no migration, no portal logic change, no calendar feed phase 2.

## Earlier production status (as of PR #185)

- **localTimeString hour-24 normalization** (PR #185, no migration). Some ICU builds resolve Intl's `hour12: false` to the h24 hour cycle and render hour 0 as "24" ("24:30" instead of "00:30"); the PR #184 CI run surfaced this when its runner ICU emitted 24:xx where dev machines emitted 00:xx. `lib/booking/tz.ts:localTimeString` now rewrites a leading `24:` to `00:` via a private `normalizeHour24` helper, so HH is always 00-23 on every runtime (calendar grid labels, dashboard roster, SMS templates). `tzOffsetMinutes` already guarded the same quirk numerically; `localTimeString12h` (h12 cycle) and `localDateString` (date-only) are unaffected and pinned by test; `utcInstantFromLocal` untouched. The PR #184 DST round-trip tests now exercise the production normalization directly (the test-side copy was removed). No conversion logic change, no payment behavior change, no Stripe behavior change (gates unchanged from PR #184), no migration, no new dependency.

## Earlier production status (as of PR #184)

- **DST two-pass offset correction in utcInstantFromLocal** (PR #184, no migration). `lib/booking/tz.ts:utcInstantFromLocal` previously applied a single offset correction sampled at the naive instant; when the naive and corrected instants straddle a DST transition the sample is the pre-transition offset and local times in the hours after a spring-forward jump were stored one hour late (Toronto 2026-03-08 `03:30` -> `08:30Z` -> rendered back `04:30`; `05:30` -> `10:30Z` -> `06:30`). The fix re-samples the offset at the corrected instant and re-applies when it differs. Behavior is unchanged for every wall-clock time that exists: normal days, already-correct DST-day times, and the fall-back ambiguous hour (still resolves to the first, pre-transition occurrence). Only nonexistent spring-forward gap times changed convention (now map one hour before the wall string; pinned by test). Regression suite `tests/lib/booking/tz-dst-two-pass.test.ts` covers Toronto spring-forward round-trips (03:30, 05:30, 09:00, 00:00), 23/24/25-hour day-window spans, fall-back conventions, and zero-dependency pinning. No payment behavior change, no Stripe behavior change (gates unchanged from PR #183), no migration, no new dependency, no portal or calendar feed change.

## Earlier production status (as of PR #183)

- **Client portal session last_seen_at lazy-builder fix** (PR #183, no migration). `lib/portal/session.ts:getCurrentPortalSession` previously "fired" the `last_seen_at` touch as a bare `void admin.from("client_portal_sessions").update({ last_seen_at: nowIso }).eq("id", data.id);`. Supabase/PostgREST builders are lazy thenables: without `await` or `.then(...)` no request is sent, so `last_seen_at` was never written despite the fire-and-forget comment. The fix appends `.then(onFulfilled, onRejected)` so the update actually executes while staying un-awaited (a slow touch never blocks the portal render). The fulfilled arm inspects the PostgREST `{ error }` result, the rejected arm covers transport throws; both log the sanitized structured event `portal_session_last_seen_update_failed` with only the session id, error code/message, and timestamp (no cookie token, no token hash, no email, no client PII). A failed touch still resolves the session normally. No schema change (`last_seen_at timestamptz` exists since migration 0052), no auth behavior change (hash lookup, revoked/expired checks, cookie attributes, login timing padding all untouched), no payment behavior change, no Stripe behavior change (gates unchanged from PR #182: 2 allowlisted `paymentIntents.create`, 1 allowlisted `refunds.create`, zero `charges.create`/`checkout.sessions`, 1 allowlisted `STRIPE_ALLOW_LIVE_MODE=true` string), no calendar feed phase 2, no SMS.

## Earlier production status (as of PR #182)

- **Calendar feed token hash-at-rest, phase 1** (PR #182, migration 0079). Closes `docs/13` security backlog item "Hashed `practitioners.calendar_feed_token` | Currently stored raw" for the runtime route + future rotations without breaking existing in-the-wild feed URLs. Migration 0079 adds nullable `practitioners.calendar_feed_token_hash text` + a 64-hex-char CHECK + a partial unique on the hash + a backfill via `encode(extensions.digest(calendar_feed_token, 'sha256'), 'hex')` (pgcrypto already enabled by migration 0001; Supabase installs it under the `extensions` schema, matching the 0032 precedent). The runtime feed route `app/calendar-feed/[token]/route.ts` now hashes the URL token via the new `lib/calendar-feed/token.ts:hashCalendarFeedToken` helper and looks up by `calendar_feed_token_hash`; the raw `calendar_feed_token` column is no longer in the route's SELECT list so a database read that leaks a row to logs does not include the bearer token. `rotateCalendarFeedTokenAction` + `clearCalendarFeedTokenAction` in `app/(app)/settings/profile/actions.ts` now use the shared `generateCalendarFeedToken` + `hashCalendarFeedToken` helpers and write BOTH columns (rotate writes both; clear nulls both). The raw column is intentionally KEPT in phase 1 for rollout safety because `CalendarFeedCard.tsx` still renders the existing URL from the raw column on page render; phase 2 (a later PR) refactors the UI to display the URL only at rotation time and then nulls the raw column. Prod verified post-migration: 2 practitioners had raw tokens; both now have well-formed 64-hex hashes; zero raw-without-hash or hash-without-raw mismatches; live-mode CHECKs intact. Stripe gates unchanged from PR #181 (2 allowlisted `paymentIntents.create`, 1 allowlisted `refunds.create`, zero `charges.create`/`checkout.sessions`, 1 allowlisted `STRIPE_ALLOW_LIVE_MODE=true` string). No SMS, no email, no payment behaviour change, no RLS change, no payment_charge_attempts touch.

## Earlier production status (as of PR #181)

- **Session completion to billing workflow + payment UI cleanup** (PR #181, no migration). Two surfaces touched. Calendar: new `NextStepCard` on `app/(app)/calendar/[id]/page.tsx` replaces the bare `Completed` placeholder. Renders "Appointment completed" + "Next step: chart the session and bill the client." + ONE primary CTA chosen from three states: no linked session -> `Start session` (forwards to `/clients/<id>/sessions/new?appointment_id=<id>`), linked unstarted session -> `Open session`, linked started session -> `Go to billing` (deep-links to `#session-payment` on the session page). `ChartSessionCard` is now hidden when status is `completed` so the new card owns the CTA surface (it still renders for confirmed / no_show; cancelled hides both). Session payment card cleanup (`components/session-payment-prepare-card.tsx`): (1) the stale "Session payment prepared / Attempt id: ... / No charge has been run / Refresh to see the persisted state" banner is replaced with a concise "You can now run the test charge." line gated on `!activeAttempt`, so it disappears as soon as the persisted Ready row catches up; (2) `router.refresh()` is now called after a successful prepare so the persisted ready row replaces the local banner immediately; (3) `SucceededPanel` promotes `refund_status='succeeded'` to the top heading: when refunded the panel uses the amber palette, reads "Test payment refunded.", and shows a `Refund details` block (Amount refunded / Refunded / Refund id) directly under the charge details. Receipt + Refund sub-panels continue to render below as the per-section detail; "Refund test charge" button stays hidden when `refund_status='succeeded'`. Session detail page: `<div id="session-payment">` anchor wraps the payment card so the calendar `Go to billing` deep link lands precisely on the payment surface. Receipt template: `lib/email/templates/payment-receipt.ts` `NO_REFUND_BODY_DISCLAIMER` renamed to `REFUND_AVAILABLE_BODY_DISCLAIMER` with new copy "If this test payment needs to be refunded, the practitioner can issue a test-mode refund in Hone." (PR #178 made test-mode refunds available; the old "Refund handling is not enabled in Hone yet." disclaimer is gone from both text and html bodies). No payment behavior change. No new Stripe call. No live mode. No migration. No SMS / client-portal / `manual_fee_charge_attempts` touch. Stripe gates unchanged from PR #180.

## Earlier production status (as of PR #180)

- **Appointment completion + session-start workflow unblock for payment smoke** (PR #180, no migration). Re-exposes the "Mark completed" button on `app/(app)/calendar/AppointmentLifecycleActions.tsx` (removed earlier per pre-payments feedback) alongside the existing Mark no-show. Same gating: `status === "confirmed"` AND `hasEnded`. Two-click `window.confirm` with the exact copy "Mark this appointment completed? This marks the appointment completed and allows the session to be charged after charting." Success hint "Appointment marked completed." Primary (filled) styling so the happy path is the obvious affordance. ALSO adds `maybeMarkAppointmentCompletedOnSessionStart` in `app/(app)/clients/[id]/sessions/new/actions.ts`: after a session insert with a linked appointment, if `appointment.status='confirmed'` AND `appointment.ends_at <= now()`, the helper calls the `mark_appointment_complete` RPC via the admin client. Cancelled / no-show / completed / future appointments are explicitly skipped. Fail-soft: RPC errors are logged (`session_start_auto_mark_complete_rpc_error` / `session_start_auto_mark_complete_threw`) but never rethrown, so a failed auto-complete cannot break session start. The appointment SELECT in the action is widened from `id, studio_id, client_id, practitioner_id` to also include `status, ends_at` so the auto-complete decision is made off the same roundtrip as the lineage check. The PR #172 payment prepare gate (`appointment.status='completed'`) is unchanged. No new Stripe call, no live mode, no migration, no payment_charge_attempts change, no manual_fee_charge_attempts change, no SMS, no client-portal mutation. Stripe gates unchanged from PR #179.

## Earlier production status (as of PR #179)

- **Stripe webhook reconciliation for payment_charge_attempts** (PR #179, no migration). Adds four event handlers in `lib/billing/payment-webhook-reconciliation.ts` dispatched from `app/api/stripe/webhook/route.ts`: `payment_intent.succeeded`, `payment_intent.payment_failed`, `charge.refunded`, `charge.dispute.created`. The existing `stripe_events` ledger (migration 0032) + `claim_stripe_event` RPC chain provide Stripe-event idempotency; no new ledger needed. Test mode only: `event.livemode === true` is a hard dormancy guard that records a warning `stripe_webhook_livemode_event_ignored` ops_alert and returns without mutation. Reason-agnostic by construction (handlers read `row.charge_reason`, never branch on it). Row-lookup order: canonical metadata `hone_payment_charge_attempt_id` → legacy `hone_session_payment_charge_attempt_id` (PR #173 backward compat) → fallback by `stripe_payment_intent_id` or `stripe_charge_id`. Metadata mismatch on studio/client/reason fires a critical `stripe_webhook_metadata_mismatch` and refuses to mutate. State-transition discipline: a row already in a terminal local state (`failed`/`cancelled`/`blocked`/`refund_status=succeeded`) is NEVER silently flipped; mismatch fires critical ops_alert (`payment_intent_succeeded_local_terminal_mismatch`, `payment_intent_failed_after_local_succeeded`, etc.). Out-of-band FULL refunds (Stripe Dashboard) are reconciled to `refund_status='succeeded'` with a warning `charge_refunded_out_of_band_reconciled`. Out-of-band PARTIAL refunds fire critical `charge_refunded_partial_out_of_band` and leave the row alone (v1 schema cannot represent partial). Disputes are alert-only (`payment_charge_dispute_created`); no automated response. Stripe gates unchanged: 2 allowlisted `paymentIntents.create`, 1 allowlisted `refunds.create`, zero `charges.create` / `checkout.sessions`, 1 allowlisted `STRIPE_ALLOW_LIVE_MODE=true` string. The webhook helper itself imports `recordOpsAlert` and `createAdminClient` only; it does NOT import `getStripe` (no SDK access from the reconciliation module). No SMS, no email, no client-portal mutation, no `manual_fee_charge_attempts` touch, no migration.

## Earlier production status (as of PR #178)

- **Reason-agnostic test-mode refunds on payment_charge_attempts** (PR #178, migration 0078). Adds nine nullable refund columns to `public.payment_charge_attempts` + a single allowlisted `refunds.create` call site in `lib/billing/payment-refund.ts` + a `RefundSubPanel` inside `SucceededPanel` only. Reason-agnostic by construction: the helper records `charge_reason` as Stripe-refund metadata and never branches on the value, so a future `late_cancellation_fee` / `no_show_fee` row refunds with no code change. v1 is full-refund only (`refund_amount_cents = amount_cents`); one refund per attempt enforced by partial unique on `stripe_refund_id`. Triple dormancy guard: `inferStripeLivemode()` short-circuit at entry, `payment_charge_attempts_livemode_false_check`, conditional UPDATE claim requires `status='succeeded' AND stripe_livemode=false AND (refund_status IS NULL OR refund_status='failed')`. Deterministic idempotency key `hone:payment_refund:<attemptId>:v1` + partial-unique `payment_charge_attempts_refund_idempotency_uniq`. Stripe gates: 2 allowlisted `paymentIntents.create`, 1 allowlisted `refunds.create` (`lib/billing/payment-refund.ts`), zero `charges.create` / `checkout.sessions`, 1 allowlisted `STRIPE_ALLOW_LIVE_MODE=true` string. UI uses two-click confirm with the amount in the second button; forbidden copy ("Live refund", "Refund complete", "Money returned", "Official refund receipt") pinned absent by negative source-grep tests. Migration 0078 applied to prod before merge; live-mode CHECKs intact. No webhook handling of `charge.refunded` (out-of-band Stripe-dashboard refunds NOT reconciled). No dispute automation. No refund receipt email. No SMS. No client-portal refund surface. No `manual_fee_charge_attempts` touch (the legacy 0032 `stripe_refunds` tables remain dormant; PR #178 ships refund state ON `payment_charge_attempts` directly).

## Earlier production status (as of PR #177)

- **Card authorization pointer refresh + tightened charge gate** (PR #177, migration 0077). Closes the `docs/16` §5.11 audit-trail gap surfaced in PR #176. Three pieces ship together: (1) `lib/payment-methods/refresh-card-authorization-pointer.ts` is called from `app/portal/consent-actions.ts` after a successful `card_authorization` signature insert; it updates active, non-removed `client_payment_methods` rows for `(studio_id, client_id, stripe_livemode=inferStripeLivemode())` to `card_authorization_signature_id = newSignatureId`. Fail-soft (critical `card_authorization_pointer_refresh_failed` ops_alert; never rolls back the signature). (2) Migration 0077 backfills existing prod rows; idempotent via `IS DISTINCT FROM`; scoped strictly by studio/client/livemode/status='active'/removed_at IS NULL/signature.template_version=template.version. NOTICE confirms row count. Applied to prod 2026-06-08; 1 row updated (the known Sai @ My Studio case); known row pointer went `a6b1fdbe-…0dd4` → `cd3af5cb-…a278`. (3) `getChargeReadyCardAuthorizationStatus` (new in `lib/consent/current-card-authorization.ts`) wraps the base PR #170 helper, adds the card-row pointer-equality check, returns the existing four variants plus `signed_current_but_card_pointer_stale`. Wired into session payment PREPARE eligibility + session payment EXECUTE recheck; remedy copy "Client must re-sign the current card authorization for the card on file." Critically, the charge-only helper is NOT used by `app/portal/consent-actions.ts` (re-sign), `app/portal/payment-method-actions.ts` (Add Card / Replace Card), or `lib/billing/manual-fee-eligibility.ts` (which already gates on the card row pointer); the deadlock-prevention contract is enforced both by code and by tests. Live-mode CHECKs (`manual_fee_charge_attempts_livemode_false_check`, `payment_charge_attempts_livemode_false_check`) verified intact post-migration. PR #175 receipt smoke can now resume against the repaired row. No new Stripe call, no live mode, no refund, no receipt-behavior change beyond eligibility unblock, no SMS, no client-portal payment UI expansion, no manual_fee runtime touch.

## Earlier production status (as of PR #175)

- Production domain: `https://hone.care`.
- Default branch: `claude/build-hone-saas-hOex7`. Every push to it triggers a production deploy. Vercel project: `prj_pJUjs6ImP01FBPqrZyiJRpbpJ2mk`, team `team_Pwj27KsmnBKe3ZUBfKLcFczf`.
- 94 migrations in-tree. Most recent: `0094_tenant_consistency_constraints.sql` (PR #278 — **not yet applied to production**; 0093 is the latest APPLIED). The next migration is `0095`. Always double-check the highest file in `supabase/migrations/` before assuming the count.
- Practitioner notification center (PR #164, migration 0070) records business events (`new_booking`, `appointment_cancelled`, `appointment_rescheduled`) into `public.practitioner_notifications`. Writes happen via the server-only `lib/notifications/practitioner-notifications.ts:recordPractitionerNotification` helper (admin/service-role client, never-throws fire-and-forget IIFE; a notification failure cannot roll back the booking / cancel / reschedule that just committed). Reads + mark-all-read happen via the authenticated RLS client on `/notifications`. Visibility is studio-wide in v1; `practitioner_id` is stored for future per-practitioner filtering. Separate from `ops_alerts` (which is the operator surface for system failures, PR #153).
- Thermolysis duration is fractional (PR #165, migration 0071). `electrolysis_entries.thermolysis_duration_seconds` is `numeric` (was integer in migration 0042). The form input uses `step="0.01"` + `inputMode="decimal"`; the read view routes through `lib/sessions/format-seconds.ts:formatSeconds` which yields `"0.15 seconds"` / `"1 second"` / `"2 seconds"`. Only the thermolysis column was widened; galvanic_duration_seconds and intensity_percent fields stay integer.
- Portal magic-link expiry is **60 minutes** (PR #166, raised from 30 minutes). The TTL constant `MAGIC_LINK_TTL_MS` lives in `app/portal/login/actions.ts` and is the single source of truth; the email body copy ("This link expires in 1 hour.") in `lib/email/templates/portal-magic-link.ts` is pinned by `tests/lib/email/portal-magic-link.test.ts`. No migration. The GET/POST split-consumption model (PR #142), the single-use atomic UPDATE on `consumed_at IS NULL`, and the 7-day portal session cookie TTL are all unchanged. PR #166 also flipped the portal-header right cluster from `flex-col items-end` to `flex-row items-center` so Sign out sits visibly at top-right next to the Email <studio> button instead of stacked below it.
- **Reason-agnostic test-mode receipt path** (PR #175, migration 0076). Adds the receipt-state columns to `public.payment_charge_attempts` and ships `lib/billing/payment-receipt.ts:sendPaymentChargeReceipt` + `lib/email/templates/payment-receipt.ts:buildPaymentReceiptEmail` (uses the existing `sendEmailSafely` helper from `lib/email/send-appointment.ts`). The helper is reason-agnostic: today only `session_payment` succeeded rows exist, but `late_cancellation_fee` and `no_show_fee` rows will work without code changes. UI: new `ReceiptSubPanel` inside `SucceededPanel` reads `receipt_status` from the persisted row so the already-sent / failed / sending states survive page refresh; a Send test receipt button appears only when status is null or failed. Atomic dedup via conditional UPDATE on `receipt_status IS NULL OR receipt_status = 'failed'` → `'sending'`. Subject "TEST MODE receipt from <studio>: <reason> $X.XX CAD". Body carries three disclaimers ("This is a Stripe test-mode receipt. No live card was charged.", "No tax calculation is included on this receipt.", "Refund handling is not enabled in Hone yet."). No auto-send from `runSessionPaymentCharge` -- the spec is explicit. No new Stripe call. No live mode. No refund. No SMS. No client-portal change. No manual_fee touch. Forbidden copy ("Pay now", "Send invoice", "Tax receipt", "Official invoice", "Payment complete", "Live payment") absent from the receipt surface, pinned by negative source-grep tests. Migration ledger advanced to 0076.
- **Session payment UX hardening** (PR #174, no migration). `SessionPaymentPrepareCard` refactored so every post-refresh state renders rich detail driven by the persisted `payment_charge_attempts` row. New `AttemptStatusPanel` dispatcher switches on `attempt.status` and returns one of `ReadyPanel` / `PendingPanel` / `SucceededPanel` / `FailedPanel` / `CancelledPanel` / `BlockedAttemptPanel` (mirrors `ManualFeeChargeCard.tsx` precedent). `SessionPaymentExistingAttemptSummary` widened to carry `stripePaymentIntentId / stripeChargeId / chargedAt / failedAt / failureCode / failureMessageSafe`; `getSessionPaymentEligibility` SELECT widened accordingly. `SucceededPanel` displays the PaymentIntent + Charge ids + charged-at timestamp + explicit "Stripe test-mode charge. No live card was charged. No receipt was sent in this PR." `FailedPanel` displays the sanitised failure message + failure code + failed-at + PaymentIntent id with "Prepare a new session payment attempt" guidance. `STATUS_LABEL` map updated to reflect post-PR-#173 reality (`ready: "Ready (test mode)"`, `succeeded: "Succeeded (test mode)"`, `failed: "Failed (test mode)"`, `pending_stripe: "Pending Stripe (test mode)"`). Forbidden copy ("Pay now", "Charge card", "Collect payment", "Payment complete", "Live payment", "Receipt sent") absent from actionable JSX (pinned by negative source-grep tests). No new Stripe call. No webhook change. No SMS / email. No client-portal change. No migration. Migration ledger remains 0075.
- **Session payment EXECUTE flow** (PR #173, migration 0075, test mode only). Adds `lib/billing/session-payment-charge.ts:runSessionPaymentCharge`, a faithful port of `runManualFeeCharge` adapted for `payment_charge_attempts`. Stripe gate script + live-mode-blocker test updated to allow exactly 2 allowlisted `paymentIntents.create` call sites (`lib/billing/manual-fee-charge.ts` + the new file). Migration 0075 adds the atomic claim RPC `claim_session_payment_charge_attempt` (mirror of 0065's manual fee RPC). Execution flow: live-mode early return -> attempt + reason + livemode-row guards -> PR #170 current-card-authorization recheck (signature id must still match the stamped value) -> lineage recheck -> claim RPC (status='ready' -> 'pending_stripe' + deterministic idempotency key `hone:session_payment:<attemptId>:v1`) -> `paymentIntents.create({amount, currency: 'cad', customer, payment_method, confirm: true, off_session: true, description, metadata}, {stripeAccount, idempotencyKey})` -> write succeeded (PI id, charge id, charged_at) or failed (sanitised failure_code + failure_message_safe + failed_at + ops_alert). No `application_fee_amount`, no `receipt_email`, no `statement_descriptor_suffix`. UI: new "Run test charge" button on `SessionPaymentPrepareCard`'s existing-attempt branch with a two-click confirm + clear test-mode disclaimer; no "Pay now" / "Charge card" label. No live mode. No receipt. No refund. No webhook business logic added. No SMS / email. manual_fee_charge_attempts runtime untouched. Per-file `paymentIntents.create` count: 1 in each of the two allowlisted files; `STRIPE_ALLOW_LIVE_MODE=true` still 1 in `lib/stripe/server.ts` only.
- **Session payment PREPARE flow** (PR #172, test mode only). The session detail page (`app/(app)/clients/[id]/sessions/[sessionId]/page.tsx`) now renders a `SessionPaymentPrepareCard` after `SessionInfoCard`. The card resolves eligibility via `lib/billing/session-payment-eligibility.ts:getSessionPaymentEligibility` and dispatches between blocked / existing-attempt / ready states. The action `prepareSessionPaymentChargeAction` writes one row to `public.payment_charge_attempts` with `charge_reason='session_payment'`, `status='ready'`, `stripe_livemode=false`, the practitioner-confirmed amount, the active card's lineage, and the PR #170 `signed_current` `card_authorization_signature_id`. The chargeability proxy (no `sessions.completed_at` column exists today) is `sessions.appointment_id IS NOT NULL AND appointments.status='completed' AND sessions.started_at IS NOT NULL`. NO Stripe call. NO PaymentIntent. NO charge. NO webhook. NO SMS. Duplicate protection via PR #171's `payment_charge_attempts_active_session_payment_uniq` partial unique + a 23505 catch in the action. UI explicitly does not render any "Pay now" or "Charge card" button; the disclaimer reads "This prepares a test-mode payment record. It does not charge the client." manual_fee_charge_attempts runtime fully untouched.
- **Canonical `payment_charge_attempts` ledger** (PR #171, migrations 0073 + 0074). Dormant: 0 rows in production. First writes land in PR #181 (test mode only) for `session_payment`. Schema covers all three charge reasons (`session_payment`, `late_cancellation_fee`, `no_show_fee`); status enum mirrors `manual_fee_charge_attempts` exactly (no parallel state machine); amount_cents bounded `> 0 AND <= 200000` ($2,000 CAD vs manual_fee's $200, intentionally larger for full session amounts); `payment_charge_attempts_livemode_false_check` is the named dormancy guard the future live-enablement PR drops deliberately; `payment_charge_attempts_reason_shape_check` enforces session_payment-requires-session_id (appointment_id OPTIONAL so a future freeform-session charge does not need a relax migration) AND cancellation/no-show-requires-appointment_id-and-forbids-session_id; `card_authorization_signature_id` nullable in this dormant PR (execution PR #181 must refuse to charge unless `lib/consent/current-card-authorization:getCardAuthorizationStatus` returns `signed_current` AND stamps the matching signature id); FK ON DELETE rules audited (studio CASCADE; client/appointment/practitioner composite RESTRICT; session_id RESTRICT after corrective 0074 -- initial 0073 declared SET NULL but that contradicted the reason_shape_check requiring session_payment rows to have non-null session_id; signature/payment_method RESTRICT); 12 secondary + 4 partial-unique indexes (idempotency, stripe_payment_intent, active-fee-per-appointment, active-session_payment-per-session) match manual_fee's three-layer duplicate-protection pattern; RLS enabled with studio-member SELECT only, no INSERT/UPDATE/DELETE policy, service-role admin owns mutations. **TEMPORARY TWO-TABLE STATE:** `manual_fee_charge_attempts` remains the runtime test-mode ledger; runtime fee charging must be migrated or unified onto `payment_charge_attempts` BEFORE live `late_cancellation_fee` or `no_show_fee` charging ships (see docs/13 + docs/16 §12.5b for the dated checkpoint + gate language).
- **Card authorization current-version re-sign gate** (PR #170). The product-ready DRAFT body lives at `lib/consent/card-authorization-draft.ts:CARD_AUTHORIZATION_DRAFT_V1_BODY` (around 2.5 kB; covers card on file, completed-session off-session charges, late cancellation, no-show, receipts/refunds/disputes, payment processing + privacy, scope + revocation; explicit "does not waive my dispute rights" line; no legal-approval claim). The shared helper `lib/consent/current-card-authorization.ts:getCardAuthorizationStatus` returns one of four kinds (`no_live_template` / `unsigned` / `signed_out_of_date` / `signed_current`); `createCardSetupIntentAction` refuses unless `signed_current`; `manual-fee-eligibility.ts` performs the same comparison inline. Portal page renders a new "Card authorization was updated" block when out-of-date; the practitioner `PaymentMethodCard` renders `AuthorizationOutOfDateBlock` (no card) or `AuthorizationOutOfDateWarning` (active card present). The unsignedConsentTemplates filter special-cases card_authorization so re-signing reuses the existing Review and sign UI; other consent types do NOT force re-sign on every edit. No migration. No production data change in this PR -- the operator step (Chloe pastes the body via Settings -> Consent forms, which bumps version 1 -> 2 via the existing updateConsentTemplateAction) is documented in docs/05; until then existing "test" signatures continue to satisfy version=1 and nothing visible changes for current clients.
- **Session payment product model defined** (PR #169, docs + guardrails only). [docs/16 §12](./16_LIVE_PAYMENTS_READINESS.md#12-session-payment-product-model-pr-169) carries the v1 model: charge AFTER the session with a practitioner-confirmed amount (no auto-charge from `services.price_cents` or duration), one charge primitive parameterized by `charge_reason` (`session_payment` / `late_cancellation_fee` / `no_show_fee`), off-session SetupIntent already satisfied at `lib/stripe/setup-intent.ts:202`, 0% Hone platform fee in v1, no tax calculation, paid status derived from charge rows (no boolean), studio is merchant of record, risk-ordered enablement (session_payment ships live first, then cancellation, then no-show). The PR sequence in docs/16 §11 + §12.13 was renumbered: PR #169 is the product model, PR #170 is the legal review of `card_authorization` wording (was original PR #169), and the session_payment build PRs are #179 to #183. No schema commitment yet (separate table vs unified table with reason enum); the schema PR's own audit decides.
- **Live payments are NOT enabled.** PR #168 is a docs + guardrails-only readiness review concluding **NOT READY FOR LIVE PAYMENTS**. Three independent dormancy guards remain in place: (1) `lib/stripe/server.ts` rejects `sk_live_` keys unless `STRIPE_ALLOW_LIVE_MODE=true`; (2) `lib/billing/manual-fee-charge.ts:runManualFeeCharge` short-circuits with `outcome: "live_mode_blocked"`; (3) migration 0065 adds `CHECK (stripe_livemode = false)` on `manual_fee_charge_attempts`. The full readiness doc + 9-PR unblock sequence lives at [docs/16_LIVE_PAYMENTS_READINESS.md](./16_LIVE_PAYMENTS_READINESS.md). The "Test mode only" UI copy in 7 locations across the portal + practitioner surfaces survives PR #168 deliberately; PR #170 will remove it as part of the live-mode-enablement track. Willow's `card_authorization` template is still titled `"test"` (not legally reviewed); PR #169 covers the legal review.
- Consent templates have a **Live in client portal** control separate from the practitioner-facing `status` enum (PR #167, migration 0072). `consent_form_templates.is_live boolean NOT NULL DEFAULT false`; backfilled to `is_live = (status = 'active')` to preserve pre-migration portal visibility. DB CHECK constraint `(NOT is_live OR status = 'active')` guarantees a draft / archived row can never be live. The portal query in `lib/consent/queries.ts:getActiveConsentTemplatesForPortal` filters `is_live=true AND status='active'` (defense-in-depth); the portal Add card flow in `app/portal/payment-method-actions.ts` and the sign action in `app/portal/consent-actions.ts` apply the same two-clause gate. `createConsentTemplateAction` forces `status='draft'` and `is_live=false` on insert; `setConsentTemplateStatusAction` auto-sets `is_live=false` when moving to draft or archived; a new `setConsentTemplateLiveAction` is the only path to `is_live=true` and pre-flights the requirement that the row be active first. The `Settings -> Consent forms` UI gained a `Live` / `Draft` badge per row and a `Make live in client portal` / `Hide from client portal` button. Historical `client_consent_signatures` are untouched; the "Completed forms" portal surface reads signatures directly without joining the templates table. Audience targeting (per-modality / per-service / new-vs-existing-client) is deferred to a future PR. No payment / live-mode / SMS / RLS change.
- Client profile (`app/(app)/clients/[id]/page.tsx`) Sessions tab leads with an Appointments timeline (PR #157) that groups every appointment into Upcoming / Needs charting / Charted / Cancelled / No-show with per-row Chart / View / Open affordances. The query helper is `getAppointmentsForClientProfile` (`lib/supabase/queries.ts`); the component is `components/client-appointment-timeline.tsx`. No service role.
- Portal card-on-file section (PR #158) renders one of four explanatory states (no template configured / authorization needed / signed but no card / active card) with a deep-link `#forms-to-sign` from the "Card authorization needed" placeholder to the existing "Review and sign forms" block. Matching practitioner-side `PaymentMethodCard` on the client profile renders the same four branches with practitioner-actionable copy. Manual fee blocked-reason strings (`lib/billing/manual-fee-eligibility.ts`) updated to tell the practitioner exactly what to ask the client to do next. No new schema, no new RPC, no new payment behavior.
- Portal layout (PR #159) replaces the legacy "Your info" wrapper with four top-level sections (Appointments / Care instructions / Forms and records / Payment method). Header now carries an `Email <studio>` contact button next to Sign out. Care instructions render `<details open>` so the client sees them without clicking. "Signed forms" renamed to "Completed forms" with quiet border-top divider styling so the rows do not read as actionable. PR #158 card-authorization guidance preserved and verified by existing tests.
- Pre-appointment instructions (PR #160) are studio/service-owned via the existing `services.pre_care_instructions` field (migration 0025), edited from `Settings → Services`. The same field feeds the booking confirmation email, both reminder emails, AND the portal Care instructions section. The prior hardcoded "Please arrive 5 minutes early. Wear comfortable clothing. Avoid caffeine before your appointment." paragraph was removed from `lib/email/templates/appointment.ts`; empty prep text now omits the block entirely. No migration; no payment / SMS / public-route behavior change.
- Charting terminology (PR #162). The `session_blocks.side` value `bilateral` now renders as `"Both sides"` everywhere (Chloe's charting feedback); the stored enum, the migration 0039 CHECK constraint, and the server validation array all keep the canonical `bilateral` lowercase value. Label mapping lives in `lib/sessions/side-labels.ts`; both `block-setup-form.tsx` and `session-blocks-view.tsx` read from it. Thermolysis input order in the block editor is now `Duration -> Intensity -> Pulse count` (was `Intensity -> Duration -> Pulse count`) to match Chloe's machine; persisted column names (`thermolysis_duration_seconds`, `thermolysis_intensity_percent`, `pulse_count`) are unchanged. No data migration.
- Booking referral attribution (PR #163, migration 0069). Public booking form asks "How did you hear about us?" with seven canonical options (Google / Instagram / Friend or referral / Existing client / Studio website / Other / Prefer not to say). Answer stored on the nullable `appointments.referral_source` text column; no CHECK constraint at the DB layer (option set enforced at the action layer in `lib/booking/referral-source.ts`). Practitioner sees the value on the calendar appointment detail page and in the new-booking notification email; client-facing surfaces (confirmation email, reminder emails, portal) deliberately do not surface it. v1 is appointment-level only.
- Card-on-file and test-mode manual fee charge are live in production **test mode**. Live mode is blocked by three independent guards.
- GitHub Actions CI (PR #154) runs typecheck, lint, build, `npm test`, `git diff --check`, and `npm run check:stripe-gates` on every PR and push to default. `npm run ci` is the local shortcut. A red CI check blocks merge. Since PR #220 a separate `db-integration` job also applies the full migration chain to a LOCAL Supabase Postgres and runs `npm run test:db` (tests/db/, real RLS/trigger/RPC behavior; local only, no secrets). If a PR touches migrations, policies, triggers, or claim RPCs, run the DB lane locally too: `supabase db start && supabase db reset --local && npm run test:db`.
- Ops alerts (PR #153, migration `0067_ops_alerts.sql`) capture silent failure states for the manual-fee path, Stripe webhook, SMS path, and the appointment-reminder cron. The SMS path stamps `studio_id` on alerts as of PR #155.
- Sessions can be explicitly linked to appointments via `sessions.appointment_id` (PR #156, migration `0068_sessions_appointment_link.sql`). Write-forward is partial: appointment-context flows stamp the FK, client-scoped session creation remains null. Historical sessions remain null; no backfill has been run. The dedup site in `lib/supabase/queries.ts:getPastConfirmedAppointmentsForClient` prefers the explicit link and falls back to the `+/- 2 hour` heuristic only for unlinked sessions.

## Branch / PR / deploy workflow

1. Branch off `claude/build-hone-saas-hOex7` with a short descriptive name.
2. Build, validate locally:
   ```bash
   npm run typecheck
   npm run lint
   npm run build
   npm test
   npm run check:stripe-gates
   git diff --check
   # or as one command:
   npm run ci
   ```
3. Commit as `SaiSamyukthVemuri <samyukth.ssv@gmail.com>` (the Vercel author gate requires this).
4. Push. Open a PR with the `.github/pull_request_template.md` checklist filled honestly.
5. STOP. Do not merge until reviewed.
6. On review approval: `gh pr merge <N> --merge`.
7. Poll the Vercel commit status:
   ```bash
   gh api "repos/SaiSamyukthVemuri/Hone/commits/<sha>/status" --jq '.state'
   ```
8. When `state=success`, confirm `READY` via Vercel MCP `get_deployment`.
9. Report merge SHA, deployed SHA, production status, any check failures.
10. Run the post-deploy smoke that you CAN do from a non-authenticated harness. Clearly say what could not be verified.
11. Do not start another PR until the deploy is `READY`.

## What not to touch casually

The list of high-risk areas. Each has a doc you MUST read before changing anything in it.

| Area | Doc | Why high-risk |
|---|---|---|
| Stripe / payment / live mode | [docs/06](./06_PAYMENTS_AND_STRIPE.md) | Money. Live charging is blocked by three independent guards; do not weaken any of them. |
| Token routes (`/cancel`, `/reschedule`, `/manage`, `/intake`, `/portal/verify`, `/calendar-feed`) | [docs/03](./03_SECURITY_AND_PRIVACY.md) | Token IS the credential. No analytics. noindex + no-referrer headers. |
| Portal session / magic link / `client_portal_magic_links` | [docs/03 §3](./03_SECURITY_AND_PRIVACY.md#3-portal-session-model) | The GET on `/portal/verify/<token>` is deliberately non-consuming. POST consumes. Do not flip this. |
| Manual fee `paymentIntents.create` | [docs/06](./06_PAYMENTS_AND_STRIPE.md), [docs/13](./13_BACKLOG_AND_DECISIONS.md) | The single allowed occurrence. Do not delete. Do not add a second. |
| RLS policies + `SECURITY DEFINER` RPCs | [docs/09](./09_DATABASE_AND_RLS.md) | Trust boundary. |
| Service-role client usage | [CONTRIBUTING.md](../CONTRIBUTING.md) | Never in `"use client"`. Never to bypass RLS as a convenience. |
| Analytics mount points | [docs/03 §6](./03_SECURITY_AND_PRIVACY.md#6-analytics-privacy-pr-142) | Root layout MUST NOT mount Analytics. Safe trees opt in. |
| `ADMIN_EMAILS`, `NEXT_PUBLIC_APP_ORIGIN`, `PORTAL_FINGERPRINT_SALT` fail-closed behavior | [docs/03 §7](./03_SECURITY_AND_PRIVACY.md#7-production-config-fail-closed-pr-143) | Production with missing values fails closed deliberately. |

## Payment safety rules (non-negotiable)

- **No live charges** unless `STRIPE_ALLOW_LIVE_MODE=true` AND a deliberate live-mode PR is open AND the `payment_charge_attempts_livemode_false_check` constraint (the canonical ledger CHECK) has been deliberately replaced AND the [docs/06 §9 checklist](./06_PAYMENTS_AND_STRIPE.md#9-live-charging-requirements) is complete.
- **No automatic / batch / background / public-triggered charge.** Charging is one manual practitioner click on a `ready` attempt.
- **No platform customer / platform PaymentMethod.** Every Stripe call must carry `{ stripeAccount }`.
- **No raw card / CVC / `client_secret` storage.**
- **No blind retry past the 60-minute reconciliation window.** Stripe idempotency is a 24-hour belt; the action's 60-minute window is the suspenders. Past that window, return `needs_manual_review`.

## PR review pattern

When reviewing a PR:

1. Read the PR template. Refuse to review further if the checklist is empty or dishonest.
2. Read the migrations (if any). Confirm RLS posture, grants, `search_path`, additive shape.
3. Read the diff for the search gates:
   ```bash
   git diff | grep -E '^\+' | \
     grep -E 'paymentIntents\.create|charges\.create|refunds\.create|checkout\.sessions|set_studio_require_card_on_file|STRIPE_ALLOW_LIVE_MODE=true'
   ```
4. Confirm no new Stripe SDK imports outside the existing helper.
5. Confirm the migration was applied to prod BEFORE the code references it.
6. Confirm docs are updated per [docs/15](./15_DOCS_MAINTENANCE.md).
7. Confirm the PR body lists what could not be verified.
8. Stripe-touching PRs: confirm idempotency, claim-then-act, evidence recheck, lineage recheck, test-mode gate.

## Standard validation commands

```bash
npm run typecheck
npm run lint
npm run build
npm test
git diff --check
npm run check:stripe-gates
```

Or `npm run ci` to run all six in sequence. All must pass before pushing. The Vercel preview deploy must reach `READY` before merge.

GitHub Actions runs the same six steps automatically on every PR and on every push to the default branch (`claude/build-hone-saas-hOex7`). See `.github/workflows/ci.yml`. A red CI check is a hard merge block. CI does NOT replace manual smoke; browser / Stripe Elements / real-send paths still need a human against the live deploy.

## Grep gates (current)

```
charges.create:                       zero
checkout.sessions:                    zero unless explicit Checkout PR
refunds.create:                       exactly one (lib/billing/payment-refund.ts, PR #178)
set_studio_require_card_on_file:      zero unless explicit card-required booking PR
STRIPE_ALLOW_LIVE_MODE=true:          zero unless explicit live-mode PR

paymentIntents.create:                exactly one occurrence allowed today:
                                      lib/billing/session-payment-charge.ts
                                      (the canonical executor; the legacy
                                      manual-fee-charge.ts was deleted in
                                      PR #218)

                                      Any new paymentIntents.create
                                      occurrence is high-risk and must be
                                      explicitly reviewed.
```

The single `paymentIntents.create` path is the **unified test-mode executor** `lib/billing/session-payment-charge.ts:runSessionPaymentCharge` (session payments AND late-cancel/no-show fees since PR #196/#218; **live payments remain disabled**). It is behind:

- practitioner auth
- reason-appropriate eligibility recheck (`getSessionPaymentEligibility` for session payments; `getManualFeeChargeEligibility` for fees)
- `loadCardAndVerifyLineage` lineage recheck
- `claim_session_payment_charge_attempt` RPC (`FOR UPDATE`, conditional UPDATE, idempotency key stamp in one transaction; reasons widened in migration 0083)
- deterministic idempotency key
- connected-account context `{ stripeAccount }`
- `inferStripeLivemode()` test-mode gate
- `payment_charge_attempts_livemode_false_check` DB CHECK

(The legacy `lib/billing/manual-fee-charge.ts` executor was removed in PR #218; charges now persist on the canonical `payment_charge_attempts` ledger.)

**Never live without an explicit live-mode PR.** A live-mode PR must add stronger stale-pending reconciliation (Stripe `paymentIntents.search` by metadata before retry) and must deliberately alter or drop the `livemode_false_check` constraint after review.

## Merge discipline

- One PR per logical change. Do not mix schema + product behavior + security in one PR.
- Migrations apply to prod BEFORE the code PR merges.
- The PR description is the contract; if it does not match the diff, the reviewer rejects it.
- Em-dashes in added lines: zero. Use plain hyphens or colons.
- Commits authored as `SaiSamyukthVemuri <samyukth.ssv@gmail.com>`.

## How to write prompts for future PRs

Useful patterns:

- State the goal first. Then state non-goals. Then the acceptance criteria.
- Always include the validation list (typecheck / lint / build / diff-check) and the grep gates.
- Always include "Do not merge until reviewed."
- Include the SQL the reviewer can run to verify state after merge.
- Include the explicit "what could not be verified" list.

## How to review PRs

- Read the diff before the description.
- Match every claim in the description to a line in the diff.
- If the description says "no schema change" and the diff shows a migration: stop.
- If the description says "no Stripe behavior change" and the diff adds a Stripe SDK import: stop.
- If docs are not updated for a behavior-changing PR: stop.
- Re-run the smoke against a preview deploy where possible.

## Current non-negotiables

Repeat the list in the PR you open, every time:

- Author commits as SaiSamyukthVemuri <samyukth.ssv@gmail.com>
- Zero em-dashes in added lines.
- Apply additive migration to prod BEFORE merging code that references new columns.
- "Do not merge until reviewed."
- "Do not start the next PR until deploy is READY."
- Stripe dormancy: no charges, no `require_card_on_file=true`, no live-mode enable, exactly one `paymentIntents.create`.
- Grep gates: `paymentIntents.create` (allowed only in `lib/billing/session-payment-charge.ts`, the canonical executor since PR #196/#218), `charges.create` (zero), `refunds.create` (only `lib/billing/payment-refund.ts`), `checkout.sessions` (zero), `set_studio_require_card_on_file` (zero), `STRIPE_ALLOW_LIVE_MODE=true`. Enforced by `scripts/check-stripe-gates.mjs` + the `npm run check:stripe-gates` script in CI (PR #154). The `STRIPE_ALLOW_LIVE_MODE=true` rule allowlists `lib/stripe/server.ts` because the string appears in an operator-facing error message there, NOT as a code path that flips the flag.
- **CSP discipline (PR #150).** The global CSP in `next.config.ts` (via `lib/security/headers.ts`) is the single source of truth. Any new third-party browser integration MUST extend the CSP source lists in the same PR. Never weaken `frame-ancestors 'none'` or `X-Frame-Options: DENY`. Never add wildcard `*`. Never add Sentry domains unless that PR actually installs Sentry. Token routes keep `Referrer-Policy: no-referrer`.
- **Ops alert hygiene (PR #153).** `lib/ops/alerts.ts:recordOpsAlert` is the single entry point for silent-failure alerts. NEVER throw from the helper to the caller; DB failures are swallowed and surface only as additional structured logs. NEVER put raw tokens / `client_secret` / Stripe secret keys / card data / CVC / API keys in `safe_details`; the helper has a defensive redactor but the contract is "the caller already redacted". **The helper MUST NOT import `lib/email/send-appointment.ts` or any module that imports it.** Operator email is intentionally deferred; the same module observes the email subsystem and cycling back through it (even with a loop guard) is avoidable. A future PR may add a standalone `lib/ops/alert-email.ts` that uses Resend directly. New silent-failure surfaces should reuse the helper and a stable event name (`<surface>_<state>`).

## If you find yourself wanting to…

- **Add a new `paymentIntents.create`:** stop. Read [docs/06](./06_PAYMENTS_AND_STRIPE.md). The only legitimate new call site is a live-mode PR that satisfies the [docs/06 §9](./06_PAYMENTS_AND_STRIPE.md#9-live-charging-requirements) checklist.
- **Use `createAdminClient` in a client component:** stop. Move the logic to a server action.
- **Mount Analytics on `app/layout.tsx`:** stop. See [docs/03 §6](./03_SECURITY_AND_PRIVACY.md#6-analytics-privacy-pr-142). Mount on the safe-tree layout instead.
- **Add a wildcard `*` to the CSP, weaken `frame-ancestors`, or add Sentry domains "just in case":** stop. Read [docs/03 § Global browser security headers](./03_SECURITY_AND_PRIVACY.md). The CSP is the single source of truth and additions require explicit review.
- **Trust a `studio_id` / `client_id` / `appointment_id` from formData:** stop. Resolve from the session or from the token.
- **Add `process.env.NEXT_PUBLIC_APP_ORIGIN ?? "https://hone.care"`:** stop. Use `getRequiredAppOrigin()` from `lib/app-origin.ts`. PR #143 removed every silent fallback.
- **Skip a doc update because "the PR is small":** stop. The doc update is part of the PR.

## What was not verified by the agent in PR #148

This documentation overhaul is itself a docs-only PR. I did NOT:

- Run any new code paths.
- Apply any new migrations (no schema change in this PR).
- Send any email or SMS.
- Trigger any Stripe call.

The validation gates that DID run: `typecheck`, `lint`, `build`, `git diff --check`, em-dash count, and a grep for stale `hone.studio` references. Each doc was cross-referenced against the actual code, migrations, and PR template at the moment of writing (PR #147 just merged; the deployed SHA was `3ab714f`).

Where a doc references a behavior, the citation is the PR number and the migration number so a future reviewer can spot-check by reading the migration or the action file directly.
